/** Cap mobileSessionPagination — initial visible roots + show-more step. */
export const getMobileSessionDefaultVisibleCount = (): number => 3;
export const getMobileSessionShowMoreIncrement = (): number => 7;
