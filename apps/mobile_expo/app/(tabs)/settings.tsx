import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function SettingsScreen() {
  return (
    <PlaceholderScreen
      title="Settings"
      body="Stub settings home. MOBILE_SETTINGS_PAGE_SLUGS land later. No Voice page. No openchamber.iosNativeUi toggle. Native is always on."
    />
  );
}
