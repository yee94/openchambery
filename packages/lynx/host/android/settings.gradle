pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Lynx Android SDK maven — not resolved on Linux agent.
        // maven { url "https://…" }
    }
}
rootProject.name = "OpenChamberLynx"
include ':app'
