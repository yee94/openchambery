# Lynx mobile rewrite — gap board

Independent track: **`work/lynx-native`** from `main` @ `b444b0316` (`v1.19.7-beta.7`). Do **not** merge to `main`.

Lynx package + Android host scaffold **exist** on `work/lynx-native` (代码接上). This board stays honest: product rows are **not** landed under 三关 until CI绿 + 真机过. Update a row only when code, CI, and (for ship) 真机 evidence exist. Status words:

| Status | Meaning |
|---|---|
| **landed** | On `work/lynx-native`, wired to real APIs, and at least CI-green for the claimed surface |
| **missing** | Not implemented (default) |
| **Android降级** | iOS has (or will have) a richer native treatment; Android ships a documented lesser analogue — not a broken iOS clone |
| **next** | Immediate implementation slice after the doc gate |
| **真机残差** | Code+CI exist; physical device still fails or is unproven |
| **故意不移植** | Cap/web has it, Lynx will not (with a reason) |

---

## Landed

| Item | Notes |
|---|---|
| Branch `work/lynx-native` from `b444b0316` | Docs gate |
| This documentation set | `docs/lynx-feature-inventory.md`, `docs/lynx-pitfalls.md`, `docs/lynx-gap-board.md`, `docs/lynx-acceptance.md`, `docs/lynx-ia-ui.md` |

## Scaffold (shell / host / glass — not 真机过)

These rows are **代码接上** for the scaffold contracts only (no OpenChamber HTTP). Package Vitest + `tsc` are the CI for this claim. They are **not** shipped.

| Item | Notes |
|---|---|
| Lynx app package | `packages/lynx` (`@openchamber/lynx`). Workspace Vitest project. No rspeedy/APK/IPA job yet. |
| Host Tab/Nav embedding decision | **Locked:** Mode B iOS 26 host `UITabBar`; Mode A older iOS + Android. Mode C forbidden. `src/host/embedding.ts` + `host/ios/` + `host/android/`. |
| Four-tab dock IA | Projects / Assistant / Scheduled / Settings. Chat is a pushed secondary page; dock hidden. Settings home lists real slug rows. Projects/Assistant/Scheduled tab **bodies** are 代码接上 in the tabs-home slice (see below). |
| Lynx 3.8 glass mapping | iOS `glass` → `UIGlassEffect`, `glass-container` → `UIGlassContainerEffect`, plus `glass-style` / `glass-interactive` / `glass-tint-color` / `spacing`. Android: `blur-radius` 降级. |

真机过: **not executed** (environment: Linux cloud agent; no Xcode/adb device).


## 代码接上 (connect client — not landed under 三关)

Client library modules under `packages/lynx` (`src/connection/`, `src/pairing/`, `src/session-index/`, `src/deep-links/`). No host LynxView splash UI, no track CI.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Pairing v2 parse + paste/QR payload | `connectionPayload.ts`, `mobileQrScan.ts` | v1 rejected. Camera scan is host-owned. No Nearby / Bonjour. |
| `openchamber://` parse + build | `deepLinks.ts` | Same intent union. Apply/navigation is still host. |
| Connect / auto-connect / password / redeem | `mobileConnections.ts` | Real `GET /health`, `GET|POST /auth/session`, `POST /api/client-auth/pairing/redeem`. Persists full LAN+relay candidate set. Token in injected secure store — never logged, never in metadata. |
| Connect race harness | `mobileConnections.ts` | Unit: relay-only skips the 1.5s LAN headstart (`src/connection/probe.test.ts`). |
| Session-index GET / pin / lookup | `session-index-api.ts` | `GET /api/openchamber/session-index`. Failure ≠ empty. Runtime-key cache. |
| Projects home data path | `useMobileProjectsHomeModel.ts` | `projectSessionIndexHome` + `createSessionIndexHomeBindings`. No pixel polish. |

**CI绿:** lynx-ci + lynx-mobile-ci live on tip (Android sideload). Not 真机过.

**真机过:** not executed (environment: Linux cloud VM; no Xcode, no adb, no physical device).

---

## 代码接上 (chat LegendList + settings home — not landed under 三关)

Chat timeline + Settings tab home on `cursor/lynx-chat-settings-local` (PR into `work/lynx-native`). Package Vitest + `tsc` gate the claim. No track CI / 真机过.

| Item | Cap/web source | Lynx note |
|---|---|---|
| LegendList-semantics timeline | `TimelineList.tsx` | `src/chat/listSemantics.ts` + `LynxTimelineList`: one `<list>`, `recycle-items={false}`, `initialScrollAtEnd` / `maintainScrollAtEnd` / `maintainVisibleContentPosition`, load-older **button** only (bounce forbidden). TanStack 1.18 forbidden. |
| Send / Stop / queue hooks | ChatInput + queue | `src/chat/composerActions.ts` + `sessionApi.ts` → official `POST /session/:id/prompt_async`, `POST /session/:id/abort`, `GET /session/:id/message`. No runtime → explicit `no-runtime` failure (never fake-success). |
| Chat pushed page chrome | `MobileChatScreen.tsx` | `LynxChatScreen` header + timeline + composer actions. Markdown cards / Files / Changes / MCP sheets / IME FLIP **not** in this slice. |
| Settings search + 22 slug rows | `MOBILE_SETTINGS_PAGE_SLUGS`, `SettingsView` | `src/settings/metadata.ts` + `SettingsTab`: editable `LynxInput` (`value` + `bindinput`, Next #139) + `filterLynxSettingsPages`, Cap main group order, all 22 rows (incl. `archived-sessions`), in-tab push. Bodies are **labeled stubs** in early slices; tip `voice` is **代码接上** status + model download/delete (no invented ASR); tip `archived-sessions` is **代码接上** list/unarchive page (landed Next #90). No `iosNativeUi` toggle. Not 三关 landed / not 真机过. |

**CI绿:** lynx-ci live on tip. Local Vitest also. Not 真机过.
**真机过:** not executed.

---


## 代码接上 (Projects / Assistant / Scheduled tabs — not landed under 三关)

Tab bodies on `cursor/lynx-tabs-home-local` (PR into `work/lynx-native`). Package Vitest + `tsc` gate the claim. No track CI / 真机过.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Projects home UI | `MobileProjectsHome.tsx`, `useMobileProjectsHomeModel.ts` | `ProjectsHome` wired to `createSessionIndexHomeBindings` / `projectSessionIndexHome`: project cards, worktree groups, session rows, search, pin/in-progress cues, `项目 · 分支` subtitle, collapsing-title header spirit, draft→push Chat. **failure ≠ empty**. |
| Assistant catalog + conversation chrome | `MobileAssistantTab.tsx`, assistants snapshot | `assistants/*` → `GET /api/openchamber/assistants/snapshot` (+ ensure-session hook). Catalog tab; open conversation reuses `LynxChatScreen`. Unread badge + mark-all + `POST …/contact/read` landed in Next #91. No invented ASR. Missing session → labeled stub (not fake chat id). |
| Scheduled list / history / editor hooks | `scheduledTasksApi.ts`, `MobileScheduledTab` | `scheduled/*` → `GET /api/openchamber/scheduled-tasks`, runs history, upsert PUT hook. Editor chrome is a **labeled stub**. Partial `failedProjectIds` preserved; no-runtime / HTTP failure ≠ empty success. |

**CI绿:** lynx-ci live on tip. Local Vitest also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (settings bodies + connect welcome + Projects header + CI skeleton — not landed under 三关)

Slice on `cursor/lynx-settings-ci-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy build via `.github/workflows/lynx-ci.yml` (**live** on tip; template also under `packages/lynx/ci/`) (Linux). APK/iOS sim jobs need Mac/Android runners — **not claimed**. 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Settings page bodies (22 slugs) | SettingsView + settings blob + catalogs | Wired GET/PUT: instances, appearance (flexoki ids), chat, notifications hooks, sessions, gitmoji, about (Lynx version ≠ instance). List endpoints: providers/agents/mcp/plugins/skills/commands/magic-prompts/snippets/usage/assistants/projects. `archived-sessions` page reuses `listLynxArchivedSessions` / `unarchiveLynxSession` (landed Next #90). Editors labeled stubs (no fake-success). Tip `voice` = status + model download/delete UI (no invented ASR). No iosNativeUi. |
| Connect welcome / instances UI | MobileApp welcome + MobileInstancesSurface | Splash while auto-connect; instance list add/delete/password unlock; paste pairing link. QR camera labeled stub (host-owned). No Bonjour. |
| Projects MobileTabPageHeader | `MobileTabPageHeader.tsx` | Sticky translucent collapsing title + trailing glass search chip + primary +. Collapse math in `tabPageHeader.ts`. iOS glass via GlassChrome; Android blur-radius only. |
| CI skeleton | — | `.github/workflows/lynx-ci.yml` (**live** on tip; template also under `packages/lynx/ci/`) on PR → `work/lynx-native`: type-check + vitest + rspeedy. Documented that APK/iOS sim need Mac/Android runners. |

**CI绿:** lynx-ci + Android sideload APK live on tip. iOS IPA still missing. Not 真机过.
**真机过:** not executed (environment: Linux cloud VM).

---
## 代码接上 (gap-close: summary-ai/behavior, scheduled editor, assistant ensure, chat sheets — not landed under 三关)

Slice on `cursor/lynx-gap-close-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Settings `summary-ai` body | `SummarySettings.tsx`, `/api/config/settings`, `/api/small-model` | Wired GET/PUT settings blob fields + callableModels. Failure / empty capabilities ≠ silent empty success. |
| Settings `behavior` body | `BehaviorPage.tsx`, `/api/behavior/agents-md`, response-style settings | Wired agents.md GET/PUT + responseStyleEnabled/Preset/Custom via settings blob. Failure ≠ empty. |
| Scheduled editor UI | `ScheduledTaskEditorDialog`, PUT upsert | Real editor chrome (name/enabled/schedule/prompt/provider/model) calling `upsertScheduledTask`. Create needs project id from settings projects. Never fake-success. |
| Assistant unbound ensure | `ensureAssistantSession`, `AssistantView` | Shell calls Cap `POST …/session/ensure` when runtime present; null sessionID stays unbound labeled (no invented chat id). |
| Chat overflow + Files/Changes stubs | MobileApp overflow, Files/Changes sheets | Overflow menu hooks; Files/Changes navigate to labeled stub sheets with correct back. Bodies not ported. |

**CI绿:** lynx-ci live on tip (Linux type-check + vitest + rspeedy). Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (editors + Files/Changes/MCP sheets + deep-link apply — not landed under 三关)

Slice on `cursor/lynx-editors-sheets-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Settings entity editors (list-backed) | SettingsView split → entity CRUD stores | Detail push + save/delete for providers (auth delete only; save unsupported), agents, assistants, mcp, plugins, commands, snippets, magic-prompts, skills.installed, projects. Real Cap routes; failure ≠ empty / fake-success. |
| Chat Files sheet | `MobileFilesSurface` → `/api/fs/list` | Real directory listing; no-directory / HTTP failure labeled. Text preview 代码接上; HTML Cap-like toggle + host stub in host-bridge deepen. |
| Chat Changes sheet | `MobileChangesSurface` → `/api/git/status` | Real status list (branch + staged/unstaged/untracked). Diff viewer / commit / sync not ported. |
| Chat MCP sheet | mobile MCP surface / `/api/config/mcp` | Overflow entry + catalog list. |
| `openchamber://` apply | `deepLinkNavigation.ts` | Parse already existed; apply maps intents → shell navigation (session/draft/tab/settings/sheets/instances). Stash until connect ready + handlers. |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (diff/preview + commit/sync + provider auth + push/share hooks — not landed under 三关)

Slice on `cursor/lynx-diff-push-share-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Changes turn/file diff | `MobileChangesSurface` → `/api/git/file-diff`, `/api/git/diff` | Tap file → preview unified/original+modified. Binary labeled. |
| Changes commit / sync | `CommitSection`, `SyncActions` → `POST /api/git/commit\|fetch\|pull\|push` | **Real** Cap endpoints (not stubs). Failure ≠ fake-success. |
| Files text preview | `MobileFilesSurface` / FilesView → `/api/fs/read` | List stays real; tap file → text preview (truncated). HTML Cap-like source/preview stub in host-bridge deepen (no invent iframe). |
| Provider auth UI | `ProvidersPage` auth | API key `PUT /api/auth/:id`; OAuth authorize/callback Cap routes; **host-only** browser open documented (no invented OAuth/Capgo). Clear-auth delete unchanged. |
| Push registration hooks | `useNativePushRegistration` → `/api/push/apns-token` | Host injects APNs/FCM tokens; Lynx stores + registers/unregisters. FCM `applicationId` must be `com.yee94.openchamber[.debug]` (pitfalls §6). |
| Share inbox intake | `MobileShareBridge` → assistants `/share` | Accept host share envelope / openchamber share intents; dispatch to Assistant session. No Capgo. |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (rich turn cards + swipe menu + share welcome + draft + list harness — not landed under 三关)

Slice on `cursor/lynx-cards-swipe-harness-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Chat rich turn cards | message parts, ProgressiveGroup, QuestionCard, PermissionCard | Wire Cap part types (`text`/`reasoning`/`tool`/`file`/`agent`); Activity collapsed/expanded; pending `/question`+`/permission` reply. No invented types. |
| Projects swipe / long-press | `sessionMenuModel.ts` | Long-press sheet: pin (session-index), archive/delete (`PATCH`/`DELETE /session/:id`). Share/rename gated when callbacks exist. |
| Share welcome chrome | `AssistantShareWelcome` | Education cards + Cap storage key on Assistant tab above share inbox. |
| Draft composer body | mobile `kind: 'draft'` | `LynxDraftComposer` materializes `POST /session` → `prompt_async` then opens chat. |
| List perf harness | Cap `streamingRenderCadence.ts` + acceptance harness | Unit harness measures synthetic scroll/update cadence + prepend anchor; documents Cap 20/64 & Android 100/128 — **no fake device numbers**. |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (SSE live tail + IME contract + nested chat stack — not landed under 三关)

Slice on `cursor/lynx-sse-live-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Chat SSE / event live tail | `event-pipeline.ts` `/api/global/event` (+ WS) | Parse Cap/OpenCode envelopes; fold `message.*` / `session.status` into the **same** LegendList (`liveEvents.ts` / `liveTail.ts`). No TanStack / no live overlay. Abort/working + queue flush on idle. Direct fetch exposes `body` stream; relay without stream fails honestly. |
| IME / composer occupancy contract | pitfalls §3, native composer README | Documented in `imeOccupancy.ts` + acceptance: host binds IME; occupancy = collapsed height only; no WebView FLIP. |
| Nested child session stack / predecessor | `mobileNavigation.ts` | `reconcileLynxChatPredecessor` + stack window chrome; ShellApp back pops predecessor (Cap decision). |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (context usage + edge swipe + host media/haptics + pin reveal — not landed under 三关)

Slice on `cursor/lynx-context-edge-media-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Context usage chrome | `mobileContextUsage.ts`, `/api/config/providers` | Port baseline scan + display; resolve context limit from real Cap providers catalog; chip in chat header. Failure / missing limit → hide (never invent). |
| Edge swipe session switch | `useEdgeSwipeSessionSwitch.ts` | Composer-only ownership + geometry + state machine (`edgeSwipeSessionSwitch.ts`). Host binds pan via `edgeSwipeDispatchRef`. Docs: `LYNX_EDGE_SWIPE_HOST_CONTRACT`. |
| Haptics adapter | `OpenChamberHaptics`, `streamingHaptics.ts` | Host-inject `createLynxHapticsAdapter`; no host → `unavailable` (not fake-success). Wired from edge-swipe effects. |
| HEIC / media pick | `OpenChamberMedia`, `native-media-pick.ts`, `native-image-transcode.ts` | Host-inject pick + transcode; composer Attach call site; no host → unavailable / not-heic skipped honestly. |
| Load-older / markdown pin reveal polish | Cap load-older + `markdownPinReveal.ts` | `canAcceptLynxLoadOlderTap` rejects settle/busy; pin-reveal arm/ready/timeout state + TimelineList visibility attr. |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---
## Missing

Seeded from the inventory. Grouped so a slice can pick a coherent vertical.

### Host / skeleton

| Item | Cap/web source | Lynx note |
|---|---|---|
| Lynx rspeedy bundle + signed host apps | `packages/lynx` scaffold | **Live** `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` on tip (lynx-ci green; Android sideload APK green; prerelease `lynx-v2-debug-b170e47`). iOS IPA / CocoaPods resolve still missing. Sideload `applicationId` = `com.yee94.openchamber.lynx.debug`. First-paint cream + globalProps 代码接上 — **not** 真机过. |
| ~~Connect / splash while auto-connect resolves~~ | `MobileApp.tsx` welcome | **代码接上** ConnectWelcome splash + welcome; host LynxView chrome still thin |
| ~~Instance list, add, delete, password unlock~~ | `mobileConnections.ts` | **代码接上** instances UI on welcome + settings/instances |
| QR + pairing-link redeem v2 | `mobileQrScan.ts` | Link parse + redeem exist; **camera plugin** is host-owned |
| ~~`openchamber://` parse + apply~~ | `deepLinks.ts` | Parse/build + **apply → navigation** 代码接上 (stash until connect ready) |
| Secure store (Keychain / Keystore) | Capacitor secure storage | JS adapter + host Keychain/EncryptedSharedPreferences **stubs** 代码接上; live OS wiring still 真机 |
| Four-tab **product** content (session-index, catalogs) | `mobileTabs.ts` | Shell IA + tab bodies + **swipe/long-press menus** **代码接上**; rich pixel polish still missing |
| Host Tab/Nav **binary** (linked Lynx SDK) | `packages/lynx/host/*` | Strategy + Xcode/Gradle **scaffold** 代码接上 (PR#48); CocoaPods/AAR resolve still missing on Linux |

### Projects (chat list)

| Item | Cap/web source |
|---|---|
| ~~Project cards + worktree groups~~ | `MobileProjectsHome.tsx` | **代码接上** in `ProjectsHome` (worktree groups from session-index + optional parent map) |
| ~~Session rows, search, pin / in-progress~~ | `useMobileProjectsHomeModel.ts` | **代码接上** search + pin/busy cues |
| ~~`项目 · 分支` subtitle~~ | `formatHomeSessionSubtitle` | **代码接上** |
| ~~Collapsing MobileTabPageHeader~~ | `MobileTabPageHeader.tsx` | **代码接上** glass search + primary + |
| ~~Swipe / long-press actions~~ | `sessionMenuModel.ts` | **代码接上** long-press sheet → pin/archive/delete/**rename** real APIs |
| ~~New-session draft page~~ | `kind: 'draft'` | **代码接上** draft composer body materializes POST /session |
| ~~Add project directory explorer~~ | `DirectoryExplorerDialog` | **代码接上** in PR#48 (`DirectoryExplorer` + settings projects add) |
| ~~Header 扫一扫 / 切换实例~~ | `MobileProjectsHome` | **代码接上** chrome; camera host still unavailable until binder |
| ~~Session index as data source~~ | `GET /api/openchamber/session-index` (server) | Client + home projection + **Projects UI** 代码接上 |

### Chat

| Item | Cap/web source | Lynx note |
|---|---|---|
| ~~**LegendList-semantics timeline**~~ | `TimelineList.tsx` | **代码接上** list semantics + LynxTimelineList + **rich turn cards** (Activity / Q&P) |
| ~~SSE / event live tail~~ | `event-pipeline.ts` | **代码接上** Cap `/api/global/event` SSE fold into same list; WS host inject still thin |
| Chat header / overflow | `MobileChatScreen.tsx` | Header + Cap phone overflow **new-session** + Files/Changes/MCP/refresh **代码接上**; Capgo update 故意不移植; iPad Settings omitted (Lynx has Settings tab) |
| ~~Send / Stop / queue / abort~~ | ChatInput + queue | **代码接上** hooks + official routes; composer text input host binding still thin |
| ~~Questions / permissions~~ | chat cards | **代码接上** pending `/question`+`/permission` cards + reply |
| ~~Activity / sorted / collapsed~~ | chat DOCUMENTATION | **代码接上** collapsed Activity disclosure (detail rows hidden until expand) |
| ~~Load-older button (no scroll auto-load)~~ | timeline controller | **代码接上** button + bounce forbid + prepend-settle gate + pin-reveal polish |
| ~~Nested child session stack + predecessor~~ | `mobileNavigation.ts` | **代码接上** reconcile + predecessor chrome; host underlay pixel polish still thin |
| ~~Files / Changes / turn-diff sheets~~ | `MobileFilesSurface`, `MobileChangesSurface` | **代码接上** list + text preview + file/turn diff + commit/fetch/pull/push + **stage/unstage** (GlassChrome searchChip +/−) + Cap **stageAll/unstageAll** group headers (Next #61) + **revert** Cap `arrow-go-back` ↩ + Cap **revertAll** toolbar + Cap **centered Dialog** at **shell-root portal** (full-screen; status.error / onError tokens) + Cap commit→push **pull-if-behind**. HTML preview = labeled text stub until host WKWebView; PierreDiff = portable text only — Cap `@pierre/diffs` **unavailable** (Shadow DOM / react-dom blocker). |
| ~~MCP sheet~~ | `mobile-mcp` | **代码接上** overflow + `/api/config/mcp` list |
| ~~Context usage~~ | `mobileContextUsage.ts` | **代码接上** header chip + Cap `/api/config/providers` limit |
| | ~~Composer attachments, `/` `@`, agent/model~~ | composer DOCUMENTATION | Attach + `/` `@` catalogs **代码接上** (Chat + Draft); autocomplete **above glass**; GlassChrome + in-glass Attach/Send/Stop/Queue **代码接上**; Cap Agent·model **picker sheets** (`/api/agent` + `/api/config/providers` → prompt_async selection) **代码接上**; host Mode B overlay / 真机 still thin |
| Native-quality IME (not WebView FLIP) | pitfalls §3 | **Contract 代码接上** (`imeOccupancy.ts`); host keyboard binding / 真机 still missing |
| ~~Session swipe (composer only)~~ | `useEdgeSwipeSessionSwitch.ts` | **代码接上** state machine + composer surface; **host pan bind** still required |

### Assistant / Scheduled / Settings

| Item | Cap/web source | Lynx note |
|---|---|---|
| ~~Assistant catalog + conversation page~~ | `MobileAssistantTab.tsx`, `AssistantView.tsx` | **代码接上** catalog + LynxChatScreen; ensure path calls Cap `session/ensure` (no invented ids) |
| ~~Continuous / stateless admission~~ | assistants DOCUMENTATION | **代码接上** `POST …/assistants/:id/messages` (PR#48); mode polish still thin |
| ~~Share welcome + inbox~~ | `AssistantShareWelcome`, `MobileShareBridge` | **代码接上** share inbox + **welcome chrome** (Cap storage key + examples) |
| ~~Scheduled list / history / editor~~ | `MobileScheduledTab.tsx` | **代码接上** list + history + editor upsert UI |
| ~~Settings search + **all 22 mobile slugs**~~ | `MOBILE_SETTINGS_PAGE_SLUGS` (Cap main) | **代码接上** home search (`LynxInput` + `bindinput`, Next #139) + grouped rows + push pages (`archived-sessions` page landed in Next #90). Not 三关 landed / not 真机过. |
| ~~Settings split collection → entity editor~~ | `SettingsView.tsx` | **代码接上** detail push + Cap save/delete for list-backed slugs (providers save = auth-unsupported) |

### Native platform

| Item | Cap/web source |
|---|---|
| ~~APNs + FCM registration~~ | `useNativePushRegistration.ts` | **代码接上** host-inject + `/api/push/apns-token` register/unregister + FCM package-id guard. Native token mint still host-owned. |
| Share extension / Android share receiver | `packages/mobile` share | Host/native still owns extensions; Lynx inbox accepts payloads |
| Live Activity | iOS 17+ plugin |
| Widgets / Control Center | `OpenChamberWidget` |
| ~~Haptics~~ | `OpenChamberHaptics` | **代码接上** host-inject adapter; native impact still host-owned |
| ~~Virtual image assets~~ | `openchamber-asset://` | **代码接上** TS + host scheme stubs (PR#48 / host-bridge deepen) |
| ~~HEIC / media pick~~ | `OpenChamberMedia.transcode` / `pickMedia` | **代码接上** host-inject + Attach call site; no fake success without host |
| ~~About + diagnostics export~~ | `AboutSettings` | **代码接上** `openchamber.client-diagnostics.v1` export (PR#48) |
| ~~Predictive / edge back~~ | `OpenChamberNavigation` | **代码接上** JS policy + host stubs (PR#48); 真机 arena still host |

### Voice (existing path only)

| Item | Cap/web source | Caution |
|---|---|---|
| ~~Settings `voice` + dictation + TTS~~ | `VoiceSettings.tsx`, `/api/dictation/*` | **代码接上** status + STT/TTS model download/delete UI (`VoiceBody` + `/api/dictation/*`). Mic/WS ASR stays host-bound — do not invent ASR. |

---

## Android降级

These are **intentional** platform differences already true in Cap, plus Lynx 3.8 glass limits. Android must ship a lesser, documented analogue — not a fake `UIGlassEffect`.

| Surface | iOS | Android analogue | Source / reason |
|---|---|---|---|
| Liquid glass dock | iOS 26 `UITabBarController` / Lynx `blur-effect="glass"` | Lynx-drawn capsule or Material nav; `blur-radius` only | Cap: `OpenChamberTabBar` is iOS-only. Lynx 3.8 glass attrs are **iOS** |
| Glass composer | `UIGlassEffect` / `<blur-view blur-effect="glass">` via `LynxComposerGlassCard` (**代码接上**, not 真机过) | Material / Lynx blur-radius composer | Cap: `OpenChamberComposer` iOS-only; Lynx Android blur-radius only |
| Live Activity / Dynamic Island | iOS 17+ ActivityKit | No-op or a notification | Cap plugin iOS-only |
| Widgets / Control Center | WidgetKit | Optional; not required for first slice | Cap iOS extension |
| Share UX | Share Extension + suggestions | `ShareReceiverActivity` + full-page picker (never a sheet) | README share section |
| Photo picker | `PHPicker` / WK file input | `ACTION_PICK_IMAGES` / Lynx media | `OpenChamberMedia.pickMedia` is Android-only today |
| Back gesture | Screen-edge pan | Predictive Back (14+) / commit-only older | `OpenChamberNavigation` |
| Push | APNs | FCM + matching `applicationId` | pitfalls §6 |
| OTA | Capgo web-bundle (Cap only) | Same limitation — Lynx binary ≠ web bundle | 故意不移植 Capgo into Lynx unless a real Lynx OTA exists |
| Streaming cadence | 20ms / 64ms markdown pace | Cap already throttles Android 100ms / 128ms | chat DOCUMENTATION |

Glass-container fusion (`spacing`, `glass-interactive`, `glass-tint-color`) is **iOS 3.8**. Android `android-capture-target` / `blur-sampling` is a different effect. Do not claim “liquid glass on Android”.

---

## Next

First implementation slice after this doc gate (order is deliberate: connect → shell → list engine → one real transcript).

1. ~~Host app skeleton + embedding decision~~ — scaffold in `packages/lynx`. Remaining: link Lynx SDK, rspeedy bundle, device host; wire HTTP/Keychain/relay adapters.
2. ~~Connect + instance persistence client~~ — 代码接上 in `packages/lynx`. Remaining: welcome/instances UI, native secure store, and a real server 真机 pass (LAN then relay). No demo hosts. No Bonjour.
3. ~~Four-tab shell IA~~ — navigation + Projects/Assistant/Scheduled tab bodies 代码接上. Remaining: pixel polish / host IME.
4. ~~Projects home data path + UI~~ — session-index bindings + ProjectsHome UI + DirectoryExplorer + 扫一扫/切换实例 chrome 代码接上. Remaining: pixel polish / host camera binder 真机.
5. ~~**LegendList-semantics chat list** + send/stop on official APIs~~ — 代码接上 in `packages/lynx/src/chat`. SSE live tail 代码接上; native IME host binding still missing.
6. ~~**Settings home + slug map**~~ — 代码接上 search + 22 Cap main rows (`archived-sessions` page body landed in Next #90). Search field is editable `LynxInput` (`bindinput`, Next #139). Bodies: wired/list/stub in settings-ci slice. Remaining: rich entity editors. Not 三关 landed / not 真机过.
7. ~~**Assistant catalog + Scheduled list/history**~~ — 代码接上 snapshot/list/history hooks. Remaining: admission flows.
8. ~~**Connect welcome + instances UI**~~ — 代码接上 splash/list/paste. Remaining: host QR camera + Keychain wiring + 真机.
9. ~~**Projects MobileTabPageHeader**~~ — 代码接上 collapsing header + glass search + primary +. Remaining: pixel polish / menu.
10. ~~**CI skeleton (Linux)**~~ — **live** on tip: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` (lynx-ci green; Android sideload APK green; prerelease `lynx-v2-debug-*`). Remaining: iOS IPA / sim runners + 真机过 (do not claim from CI alone).
11. ~~**gap-close: summary-ai / behavior / scheduled editor / assistant ensure / chat Files·Changes stubs**~~ — 代码接上 in `cursor/lynx-gap-close-local`.
12. ~~**editors + Files/Changes/MCP lists + deep-link apply**~~ — 代码接上 in `cursor/lynx-editors-sheets-local`.
13. ~~**diff/preview + commit/sync + provider auth + push/share hooks**~~ — 代码接上 in `cursor/lynx-diff-push-share-local`. Remaining: host Keychain/QR/browser OAuth open, 真机.
14. ~~**rich turn cards + swipe menu + share welcome + draft + list harness**~~ — 代码接上 in `cursor/lynx-cards-swipe-harness-local`.
15. ~~**SSE live tail + IME occupancy contract + nested chat predecessor**~~ — 代码接上 in `cursor/lynx-sse-live-local`. Remaining: host IME binding / Keychain/QR, relay streaming body, 真机.
16. ~~**context usage + edge swipe + haptics/HEIC/media + pin reveal**~~ — 代码接上 in `cursor/lynx-context-edge-media-local`. Remaining: host pan/haptics/media binders, Keychain/OAuth browser, Live Activity/Widgets, 真机.
17. ~~**closable missing: host scaffold + lynx-ci workflow + DirectoryExplorer + 扫一扫/切换实例 + composer `/` `@` + admission + voice dictation status + About diagnostics + openchamber-asset hooks + predictive-back contract**~~ — 代码接上 in `cursor/lynx-closable-missing-local`. Remaining: real camera/SDK link / Keychain / IME 真机; not product-EXHAUSTED.
18. ~~**linux gaps: GitIdentity* editor + Files HTML preview stub + DraftComposer `/` `@` + autocomplete above glass + host-bridge deepen + Missing strikethrough hygiene**~~ — 代码接上 in `cursor/lynx-linux-gaps-local`. Remaining: host WKWebView HTML sheet / PierreDiff / live Keychain·camera·IME 真机; product NOT DONE.
19. ~~**composer glass: LynxComposerGlassCard GlassChrome on Chat+Draft + autocomplete sibling ABOVE glass + optional row searchChip**~~ — 代码接上 in `cursor/lynx-composer-glass-local`. Remaining: Mode B host composer overlay / live UIGlassEffect 真机; product NOT DONE.
20. ~~**composer actions in glass: Attach/Send/Stop/Queue inside LynxComposerGlassCard (Cap pill/card order); autocomplete stays ABOVE**~~ — 代码接上 in `cursor/lynx-composer-actions-in-glass-local`. Remaining: Mode B host overlay / live UIGlassEffect / 真机; product NOT DONE.
21. ~~**composer Agent·model picker sheets: Cap MobileResizableSheet spirit; `/api/agent` + providers catalog; selection → prompt_async**~~ — 代码接上 in `cursor/lynx-agent-model-picker-local`. Remaining: Mode B host overlay / live UIGlassEffect / 真机; product NOT DONE.
22. ~~**resizable picker sheets + Draft Stop abort: half-height grabber sheet (not full-screen surface.background); Draft busy Stop aborts**~~ — 代码接上 in `cursor/lynx-resizable-picker-sheets-local`. Remaining: Mode B host overlay / live UIGlassEffect / 真机 drag feel; product NOT DONE.
23. ~~**Cap sheet height snaps 72/98dvh: LynxMobileResizableSheet half 0.72 / expanded 0.98 (was 50%/92%)**~~ — 代码接上 in `cursor/lynx-sheet-height-cap-local` (PR #55). Remaining: Mode B host overlay / live UIGlassEffect / 真机 drag feel; product NOT DONE.
24. ~~**linux closable after #55: portable text diff + Cap stage/unstage + Projects rename wiring + docs hygiene**~~ — 代码接上 in `cursor/lynx-linux-closable-after-55-local`. Remaining: host-only / 真机 (Pierre runtime, WKWebView, Keychain, camera, IME, Mode B); product NOT DONE.
25. ~~**diff/chip polish on #56: Cap status add/del tokens + GlassChrome searchChip stage/unstage**~~ — 代码接上 in `cursor/lynx-diff-chip-polish-local`. Remaining: Pierre `@pierre/diffs` runtime / Mode B / 真机; product NOT DONE.
26. ~~**Pierre investigation + ChangeRow spacing on #57 tip: honest Shadow DOM blocker + Cap size-6 chip / +n/-m slash / diffStats**~~ — 代码接上 in `cursor/lynx-pierre-diffs-local`. Cap `@pierre/diffs` **cannot** run in Lynx (diffs-container Shadow DOM + react-dom). Portable path only; product NOT DONE / no 真机过.
27. ~~**Changes revert + generateCommitMessage + commitAndPush on #58 tip**~~ — 代码接上 in `cursor/lynx-changes-revert-commitmsg-local`. Cap `POST /api/git/revert`, Cap mobile `POST /api/small-model/generate` purpose commit (not dead `/api/git/commit-message`), combined commit→push. Remaining: host-only / 真机 / Pierre / WKWebView; product NOT DONE.
28. ~~**Changes revert glass + Cap confirm + pull-if-behind on #59 tip**~~ — 代码接上 in `cursor/lynx-revert-confirm-pull-local`. GlassChrome searchChip revert (↩) like stage +/−; Cap Dialog confirm before revert (Cap does **not** confirm commit&push); Cap commit→fetch→pull-if-behind(rebase)→push-if-ahead. Remaining: host-only / 真机 / Pierre / WKWebView; product NOT DONE.
29. ~~**Centered Cap Dialog revert confirm on #60 tip**~~ — 代码接上 in `cursor/lynx-centered-confirm-dialog-local`. Replace elevated-in-sheet confirm card with Cap Dialog spirit (**scrim + centered max-w-md panel**); keep RevertGlassChip + pull-if-behind. Remaining: host-only / 真机 / Pierre / WKWebView; product NOT DONE.
30. ~~**Dialog shell portal + destructive tokens + Cap arrow-go-back glyph on #61 tip**~~ — 代码接上 in `cursor/lynx-dialog-portal-theme-local`. Mount `LynxCenteredDialog` at **shell-root portal** (full-screen overlay, Cap DialogPortal spirit) — not nested absolute inside Changes relative; destructive uses `status.error` / `status.onError` (Cap `--destructive-foreground`, never `#fff`); RevertGlassChip glyph = Cap Icon `arrow-go-back` unicode ↩. Remaining: host-only / 真机 / Pierre / WKWebView; product NOT DONE.
31. ~~**Android first-paint / black-screen mitigation + live CI honesty**~~ — 代码接上 (PR #65 + docs follow-up) + follow-up `cursor/lynx-first-paint-visible-smoke`: Yee 真机 showed 有字无样式 (done-phase welcome) — Android Lynx drops inline style values containing `var(...)`; `cssVar()` / `tokenColor()` now return resolved Flexoki-light **hex** (never `var(--name, #hex)`). Strict smoke requires literal `text="Connecting` / `text="OpenChamber Lynx` (NOT content-desc alone, NOT splash console.log — false-green on `lynx-v2-debug-cfed156` / job 102340406447 fixed). Host TextView splash overlay until first_screen+20s. **三关: 真机过 still open until Yee confirms styled cream/elevated/primary.** Product NOT DONE / 三关未齐 / not EXHAUSTED.
32. ~~**Cap phone overflow new-session → draft**~~ — 代码接上 in `cursor/lynx-overflow-new-session-local`. Cap `MobileApp` overflow first item `new-session` opens draft; Lynx overflow previously started at Files. Wire `onOpenDraft` → shell `openDraft` / `LynxDraftComposer`. Docs honesty: tip APK `lynx-v2-debug-b170e47` (was stale `09b3853`); note #67–#69. Remaining: host-only / 真机 / Pierre / WKWebView / Mode B; product NOT DONE.
33. ~~**Settings Voice model download/delete UI**~~ — 代码接上 in `cursor/lynx-voice-models-download-local`. Cap `VoiceSettings` STT model rows + optional Kokoro `ttsModels` via existing `/api/dictation/status` + `POST/DELETE …/models/:id`; `VoiceBody` download/progress/delete; no invented ASR/mic/WS. Docs honesty: tip APK was `lynx-v2-debug-1a5c899` at merge; current tip `lynx-v2-debug-b170e47`. Remaining: host-only / 真机 / Pierre / WKWebView / Mode B / iOS IPA; product NOT DONE.
34. ~~**Voice STT model select + Overflow Changes dirty badge**~~ — 代码接上 in `cursor/lynx-voice-select-dirty-badge-local`. Cap `LocalModelPicker` / `setSttLocalModel` → PUT `sttLocalModel` (+ optional `dictationEnabled`); pass `localModel` into status; preview/browser TTS/say labeled unavailable; Cap `dirtyChangeCount` badge on Changes overflow from git status entry count (no fake on failure). Docs honesty: tip APK `lynx-v2-debug-b170e47`. Remaining: host-only / 真机 / Pierre / WKWebView / Mode B / iOS IPA; product NOT DONE.

35. ~~**PermissionCard metadata + permission auto-accept**~~ — 代码接上 in `cursor/lynx-permission-metadata-auto-accept-local` (PR #73). Cap PermissionCard metadata plain text + GET/PUT permission-auto-accept. Remaining: host-only / 真机 / Pierre / WKWebView / Mode B / iOS IPA; product NOT DONE.
36. ~~**Cap share-recipient picker**~~ — 代码接上 in `cursor/lynx-share-recipient-picker-local`. Cap `MobileShareRecipientPicker` full-page overlay (never a sheet) + `NativeShareDraft` Partial target + `MobileShareBridge` unassigned→picker / assigned→dispatch; never silent-default Assistant. Host Share extension / `ShareReceiverActivity` stay host-only. Docs honesty: tip APK `lynx-v2-debug-93b4355`; product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty.
37. ~~**Projects home project/worktree menus + session share**~~ — 代码接上 in `cursor/lynx-projects-menus-share-local`. Wire `buildLynxProjectMenuItems` / `buildLynxWorktreeMenuItems` + session share/copyLink/unshare against Cap/OpenCode `POST|DELETE /session/:id/share`; Linux-closable newSession/syncSessions/edit/closeProject/newWorktree/deleteWorktree (honest unavailable when HTTP missing). Docs honesty: tip APK `lynx-v2-debug-0b470a2`; product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty.
38. ~~**QueuedMessageChips + SessionGoalRow**~~ — 代码接上 in `cursor/lynx-queue-chips-goal-local`. Cap queue chips + SessionGoal strip above composer glass; local `composerActions` queue with remove/send-now + portable ↑/↓ reorder (Cap `@dnd-kit` / server `/api/openchamber/message-queue` deferred). SessionGoal GET+PATCH metadata + optional `/api/goals/objective`; never fake-success. **MobileSessionStatusBar deferred** (Cap file large — follow-up). Docs honesty: tip APK `lynx-v2-debug-905c42e`; product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty.
39. ~~**MobileSessionStatusBar slim strip**~~ — 代码接上 in `cursor/lynx-session-status-bar-local`. Cap multi-session status bar **thin** parity (not full ~1900-line sheet): related session chips + busy/working indicator above composer with SessionGoal + queue chips; Cap filter/preserve-active-project resolvers; session-index related list from shell; full Cap sessions sheet / worktree menus / server message-queue deferred. Docs honesty: tip APK `lynx-v2-debug-ca32a72` (release exists); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty.
40. ~~**SessionGoal create/manage dialog**~~ — 代码接上 in `cursor/lynx-session-goal-dialog-local`. Cap `SessionGoalDialog` + `setSessionGoal`/`clearSessionGoal` parity: Lynx `setLynxSessionGoal`/`clearLynxSessionGoal` (GET `/session/:id` + PATCH `metadata.openchamber.goal` + Cap `/api/goals/objective/:id`); create/manage overlay via shell `LynxCenteredDialog`/`LynxDialogPortal`; row tap → manage; create entry when no goal + Cap `/goal` arm (never auto-send). Complete via existing `setLynxSessionGoalStatus('complete')`. Docs honesty: tip `8def01d0` / tip APK `lynx-v2-debug-8def01d` (release exists); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty.
41. ~~**Cap server message-queue client + QueuedMessageChips wire**~~ — 代码接上 in `cursor/lynx-message-queue-server-local`. Thin Cap-compatible `messageQueueServer.ts` over `LynxRuntimeFetch` (list/admit/reorder/remove/send-now; flush=send-now first); composerActions prefers server when reachable, falls back to local on unavailable; portable ↑/↓ (no `@dnd-kit` / no TanStack). Docs honesty: tip `c02f917f` / tip APK `lynx-v2-debug-c02f917` (release exists); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; full Cap sessions sheet still deferred; host-only unchanged.
42. ~~**Cap full sessions sheet from slim SessionStatusBar**~~ — 代码接上 in `cursor/lynx-sessions-sheet-local`. Cap MobileSessionsSheet / MobileSessionStatusBar sheet spirit via `LynxMobileResizableSheet` (0.72/0.98): session-index project+worktree grouped list (portable TS, not Cap Zustand), search, All/pinned/project filter chips (`LYNX_PINNED_SESSION_FILTER_ID` + `resolveMobileSessionSheetDefaultFilter` + `shouldPreserveActiveProjectOnSessionOpen`), tap → switch+close, long-press reuses `buildLynx*MenuItems` (honest unavailable when HTTP missing). Deferred: Cap `@dnd-kit` / MobileWindowMotion polish / iPad sidebar. Docs honesty: tip `f72fbe0c` / tip APK `lynx-v2-debug-f72fbe0` (release exists — do not invent newer); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
43. ~~**Cap worktree create/delete dialogs on SessionsSheet**~~ — 代码接上 in `cursor/lynx-worktree-dialogs-local`. Cap name prompt (not auto `wt-*`) + confirm delete via shared `LynxCreateWorktreeDialog` / `LynxDeleteWorktreeDialog` (ProjectsHome + SessionsSheet). Deepen Cap MobileDeleteWorktreeDialog spirit: `deleteLocalBranch` toggle (API on `deleteLynxWorktree`), archive linked sessions via `archiveLynxSession`, dirty warning via Cap `GET /api/git/status` (`probeLynxWorktreeDirty`). Deferred at merge: remote-branch delete (closed in Next #44), Cap `@dnd-kit` / MobileWindowMotion / iPad sidebar. Docs honesty: base tip `ebe3d408` / tip APK `lynx-v2-debug-ebe3d40` (release exists); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
44. ~~**Cap deleteRemoteBranch on Lynx worktree delete**~~ — 代码接上 in `cursor/lynx-delete-remote-branch-local`. Cap `MobileDeleteWorktreeDialog` / `removeProjectWorktree` remote toggle: Lynx `deleteLynxRemoteBranch` → Cap `DELETE /api/git/remote-branches?directory=` `{ branch, remote? }`; wired into `deleteLynxWorktree` (after worktree remove, Cap order) + `LynxDeleteWorktreeDialog` toggle. Real API only — remote failure after worktree remove reported honestly (`worktreeRemoved`, never fake-success). Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad sidebar / host binders. Docs honesty: tip `fd333a09` / tip APK `lynx-v2-debug-fd333a0` (release exists; was stale `b3d340b`); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
45. ~~**Cap SessionsSheet two-step archive + unarchive undo**~~ — 代码接上 in `cursor/lynx-sessions-archive-undo-local`. Cap `MobileSessionsSheet` `confirmingArchive` / `onRequestArchive` / `onConfirmArchive` spirit + `sessionMutationUndo` ~10s window: Lynx `unarchiveLynxSession` → PATCH `/session/:id` `{ time: { archived: 0 } }`; SessionsSheet row two-step archive control + portable undo banner/chip (also from long-press archive). Real API only — never fake-success. Deferred at merge: Cap toast lib / bulk multi-select / ArchivedSessionsDialog (closed in Next #46) / smart-title / `@dnd-kit` / MobileWindowMotion / iPad sidebar / host binders. Docs honesty: tip `a1ea85e0` / tip APK `lynx-v2-debug-a1ea85e` (release exists; was stale `fd333a0`); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
46. ~~**Cap ArchivedSessionsDialog on SessionsSheet**~~ — 代码接上 in `cursor/lynx-archived-sessions-dialog-local`. Cap `ArchivedSessionsDialog` spirit: list archived via OpenChamber `GET /api/experimental/session?archived=true&roots=true` (Cap `experimental.session.list`), project buckets + restore via `unarchiveLynxSession` + preview; honest empty/load/no-runtime. Entry from SessionsSheet (footer + undo-banner View archived). Session-index labels only (index drops archived roots). Deferred unchanged: bulk multi-select / smart-title / Cap toast lib / `@dnd-kit` / MobileWindowMotion / iPad sidebar / host binders. Docs honesty: tip APK `lynx-v2-debug-28bdee8` (release exists for base tip `28bdee8c`); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
47. ~~**Cap session rename smart-title**~~ — 代码接上 in `cursor/lynx-smart-title-local`. Cap `requestSessionSmartTitle` spirit: `requestLynxSessionSmartTitle` GET `/session/:id` then PATCH merged `metadata.openchamber.titleRefresh.requestedAt` (preserve titleRefresh fields + `lastAutoTitle`); wired into SessionsSheet + ProjectsHome rename overlays (Save + smart-title). Close after submit — do not wait for generation; Lynx honest inline/error label (no Cap toast lib). Real routes only — never fake-success. Deferred unchanged: Cap toast lib / bulk multi-select / `@dnd-kit` / MobileWindowMotion / iPad sidebar / host binders. Docs honesty: tip APK `lynx-v2-debug-7b2b88f` (release exists for tip `7b2b88f3`); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
48. ~~**Cap session tree archive/delete + hard-delete undo**~~ — 代码接上 in `cursor/lynx-tree-delete-undo-local`. Cap `MobileSessionsSheet` `collectSessionTreeIds` / `getParentId` + `deleteSessionsWithUndo` / `scheduleSessionDeletes` (~SESSION_DELETE_UNDO_MS 10s): Lynx tree archive/delete via `parentID` (root + descendants) with honest `archivedIds`/`failedIds`; portable delete schedule + undo banner (not Cap sonner/toast). Wired SessionsSheet + ProjectsHome menus. Real APIs only — never fake-success. Deferred leftovers: Cap toast lib (sonner) / bulk multi-select (desktop sidebar folders) / `@dnd-kit` / MobileWindowMotion / iPad. Docs honesty: tip `78a6ac535` / tip APK `lynx-v2-debug-78a6ac5` (release exists); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
49. ~~**Cap MobileProjectEditSurface parity**~~ — 代码接上 in `cursor/lynx-project-edit-surface-local`. Cap label+icon+color+iconImage discover/remove + worktree list delete + portable ↑/↓ reorder (no `@dnd-kit` / Cap toast): Lynx `updateLynxProjectMeta` / `loadLynxProjectMeta` / icon routes / Cap MQ `worktrees/order` (local-only honest when unavailable) + `LynxProjectEditSurface` via `LynxMobileResizableSheet`. Wired ProjectsHome + SessionsSheet `edit`. Real Cap/settings APIs only — unavailable when project not in settings; never fake-success. Deferred: Cap toast lib / `@dnd-kit` / MobileWindowMotion / iPad + host-only. Docs honesty: tip APK `lynx-v2-debug-ba47923` (release exists for tip `ba47923f`; was stale `78a6ac5`); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.
50. ~~**Cap SessionsSheet header trailing newChat / newWorktree / addProject**~~ — 代码接上 in `cursor/lynx-sessions-trailing-local`. Cap `MobileSessionsSheet` `trailingActions`: `newChat` when projects exist → `onOpenDraft?.(directory)`; `newWorktree` when active project is git → `LynxCreateWorktreeDialog` / `setNewWorktreeProject`; `addProject` → `DirectoryExplorerSheet` (same as ProjectsHome). Pure helpers + Vitest in `sessionsSheet.ts`. Reuses `lynx.projects.chrome.addProject` / `menu.newWorktree`; adds `lynx.chat.sessionsSheet.newChat`. Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host-only. Docs honesty: tip `8f2df6d8` / published APK `lynx-v2-debug-8f2df6d` (do NOT invent newer APK for PR head); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA/@dnd-kit/MobileWindowMotion/iPad/Cap toast).
51. ~~**Cap Assistant catalog menus + create/enable**~~ — 代码接上 in `cursor/lynx-assistant-catalog-menus-local`. Cap `MobileAssistantTab` long-press Edit/Delete + empty Create + disabled Enable: Lynx `createLynxAssistant` (POST) / `setLynxAssistantsEnabled` (PUT settings + expectedRevision) / `deleteLynxAssistant` (DELETE + expectedRevision); AssistantTab overflow/long-press → Settings assistants EntityEditor + `LynxCenteredDialog` delete; empty Create → Settings assistants; disabled Enable CTA → PUT then refresh; Settings AssistantsSettingsBody Create (resolve first provider/model → POST → open editor) + Enable when disabled. Portable inline/banner errors only (no Cap toast). Real APIs only — never fake-success. Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host-only. Docs honesty: tip `c1bb93c4` / published APK `lynx-v2-debug-c1bb93c` (release exists for tip; do NOT invent newer APK for PR head); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA/@dnd-kit/MobileWindowMotion/iPad/Cap toast).
52. ~~**Cap ProjectsHome per-bucket Show more**~~ — 代码接上 in `cursor/lynx-projects-home-show-more-local`. Cap `visibleCountByBucket` default 3 / +7 Show more per project/worktree bucket: reuse SessionsSheet `sliceLynxSessionsSheetVisible` / `nextLynxSessionsSheetVisibleCount` / `collapseLynxSessionsSheetVisibleCount` + `lynxProjectsHomeBucketKey` (`projectId::worktreeId`); Show more / Show fewer on each worktree group (SessionsSheet already had flat 3/+7). Search uses full catalog (Cap `catalogSessions` spirit) — compact slice / Show more skipped while searching. Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host binders. Docs honesty: tip `cfed15657` / published APK `lynx-v2-debug-cfed156` (release exists for tip; was stale `c1bb93c`; do NOT invent newer APK for PR head); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 empty; host-only unchanged.

53. ~~**Android cssVar hex paint + strict smoke text=**~~ — 代码接上 in `cursor/lynx-first-paint-visible-smoke`. Yee 真机 有字无样式: `cssVar()` no longer emits `var(--name, #hex)` (Android drops those inline values); returns Flexoki-light hex. ConnectWelcome done-phase cream/elevated/primary plain hex + brand `OpenChamber Lynx`. Host TextView splash overlay for smoke. Strict `lynx-android-emulator-smoke.sh` requires literal `text=` (false-green content-desc/splash-log fixed). **三关: 真机过 still open** until Yee confirms. Product NOT DONE / 三关未齐 / not EXHAUSTED.

54. ~~**Cap MCP sheet live status + connect/disconnect**~~ — 代码接上 in `cursor/lynx-mcp-sheet-connect-local`. Cap `McpDropdownContent` / `useMcpStore`: Lynx chat MCP sheet loads `GET /mcp` status ∪ `/api/config/mcp` configs; status disc + Cap Switch ON/OFF → `POST /mcp/{name}/connect|disconnect`; refresh; honest failure (never fake-success). `needs_auth` labeled (OAuth system browser stays host-only). Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host binders. Docs honesty: tip `7c5a135a` / published APK `lynx-v2-debug-7c5a135` (release exists for tip; do NOT invent newer APK for PR head); product NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome style confirmation open for Yee.

55. ~~**Connect welcome elevated card chrome**~~ — 代码接上 in `cursor/lynx-connect-welcome-card-chrome`. After cssVar→hex, cream `#fffdf4` ≈ elevated `#fbfaf2` so done-phase cards still read as a flat stack on Android (softemu). Add visible chrome on pending-password / paste / add-instance / scan-QR cards: `borderWidth 1` + `borderColor` `interactive.selection` + `borderStyle solid` + `borderRadius 12` + elevated fill; optional soft `boxShadow` (do not rely on alone). Page bg stays cream. Hex-only (no `var()`). Prefer border over inventing palette. **三关: 真机过 still open** until Yee confirms card shells. Product NOT DONE / 三关未齐 / not EXHAUSTED; tip APK honesty stays `lynx-v2-debug-7c5a135` (do NOT invent newer for PR head).

56. ~~**Docs tip APK/SHA honesty post-#55**~~ — Cap phone skim (MobileApp / sessions / chat vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only. Next #56 = **docs honesty only** in `cursor/lynx-gap-board-apk-eca4a03`: tip `work/lynx-native` @ `eca4a0305` (PR #94 / Next #55 MERGED) + published prerelease APK `lynx-v2-debug-eca4a03` (was stale `lynx-v2-debug-7c5a135` / tip bases `5239dd7e6`/`7c5a135a9`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee); host-only unchanged.

57. ~~**Cap header swipe → sessions + transcript sync hint**~~ — 代码接上 in `cursor/lynx-header-swipe-sync-hint-local`. Cap `useHeaderSwipeToSessions` portable TS machine (RTL ~1/3 viewport open sessions; LTR back on phone secondary) + ChatScreen/shell wire to `onOpenSessionsSheet` / `onBack`; host pan bind via `headerSwipeDispatchRef` (same honesty as edge-swipe — do not fake native pan success). Cap `useMobileTranscriptSyncHint` resolve + show-delay/hide-grace smoother mapped to honest Lynx signals (timeline hydrate / overflow refresh / SSE live-tail phase) — **no** Cap Zustand sync stores. Header subtitle when syncing. Docs honesty: tip `work/lynx-native` @ `489336a34` (PR #95 / Next #56 MERGED) + published APK `lynx-v2-debug-489336a` (was stale `lynx-v2-debug-eca4a03` / tip `eca4a0305`; do NOT invent newer). Deferred unchanged: `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee).
58. ~~**Cap Files sheet search (`/api/find/file`)**~~ — 代码接上 in `cursor/lynx-files-search-local`. Cap `MobileFilesSurface` search via web `GET /api/find/file?directory=&query=&dirs=false&type=file&limit=` (+ `x-opencode-directory`); Lynx `searchLynxFiles` + Files sheet `LynxInput` debounce ~250ms / maxResults 40; tap hit → existing text preview. failure ≠ empty hits; empty query stays browse. Docs honesty: base tip `work/lynx-native` @ `4d871bad0` (PR #96 / Next #57 MERGED) + published APK `lynx-v2-debug-4d871ba` (was stale `lynx-v2-debug-489336a` / tip `489336a34`; do NOT invent newer — tip advances on merge). Deferred unchanged: `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk / host Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK).

59. ~~**Docs tip APK/SHA honesty post-#58**~~ — Cap phone skim (MobileApp / Files / Changes / SessionsSheet / chat header-swipe+sync / overflow vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA). Next #59 = **docs honesty only** in `cursor/lynx-gap-board-apk-53ea9cd`: tip `work/lynx-native` @ `53ea9cdee4` (PR #97 / Next #58 MERGED) + published prerelease APK `lynx-v2-debug-53ea9cd` (was stale `lynx-v2-debug-4d871ba` / tip `4d871bad0`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-53ea9cd`); host-only unchanged.
60. ~~**Docs tip APK/SHA honesty post-#59**~~ — Cap phone skim (MobileApp / Files / Changes / SessionsSheet / chat header-swipe+sync / overflow / MCP vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA). Next #60 = **docs honesty only** in `cursor/lynx-gap-board-apk-5ae491f`: tip `work/lynx-native` @ `5ae491f6a3` (PR #98 / Next #59 MERGED) + published prerelease APK `lynx-v2-debug-5ae491f` (was stale `lynx-v2-debug-53ea9cd` / tip `53ea9cdee4`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-5ae491f`); host-only unchanged.
61. ~~**Cap Changes stageAll / unstageAll + revertAll**~~ — 代码接上 in `cursor/lynx-changes-stage-all-local`. Cap `ChangesPanel` group header Stage all / Unstage all (`onActionAll` → existing `POST /api/git/stage|unstage` multi-path) + Cap revert-all toolbar with centered Dialog confirm (`revertLynxGitFiles`). Lynx Changes sheet now partitions staged/unstaged groups with GlassChrome +/− all chips; portable TS only (no Cap toast / `@dnd-kit`). Docs honesty: base tip `work/lynx-native` @ `c8a72d6025` (PR #99 / Next #60 MERGED) + published APK `lynx-v2-debug-5ae491f` (do NOT invent newer — tip APK still `5ae491f`). Deferred unchanged: Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk multi-select / host Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-5ae491f`).
62. ~~**Docs tip APK/SHA honesty post-#61**~~ — Cap phone skim (MobileApp / Files / Changes / SessionsSheet / chat header-overflow / MCP / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA/FCM for lynx.debug / Live Activity). Next #62 = **docs honesty only** in `cursor/lynx-gap-board-apk-773dd98`: tip `work/lynx-native` @ `773dd989b9` (PR #100 / Next #61 MERGED) + published prerelease APK `lynx-v2-debug-773dd98` (was stale `lynx-v2-debug-5ae491f` / tip bases `5ae491f6a3` / `c8a72d6025`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-773dd98`); host-only unchanged.
63. ~~**Docs tip APK/SHA honesty post-#62**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #63 = **docs honesty only** in `cursor/lynx-gap-board-apk-dda945b`: tip `work/lynx-native` @ `dda945b12` (PR #101 / Next #62 MERGED) + published prerelease APK `lynx-v2-debug-dda945b` (was stale `lynx-v2-debug-773dd98` / tip `773dd989b9`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-dda945b`); host-only unchanged.
64. ~~**Docs tip APK/SHA honesty post-#63**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #64 = **docs honesty only** in `cursor/lynx-gap-board-apk-2384ea6`: tip `work/lynx-native` @ `2384ea6c95` (PR #102 / Next #63 MERGED) + published prerelease APK `lynx-v2-debug-2384ea6` (was stale `lynx-v2-debug-dda945b` / tip `dda945b12`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-2384ea6`); host-only unchanged.
65. ~~**Docs tip APK/SHA honesty post-#64**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #65 = **docs honesty only** in `cursor/lynx-gap-board-apk-3f15ea4`: tip `work/lynx-native` @ `3f15ea4196` (PR #103 / Next #64 MERGED) + published prerelease APK `lynx-v2-debug-3f15ea4` (was stale `lynx-v2-debug-2384ea6` / tip `2384ea6c95`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3f15ea4`); host-only unchanged.
66. ~~**Docs tip APK/SHA honesty post-#65**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #66 = **docs honesty only** in `cursor/lynx-gap-board-apk-8ba8989`: tip `work/lynx-native` @ `8ba89898a31` (PR #104 / Next #65 MERGED) + published prerelease APK `lynx-v2-debug-8ba8989` (was stale `lynx-v2-debug-3f15ea4` / tip `3f15ea4196`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ba8989`); host-only unchanged.
67. ~~**Docs tip APK/SHA honesty post-#66**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #67 = **docs honesty only** in `cursor/lynx-gap-board-apk-538c312`: tip `work/lynx-native` @ `538c31239fa4` (PR #105 / Next #66 MERGED) + published prerelease APK `lynx-v2-debug-538c312` (was stale `lynx-v2-debug-8ba8989` / tip `8ba89898a31`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-538c312`); host-only unchanged.
68. ~~**Docs tip APK/SHA honesty post-#67**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #68 = **docs honesty only** in `cursor/lynx-gap-board-apk-cd074ee`: tip `work/lynx-native` @ `cd074ee2b2b3` (PR #106 / Next #67 MERGED) + published prerelease APK `lynx-v2-debug-cd074ee` (was stale `lynx-v2-debug-538c312` / tip `538c31239fa4`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-cd074ee`); host-only unchanged.
69. ~~**Docs tip APK/SHA honesty post-#68**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #69 = **docs honesty only** in `cursor/lynx-gap-board-apk-50fa262`: tip `work/lynx-native` @ `50fa2626e946` (PR #107 / Next #68 MERGED) + published prerelease APK `lynx-v2-debug-50fa262` (was stale `lynx-v2-debug-cd074ee` / tip `cd074ee2b2b3`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-50fa262`); host-only unchanged.
70. ~~**Docs tip APK/SHA honesty post-#69**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #70 = **docs honesty only** in `cursor/lynx-gap-board-apk-fb9a969`: tip `work/lynx-native` @ `fb9a969c7c0d` (PR #108 / Next #69 MERGED) + published prerelease APK `lynx-v2-debug-fb9a969` (was stale `lynx-v2-debug-50fa262` / tip `50fa2626e946`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-fb9a969`); host-only unchanged.
71. ~~**Docs tip APK/SHA honesty post-#70**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #71 = **docs honesty only** in `cursor/lynx-gap-board-apk-68b1919`: tip `work/lynx-native` @ `68b1919a566c` (PR #109 / Next #70 MERGED) + published prerelease APK `lynx-v2-debug-68b1919` (was stale `lynx-v2-debug-fb9a969` / tip `fb9a969c7c0d`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-68b1919`); host-only unchanged.
72. ~~**Docs tip APK/SHA honesty post-#71**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #72 = **docs honesty only** in `cursor/lynx-gap-board-apk-3b2ae4c`: tip `work/lynx-native` @ `3b2ae4c1283700794d3b5124605cee914c4dc545` (PR #110 / Next #71 MERGED) + published prerelease APK `lynx-v2-debug-3b2ae4c` (was stale `lynx-v2-debug-68b1919` / tip `68b1919a566c`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3b2ae4c`); host-only unchanged.
73. ~~**Cap Scheduled Run now + Delete**~~ — 代码接上 in `cursor/lynx-scheduled-run-delete-local` (PR #112). Cap `POST /api/projects/:projectId/scheduled-tasks/:taskId/run` + `DELETE …/scheduled-tasks/:taskId`; ScheduledEditor Run now + two-tap Delete; never fake-success. Docs honesty: base tip `ea6349303` (PR #111 / Next #72 MERGED); published APK at merge was `lynx-v2-debug-3b2ae4c` (tip later `0a56dbed` / APK `lynx-v2-debug-0a56dbe` — see Next #74). Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome **card chrome** confirm for Yee.
74. ~~**Docs tip APK/SHA honesty post-#73**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Next #74 = **docs honesty only** in `cursor/lynx-gap-board-apk-0a56dbe`: tip `work/lynx-native` @ `0a56dbedd1635ebe3cc31ed40fa5f428bc677bfc` (PR #112 / Next #73 MERGED) + published prerelease APK `lynx-v2-debug-0a56dbe` (was stale `lynx-v2-debug-3b2ae4c` / tip bases `3b2ae4c1283700794d3b5124605cee914c4dc545` / `ea6349303`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-0a56dbe`); host-only unchanged.
75. ~~**Docs tip APK/SHA honesty post-#74**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings key flows vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. Next #75 = **docs honesty only** in `cursor/lynx-gap-board-apk-b798684`: tip `work/lynx-native` @ `b7986841cc7341800880febf6958299e65d50592` (PR #113 / Next #74 MERGED) + published prerelease APK `lynx-v2-debug-b798684` (was stale `lynx-v2-debug-0a56dbe` / tip `0a56dbedd1635ebe3cc31ed40fa5f428bc677bfc`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-b798684`); host-only unchanged.
76. ~~**Docs tip APK/SHA honesty post-#75**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. PR #115 bound auto-connect splash (`cursor/lynx-autoconnect-timeout-local`) already landed on tip — Connect welcome appears after timeout; not a remaining gap. Next #76 = **docs honesty only** in `cursor/lynx-gap-board-apk-51ab13b`: tip `work/lynx-native` @ `51ab13b832164fac30956bb1297e2f1d93ed430b` (PR #115 auto-connect timeout MERGED; PR #114 / Next #75 MERGED) + published prerelease APK `lynx-v2-debug-51ab13b` (was stale `lynx-v2-debug-b798684` / tip `b7986841cc7341800880febf6958299e65d50592`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-51ab13b`); host-only unchanged. **MERGED** as PR #116 — tip advanced to `896bd125704d86d11089ee138ccdd841a8a21a94` / APK `lynx-v2-debug-896bd12` (see Next #77).
77. ~~**Docs tip APK/SHA honesty post-#76**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #77 = **docs honesty only** in `cursor/lynx-gap-board-apk-896bd12`: tip `work/lynx-native` @ `896bd125704d86d11089ee138ccdd841a8a21a94` (PR #116 / Next #76 MERGED) + published prerelease APK `lynx-v2-debug-896bd12` (was stale `lynx-v2-debug-51ab13b` / tip `51ab13b832164fac30956bb1297e2f1d93ed430b`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-896bd12`); host-only unchanged. **MERGED** as PR #117 — tip advanced to `51870e1d175114ce0d4ad820db41311bd702d083` / APK `lynx-v2-debug-51870e1` (see Next #78).
78. ~~**Docs tip APK/SHA honesty post-#77**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #78 = **docs honesty only** in `cursor/lynx-gap-board-apk-51870e1`: tip `work/lynx-native` @ `51870e1d175114ce0d4ad820db41311bd702d083` (PR #117 / Next #77 MERGED) + published prerelease APK `lynx-v2-debug-51870e1` (was stale `lynx-v2-debug-896bd12` / tip `896bd125704d86d11089ee138ccdd841a8a21a94`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-51870e1`); host-only unchanged. **MERGED** as PR #118 — tip advanced to `46435859225186b343d439d0744385f651c1d9a2` / APK `lynx-v2-debug-4643585` (see Next #79).
79. ~~**Docs tip APK/SHA honesty post-#78**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #79 = **docs honesty only** in `cursor/lynx-gap-board-apk-4643585`: tip `work/lynx-native` @ `46435859225186b343d439d0744385f651c1d9a2` (PR #118 / Next #78 MERGED) + published prerelease APK `lynx-v2-debug-4643585` (was stale `lynx-v2-debug-51870e1` / tip `51870e1d175114ce0d4ad820db41311bd702d083`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-4643585`); host-only unchanged. **MERGED** as PR #119 — tip advanced to `cf705f5d1458824ac1a8270d498cd83b3e44ff94` / APK `lynx-v2-debug-cf705f5` (see Next #80).
80. ~~**Docs tip APK/SHA honesty post-#79**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #80 = **docs honesty only** in `cursor/lynx-gap-board-apk-cf705f5`: tip `work/lynx-native` @ `cf705f5d1458824ac1a8270d498cd83b3e44ff94` (PR #119 / Next #79 MERGED) + published prerelease APK `lynx-v2-debug-cf705f5` (was stale `lynx-v2-debug-4643585` / tip `46435859225186b343d439d0744385f651c1d9a2`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-cf705f5`); host-only unchanged. **MERGED** as PR #120 — tip advanced to `267ba627842656370ae9b1295c4b59ff9c505c38` / APK `lynx-v2-debug-267ba62` (see Next #81).
81. ~~**Docs tip APK/SHA honesty post-#80**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #81 = **docs honesty only** in `cursor/lynx-gap-board-apk-267ba62`: tip `work/lynx-native` @ `267ba627842656370ae9b1295c4b59ff9c505c38` (PR #120 / Next #80 MERGED) + published prerelease APK `lynx-v2-debug-267ba62` (was stale `lynx-v2-debug-cf705f5` / tip `cf705f5d1458824ac1a8270d498cd83b3e44ff94`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-267ba62`); host-only unchanged. **MERGED** as PR #121 — tip advanced to `0b47a71f14d8a1945dae14e333ba695451707950` / APK `lynx-v2-debug-0b47a71` (see Next #82).
82. ~~**Docs tip APK/SHA honesty post-#81**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #82 = **docs honesty only** in `cursor/lynx-gap-board-apk-0b47a71`: tip `work/lynx-native` @ `0b47a71f14d8a1945dae14e333ba695451707950` (PR #121 / Next #81 MERGED) + published prerelease APK `lynx-v2-debug-0b47a71` (was stale `lynx-v2-debug-267ba62` / tip `267ba627842656370ae9b1295c4b59ff9c505c38`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-0b47a71`); host-only unchanged. **MERGED** as PR #122 — tip advanced to `5287db7438ae51bad0033ee6614c83fee7ad6f2a` / APK `lynx-v2-debug-5287db7` (see Next #83).
83. ~~**Docs tip APK/SHA honesty post-#82**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #83 = **docs honesty only** in `cursor/lynx-gap-board-apk-5287db7`: tip `work/lynx-native` @ `5287db7438ae51bad0033ee6614c83fee7ad6f2a` (PR #122 / Next #82 MERGED) + published prerelease APK `lynx-v2-debug-5287db7` (was stale `lynx-v2-debug-0b47a71` / tip `0b47a71f14d8a1945dae14e333ba695451707950`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-5287db7`); host-only unchanged. **MERGED** as PR #123 — tip advanced to `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f` / APK `lynx-v2-debug-3d0d768` (see Next #84).
84. ~~**Docs tip APK/SHA honesty post-#83**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #84 = **docs honesty only** in `cursor/lynx-gap-board-apk-3d0d768`: tip `work/lynx-native` @ `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f` (PR #123 / Next #83 MERGED) + published prerelease APK `lynx-v2-debug-3d0d768` (was stale `lynx-v2-debug-5287db7` / tip `5287db7438ae51bad0033ee6614c83fee7ad6f2a`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3d0d768`); host-only unchanged. **MERGED** as PR #124 — tip advanced to `607eaf6dfc726f7d18fd6eac2b44542e39365b09` / APK `lynx-v2-debug-607eaf6` (see Next #85).
85. ~~**Docs tip APK/SHA honesty post-#84**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #85 = **docs honesty only** in `cursor/lynx-gap-board-apk-607eaf6`: tip `work/lynx-native` @ `607eaf6dfc726f7d18fd6eac2b44542e39365b09` (PR #124 / Next #84 MERGED) + published prerelease APK `lynx-v2-debug-607eaf6` (was stale `lynx-v2-debug-3d0d768` / tip `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-607eaf6`); host-only unchanged. **MERGED** as PR #125 — tip advanced to `edaec0d9bbab4f132a51e4fde3259dd994b49369` / APK `lynx-v2-debug-edaec0d` (see Next #86).
86. ~~**Docs tip APK/SHA honesty post-#85**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #86 = **docs honesty only** in `cursor/lynx-gap-board-apk-edaec0d`: tip `work/lynx-native` @ `edaec0d9bbab4f132a51e4fde3259dd994b49369` (PR #125 / Next #85 MERGED) + published prerelease APK `lynx-v2-debug-edaec0d` (was stale `lynx-v2-debug-607eaf6` / tip `607eaf6dfc726f7d18fd6eac2b44542e39365b09`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-edaec0d`); host-only unchanged. **MERGED** as PR #126 — tip advanced to `dbad742588da5f7c575d23c5269b3317dd1cd1ce` / APK `lynx-v2-debug-dbad742` (see Next #87).
87. ~~**Docs tip APK/SHA honesty post-#86**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #87 = **docs honesty only** in `cursor/lynx-gap-board-apk-dbad742`: tip `work/lynx-native` @ `dbad742588da5f7c575d23c5269b3317dd1cd1ce` (PR #126 / Next #86 MERGED) + published prerelease APK `lynx-v2-debug-dbad742` (was stale `lynx-v2-debug-edaec0d` / tip `edaec0d9bbab4f132a51e4fde3259dd994b49369`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-dbad742`); host-only unchanged. **MERGED** as PR #127 — tip advanced to `4d16bf8070865883c79b0e13a3c47e63d573311c` / APK `lynx-v2-debug-4d16bf8` (see Next #88).
88. ~~**Docs tip APK/SHA honesty post-#87**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #88 = **docs honesty only** in `cursor/lynx-gap-board-apk-4d16bf8`: tip `work/lynx-native` @ `4d16bf8070865883c79b0e13a3c47e63d573311c` (PR #127 / Next #87 MERGED) + published prerelease APK `lynx-v2-debug-4d16bf8` (was stale `lynx-v2-debug-dbad742` / tip `dbad742588da5f7c575d23c5269b3317dd1cd1ce`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-4d16bf8`); host-only unchanged. **MERGED** as PR #128 — tip advanced to `8ec03842faed8caa6c255a237e617ef1ddf2e89f` / APK `lynx-v2-debug-8ec0384` (see Next #89).
89. ~~**Docs tip APK/SHA honesty post-#88**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #89 = **docs honesty only** in `cursor/lynx-gap-board-apk-8ec0384`: tip `work/lynx-native` @ `8ec03842faed8caa6c255a237e617ef1ddf2e89f` (PR #128 / Next #88 MERGED) + published prerelease APK `lynx-v2-debug-8ec0384` (was stale `lynx-v2-debug-4d16bf8` / tip `4d16bf8070865883c79b0e13a3c47e63d573311c`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ec0384`); host-only unchanged. **MERGED** as PR #129 — tip advanced to `c451d696bded3b8793d5a444b9ba39f43cff8dac` / published APK still `lynx-v2-debug-8ec0384` (see Next #90).
90. ~~**Cap Settings `archived-sessions` slug + page body**~~ — 代码接上 in `cursor/lynx-settings-archived-sessions-b1f2`. Cap **main** `MOBILE_SETTINGS_PAGE_SLUGS` is 22 including `archived-sessions` (tip Cap `packages/ui` still the older 21). Settings in-tab push: Cap `ArchivedSessionsPage` / `ArchivedSessionsManager mode="page"` spirit reusing `listLynxArchivedSessions` / `unarchiveLynxSession` (no duplicate API clients). Failure ≠ empty; honest no-runtime; no Cap toast / no invented ASR. Slug tests lock 22. Docs honesty: base tip `c451d696bded3b8793d5a444b9ba39f43cff8dac` (PR #129 / Next #89 MERGED) + published APK `lynx-v2-debug-8ec0384` (release exists for tip `8ec03842f`; do NOT invent a newer APK for this PR head). Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee); host-only unchanged. Out of scope at merge: Assistant unread + mark-all-read (closed in Next #91); host binders; merge to main; `@dnd-kit`. **MERGED** as PR #130 — tip advanced to `4cc762cd2a4a9be26e75cdef84e73b24b7d51b88`; published APK later `lynx-v2-debug-4cc762c` (see Next #91 / #92).
91. ~~**Cap Assistant unread + mark-all-read**~~ — 代码接上 in `cursor/lynx-assistant-unread-4024`. Cap **main** `AssistantUnreadBadge` / `AssistantMarkAllReadButton` / `AssistantReadMarker` + `POST /api/openchamber/assistants/:id/contact/read` `{ generation, ordinal, messageID }`. Lynx: parse `unreadCount` (default 0) + nullable `readTip`/`readWatermark`; reject malformed; `markLynxAssistantContactRead` + `markAllLynxAssistantsRead` (unread+`readTip`, batch 4, `{ failed }`); catalog row + dock badges (99+); mark-all ghost-xs spirit + portable banner (no Cap toast); portable open/latest-visible mark using snapshot `readTip` (Cap IntersectionObserver / scroll-bottom / focus geometry = host residual). Failure ≠ empty; never fake mark-success. Docs honesty: base tip `work/lynx-native` @ `4cc762cd2a4a9be26e75cdef84e73b24b7d51b88` (PR #130 / Next #90 MERGED) + published APK `lynx-v2-debug-8ec0384` (release exists for tip `8ec03842f`; do NOT invent a newer APK for this PR head). Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee); host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA/@dnd-kit/MobileWindowMotion/iPad/Cap toast/bulk). **MERGED** as PR #131 — tip advanced to `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65` / published APK `lynx-v2-debug-4cc762c` (see Next #92).
92. ~~**Docs tip APK/SHA honesty post-#91**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #90 archived-sessions **MERGED** as PR #130; Next #91 Assistant unread **MERGED** as PR #131. Next #92 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-3a1d`: tip `work/lynx-native` @ `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65` (PR #131 / Next #91 MERGED) + published prerelease APK `lynx-v2-debug-4cc762c` (release exists for tip after PR #130 / `4cc762cd2`; no `lynx-v2-debug-3fda6eb` at write time — do NOT invent). Mobile CI may later publish for newer tip. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-4cc762c`); host-only unchanged. **MERGED** as PR #132 — tip advanced to `63da3d63aa354eb66677f6ff57d5dd546ddca24b` / APK `lynx-v2-debug-63da3d6` (see Next #93).
93. ~~**Docs tip APK/SHA honesty post-#92**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #92 **MERGED** as PR #132. Next #93 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-63da3d6-7704`: tip `work/lynx-native` @ `63da3d63aa354eb66677f6ff57d5dd546ddca24b` (PR #132 / Next #92 MERGED) + published prerelease APK `lynx-v2-debug-63da3d6` (was stale `lynx-v2-debug-4cc762c` / tip `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-63da3d6`); host-only unchanged. **MERGED** as PR #133 — tip advanced to `50af885b81849d92bed38116930abc3ddbbf88ef` / APK `lynx-v2-debug-50af885` (see Next #94).
94. ~~**Docs tip APK/SHA honesty post-#93**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #93 **MERGED** as PR #133. Next #94 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-50af885-1428`: tip `work/lynx-native` @ `50af885b81849d92bed38116930abc3ddbbf88ef` (PR #133 / Next #93 MERGED) + published prerelease APK `lynx-v2-debug-50af885` (was stale `lynx-v2-debug-63da3d6` / tip `63da3d63aa354eb66677f6ff57d5dd546ddca24b`). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-50af885`); host-only unchanged. **MERGED** as PR #134 — tip advanced to `4520eb00be8bd1cc8faea2cca151d3d55ae39b91` / published APK still `lynx-v2-debug-50af885` (Mobile CI red on tip blocked republish; see Next #95).
95. ~~**Docs tip APK/SHA honesty post-#94**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #94 **MERGED** as PR #134. Next #95 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-4520eb00-86f4`: tip `work/lynx-native` @ `4520eb00be8bd1cc8faea2cca151d3d55ae39b91` (PR #134 / Next #94 MERGED) + published prerelease APK `lynx-v2-debug-50af885` (release exists; was the tip before this honesty merge / PR #133 / `50af885b81849d92bed38116930abc3ddbbf88ef`). Do NOT invent a newer APK than published (no `lynx-v2-debug-4520eb00`). Lynx Mobile CI on tip `4520eb00` FAILED at Setup Android SDK (`android-actions/setup-android@v3`: Failed to find package 'tools') — no newer published APK for `4520eb00`. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-50af885`); host-only unchanged. **MERGED** as PR #136 — tip advanced to `ee82d53a736a321b6ad6fd4573b03575def90eeb` / published APK `lynx-v2-debug-ee82d53` (see Next #96).
96. ~~**Docs tip APK/SHA honesty post-#95**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #95 **MERGED** as PR #136. Next #96 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-ee82d53-99c4`: tip `work/lynx-native` @ `ee82d53a736a321b6ad6fd4573b03575def90eeb` (PR #136 / Next #95 MERGED; includes PR #135 Mobile CI setup-android pin) + published prerelease APK `lynx-v2-debug-ee82d53` (was stale `lynx-v2-debug-50af885` / tip `4520eb00be8bd1cc8faea2cca151d3d55ae39b91`). PR #135 pinned setup-android packages (`tools` removed upstream → `packages:platform-tools`); Mobile CI SUCCESS on tip after #135/#136 republished `lynx-v2-debug-ee82d53`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ee82d53`); host-only unchanged. **MERGED** as PR #137 — tip advanced to `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464` / published APK `lynx-v2-debug-d10c92d` (see Next #97).
97. ~~**Docs tip APK/SHA honesty post-#96**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #96 **MERGED** as PR #137. Next #97 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-d10c92d-eb2e`: tip `work/lynx-native` @ `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464` (PR #137 / Next #96 MERGED) + published prerelease APK `lynx-v2-debug-d10c92d` (was stale `lynx-v2-debug-ee82d53` / tip `ee82d53a736a321b6ad6fd4573b03575def90eeb`). Lynx Mobile CI SUCCESS on tip `d10c92db` after #137 republished `lynx-v2-debug-d10c92d`. lynx-ci may not have re-fired on this docs-only tip push (OK; not claimed red). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d10c92d`); host-only unchanged. **MERGED** as PR #138 — tip advanced to `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8` / published APK `lynx-v2-debug-a92b319` (see Next #98).
98. ~~**Docs tip APK/SHA honesty post-#97**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #97 **MERGED** as PR #138. Next #98 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-a92b319-743c`: tip `work/lynx-native` @ `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8` (PR #138 / Next #97 MERGED) + published prerelease APK `lynx-v2-debug-a92b319` (was stale `lynx-v2-debug-d10c92d` / tip `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464`). Lynx Mobile CI SUCCESS on tip `a92b319` (run 34943000499) after #138 republished `lynx-v2-debug-a92b319`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a92b319`); host-only unchanged. **MERGED** as PR #139 — tip advanced to `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2` / published APK `lynx-v2-debug-f1cf09e` (see Next #99).
99. ~~**Docs tip APK/SHA honesty post-#98**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #98 **MERGED** as PR #139. Next #99 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-f1cf09e-2a7e`: tip `work/lynx-native` @ `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2` (PR #139 / Next #98 MERGED) + published prerelease APK `lynx-v2-debug-f1cf09e` (was stale `lynx-v2-debug-a92b319` / tip `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8`). Lynx Mobile CI SUCCESS on tip `f1cf09ef` (run 34956940347) after #139 republished `lynx-v2-debug-f1cf09e`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-f1cf09e`); host-only unchanged. **MERGED** as PR #140 — tip advanced to `41d632abebf2c62088d46371bb5761c73925dc7b` / published APK `lynx-v2-debug-41d632a` (see Next #100).
100. ~~**Docs tip APK/SHA honesty post-#99**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #99 **MERGED** as PR #140. Next #100 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-41d632a-0ec0`: tip `work/lynx-native` @ `41d632abebf2c62088d46371bb5761c73925dc7b` (PR #140 / Next #99 MERGED) + published prerelease APK `lynx-v2-debug-41d632a` (was stale `lynx-v2-debug-f1cf09e` / tip `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2`). Lynx Mobile CI SUCCESS on tip `41d632ab` (run 34958905237) after #140 republished `lynx-v2-debug-41d632a`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-41d632a`); host-only unchanged. **MERGED** as PR #141 — tip advanced to `32aed54c6f1585f6da2a35c82ac16f78db92933e` / published APK `lynx-v2-debug-32aed54` (see Next #101).
101. ~~**Docs tip APK/SHA honesty post-#100**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #100 **MERGED** as PR #141. Next #101 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-32aed54-6fc4`: tip `work/lynx-native` @ `32aed54c6f1585f6da2a35c82ac16f78db92933e` (PR #141 / Next #100 MERGED) + published prerelease APK `lynx-v2-debug-32aed54` (was stale `lynx-v2-debug-41d632a` / tip `41d632abebf2c62088d46371bb5761c73925dc7b`). Lynx Mobile CI SUCCESS on tip `32aed54c` (run 34976026554) after #141 republished `lynx-v2-debug-32aed54`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-32aed54`); host-only unchanged. **MERGED** as PR #142 — tip advanced to `105884e5067dd5ba26bb49c2957bb255deea92cd` / published APK `lynx-v2-debug-105884e` (see Next #102).
102. ~~**Docs tip APK/SHA honesty post-#101**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #101 **MERGED** as PR #142. Next #102 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-105884e-849e`: tip `work/lynx-native` @ `105884e5067dd5ba26bb49c2957bb255deea92cd` (PR #142 / Next #101 MERGED) + published prerelease APK `lynx-v2-debug-105884e` (was stale `lynx-v2-debug-32aed54` / tip `32aed54c6f1585f6da2a35c82ac16f78db92933e`). Lynx Mobile CI SUCCESS on tip `105884e` (run 35015640496) after #142 republished `lynx-v2-debug-105884e`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-105884e`); host-only unchanged. **MERGED** as PR #143 — tip advanced to `ac8b4c99c5e821ac435bea83daec65eb8f7e7171` / published APK `lynx-v2-debug-ac8b4c9` (see Next #103).
103. ~~**Docs tip APK/SHA honesty post-#102**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #102 **MERGED** as PR #143. Next #103 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-ac8b4c9-55c6`: tip `work/lynx-native` @ `ac8b4c99c5e821ac435bea83daec65eb8f7e7171` (PR #143 / Next #102 MERGED) + published prerelease APK `lynx-v2-debug-ac8b4c9` (was stale `lynx-v2-debug-105884e` / tip `105884e5067dd5ba26bb49c2957bb255deea92cd`). Lynx Mobile CI SUCCESS on tip `ac8b4c99` (run 35031338604) after #143 republished `lynx-v2-debug-ac8b4c9`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ac8b4c9`); host-only unchanged. **MERGED** as PR #144 — tip advanced to `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76` / published APK `lynx-v2-debug-bb7aad6` (see Next #104).

104. ~~**Docs tip APK/SHA honesty post-#103**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #103 **MERGED** as PR #144. Next #104 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-bb7aad6-e044`: tip `work/lynx-native` @ `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76` (PR #144 / Next #103 MERGED) + published prerelease APK `lynx-v2-debug-bb7aad6` (was stale `lynx-v2-debug-ac8b4c9` / tip `ac8b4c99c5e821ac435bea83daec65eb8f7e7171`). Lynx Mobile CI SUCCESS on tip `bb7aad63` (run 35044475784) after #144 republished `lynx-v2-debug-bb7aad6`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-bb7aad6`); host-only unchanged. **MERGED** as PR #145 — tip advanced to `14211c02c298ebfcf4b0e2d1720ee5c9635358bf` / published APK `lynx-v2-debug-14211c0` (see Next #105).

105. ~~**Docs tip APK/SHA honesty post-#104**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #104 **MERGED** as PR #145. Next #105 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-14211c0-0287`: tip `work/lynx-native` @ `14211c02c298ebfcf4b0e2d1720ee5c9635358bf` (PR #145 / Next #104 MERGED) + published prerelease APK `lynx-v2-debug-14211c0` (was stale `lynx-v2-debug-bb7aad6` / tip `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76`). Lynx Mobile CI SUCCESS on tip `14211c02` (run 35055816332) after #145 republished `lynx-v2-debug-14211c0`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-14211c0`); host-only unchanged. **MERGED** as PR #146 — tip advanced to `a9f6605301df86cbe2b8b7ad124aaac02142a834` / published APK `lynx-v2-debug-a9f6605` (see Next #106).

106. ~~**Docs tip APK/SHA honesty post-#105**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #105 **MERGED** as PR #146. Next #106 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-a9f6605-7da5`: tip `work/lynx-native` @ `a9f6605301df86cbe2b8b7ad124aaac02142a834` (PR #146 / Next #105 MERGED) + published prerelease APK `lynx-v2-debug-a9f6605` (was stale `lynx-v2-debug-14211c0` / tip `14211c02c298ebfcf4b0e2d1720ee5c9635358bf`). Lynx Mobile CI SUCCESS on tip `a9f660530` (run 35069830726) after #146 republished `lynx-v2-debug-a9f6605`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a9f6605`); host-only unchanged. **MERGED** as PR #147 — tip advanced to `7e4af21a4f651fdf051905572188456fef28e72c` / published APK `lynx-v2-debug-7e4af21` (see Next #107).

107. ~~**Docs tip APK/SHA honesty post-#106**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #106 **MERGED** as PR #147. Next #107 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-7e4af21-05d8`: tip `work/lynx-native` @ `7e4af21a4f651fdf051905572188456fef28e72c` (PR #147 / Next #106 MERGED) + published prerelease APK `lynx-v2-debug-7e4af21` (was stale `lynx-v2-debug-a9f6605` / tip `a9f6605301df86cbe2b8b7ad124aaac02142a834`). Lynx Mobile CI SUCCESS on tip `7e4af21` (run 35083475005) after #147 republished `lynx-v2-debug-7e4af21`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-7e4af21`); host-only unchanged. **MERGED** as PR #148 — tip advanced to `22a4bc746ef851bc05d0710662e65d298fee540f` / published APK `lynx-v2-debug-22a4bc7` (see Next #108).

108. ~~**Docs tip APK/SHA honesty post-#107**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #107 **MERGED** as PR #148. Next #108 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-22a4bc7-6fb9`: tip `work/lynx-native` @ `22a4bc746ef851bc05d0710662e65d298fee540f` (PR #148 / Next #107 MERGED) + published prerelease APK `lynx-v2-debug-22a4bc7` (was stale `lynx-v2-debug-7e4af21` / tip `7e4af21a4f651fdf051905572188456fef28e72c`). Lynx Mobile CI SUCCESS on tip `22a4bc7` (run 35090970058) after #148 republished `lynx-v2-debug-22a4bc7`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-22a4bc7`); host-only unchanged. **MERGED** as PR #149 — tip advanced to `d256387d51cd4f166f86320d71b31804fe104243` / published APK `lynx-v2-debug-d256387` (see Next #109).

109. ~~**Docs tip APK/SHA honesty post-#108**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #108 **MERGED** as PR #149. Next #109 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-d256387-150c`: tip `work/lynx-native` @ `d256387d51cd4f166f86320d71b31804fe104243` (PR #149 / Next #108 MERGED) + published prerelease APK `lynx-v2-debug-d256387` (was stale `lynx-v2-debug-22a4bc7` / tip `22a4bc746ef851bc05d0710662e65d298fee540f`). Lynx Mobile CI SUCCESS on tip `d256387` (run 35104578745) after #149 republished `lynx-v2-debug-d256387`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d256387`); host-only unchanged. **MERGED** as PR #150 — tip advanced to `50934d8cf44a463310284d55a815a1d7426fb78b` / published APK `lynx-v2-debug-50934d8` (see Next #110).

110. ~~**Docs tip APK/SHA honesty post-#109**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #109 **MERGED** as PR #150. Next #110 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-50934d8-1f4a`: tip `work/lynx-native` @ `50934d8cf44a463310284d55a815a1d7426fb78b` (PR #150 / Next #109 MERGED) + published prerelease APK `lynx-v2-debug-50934d8` (was stale `lynx-v2-debug-d256387` / tip `d256387d51cd4f166f86320d71b31804fe104243`). Lynx Mobile CI SUCCESS on tip `50934d8` (run 35158475515) after #150 republished `lynx-v2-debug-50934d8`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-50934d8`); host-only unchanged. **MERGED** as PR #151 — tip advanced to `c510559b425dcd0a8e06e5961e849570e744f014` / published APK `lynx-v2-debug-c510559` (see Next #111).

111. ~~**Docs tip APK/SHA honesty post-#110**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #110 **MERGED** as PR #151. Next #111 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-c510559-f08d`: tip `work/lynx-native` @ `c510559b425dcd0a8e06e5961e849570e744f014` (PR #151 / Next #110 MERGED) + published prerelease APK `lynx-v2-debug-c510559` (was stale `lynx-v2-debug-50934d8` / tip `50934d8cf44a463310284d55a815a1d7426fb78b`). Lynx Mobile CI SUCCESS on tip `c510559` (run 35171179409) after #151 republished `lynx-v2-debug-c510559`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c510559`); host-only unchanged. **MERGED** as PR #152 — tip advanced to `b32e74c05ff9041d787442cc3ab4087d2f15e87e` / published APK `lynx-v2-debug-b32e74c` (see Next #112).

112. ~~**Docs tip APK/SHA honesty post-#111**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #111 **MERGED** as PR #152. Next #112 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-b32e74c-176f`: tip `work/lynx-native` @ `b32e74c05ff9041d787442cc3ab4087d2f15e87e` (PR #152 / Next #111 MERGED) + published prerelease APK `lynx-v2-debug-b32e74c` (was stale `lynx-v2-debug-c510559` / tip `c510559b425dcd0a8e06e5961e849570e744f014`). Lynx Mobile CI SUCCESS on tip `b32e74c` (run 35181873152) after #152 republished `lynx-v2-debug-b32e74c`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b32e74c`); host-only unchanged. **MERGED** as PR #153 — tip advanced to `aa08bbf7ed68ce46503b21db444c4aafb1787448` / published APK `lynx-v2-debug-aa08bbf` (see Next #113).

113. ~~**Docs tip APK/SHA honesty post-#112**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #112 **MERGED** as PR #153. Next #113 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-aa08bbf-a7ad`: tip `work/lynx-native` @ `aa08bbf7ed68ce46503b21db444c4aafb1787448` (PR #153 / Next #112 MERGED) + published prerelease APK `lynx-v2-debug-aa08bbf` (was stale `lynx-v2-debug-b32e74c` / tip `b32e74c05ff9041d787442cc3ab4087d2f15e87e`). Lynx Mobile CI SUCCESS on tip `aa08bbf` (run 35195640711) after #153 republished `lynx-v2-debug-aa08bbf`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-aa08bbf`); host-only unchanged. **MERGED** as PR #154 — tip advanced to `1142c4e2b0863c45ee25bc2452ea03078220155e` / published APK `lynx-v2-debug-1142c4e` (see Next #114).

114. ~~**Docs tip APK/SHA honesty post-#113**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #113 **MERGED** as PR #154. Next #114 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-1142c4e-522c`: tip `work/lynx-native` @ `1142c4e2b0863c45ee25bc2452ea03078220155e` (PR #154 / Next #113 MERGED) + published prerelease APK `lynx-v2-debug-1142c4e` (was stale `lynx-v2-debug-aa08bbf` / tip `aa08bbf7ed68ce46503b21db444c4aafb1787448`). Lynx Mobile CI SUCCESS on tip `1142c4e` (run 35209662353) after #154 republished `lynx-v2-debug-1142c4e`. Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-1142c4e`); host-only unchanged. **MERGED** as PR #155 — tip advanced to `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18` / published APK `lynx-v2-debug-a0dd0a9` (see Next #115).

115. ~~**Docs tip APK/SHA honesty post-#114**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #114 **MERGED** as PR #155. Next #115 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-a0dd0a9-07b2`: tip `work/lynx-native` @ `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18` (PR #155 / Next #114 MERGED) + published prerelease APK `lynx-v2-debug-a0dd0a9` (was stale `lynx-v2-debug-1142c4e` / tip `1142c4e2b0863c45ee25bc2452ea03078220155e`). Lynx Mobile CI SUCCESS on tip `a0dd0a9` (run 35211291083). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a0dd0a9`); host-only unchanged. **MERGED** as PR #156 — tip advanced to `04325ba8dfcd8700d4790670a03e2570d4f4acc4` / published APK `lynx-v2-debug-04325ba` (see Next #116).

116. ~~**Docs tip APK/SHA honesty post-#115**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #115 **MERGED** as PR #156. Next #116 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-04325ba-04ae`: tip `work/lynx-native` @ `04325ba8dfcd8700d4790670a03e2570d4f4acc4` (PR #156 / Next #115 MERGED) + published prerelease APK `lynx-v2-debug-04325ba` (was stale `lynx-v2-debug-a0dd0a9` / tip `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18`). Lynx Mobile CI SUCCESS on tip `04325ba` (run 35229914916). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-04325ba`); host-only unchanged. **MERGED** as PR #157 — tip advanced to `4b4fbcc0f0e05eae50c12177f8486682a544b381` / published APK `lynx-v2-debug-4b4fbcc` (see Next #117).

117. ~~**Docs tip APK/SHA honesty post-#116**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #116 **MERGED** as PR #157. Next #117 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-4b4fbcc-2e79`: tip `work/lynx-native` @ `4b4fbcc0f0e05eae50c12177f8486682a544b381` (PR #157 / Next #116 MERGED) + published prerelease APK `lynx-v2-debug-4b4fbcc` (was stale `lynx-v2-debug-04325ba` / tip `04325ba8dfcd8700d4790670a03e2570d4f4acc4`). Lynx Mobile CI SUCCESS on tip `4b4fbcc` (run 35249088590). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-4b4fbcc`); host-only unchanged. **MERGED** as PR #158 — tip advanced to `554e70e18dc18fe509ae02fdcdd43faead587ed1` / published APK `lynx-v2-debug-554e70e` (see Next #118).

118. ~~**Docs tip APK/SHA honesty post-#117**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #117 **MERGED** as PR #158. Next #118 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-554e70e-17cf`: tip `work/lynx-native` @ `554e70e18dc18fe509ae02fdcdd43faead587ed1` (PR #158 / Next #117 MERGED) + published prerelease APK `lynx-v2-debug-554e70e` (was stale `lynx-v2-debug-4b4fbcc` / tip `4b4fbcc0f0e05eae50c12177f8486682a544b381`). Lynx Mobile CI SUCCESS on tip `554e70e` (run 35266255595). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-554e70e`); host-only unchanged. **MERGED** as PR #159 — tip advanced to `6215ead5164c086d720629165ab31aac3c3b15a1` / published APK `lynx-v2-debug-6215ead` (see Next #119).

119. ~~**Docs tip APK/SHA honesty post-#118**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #118 **MERGED** as PR #159. Next #119 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-6215ead-a6ae`: tip `work/lynx-native` @ `6215ead5164c086d720629165ab31aac3c3b15a1` (PR #159 / Next #118 MERGED) + published prerelease APK `lynx-v2-debug-6215ead` (was stale `lynx-v2-debug-554e70e` / tip `554e70e18dc18fe509ae02fdcdd43faead587ed1`). Lynx Mobile CI SUCCESS on tip `6215ead` (run 35283067717). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6215ead`); host-only unchanged. **MERGED** as PR #160 — tip advanced to `06d4ac4138eba038259490d857be37ba59c8daad` / published APK `lynx-v2-debug-06d4ac4` (see Next #120).

120. ~~**Docs tip APK/SHA honesty post-#119**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #119 **MERGED** as PR #160. Next #120 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-06d4ac4-58e5`: tip `work/lynx-native` @ `06d4ac4138eba038259490d857be37ba59c8daad` (PR #160 / Next #119 MERGED) + published prerelease APK `lynx-v2-debug-06d4ac4` (was stale `lynx-v2-debug-6215ead` / tip `6215ead5164c086d720629165ab31aac3c3b15a1`). Lynx Mobile CI SUCCESS on tip `06d4ac4` (run 35295752653). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-06d4ac4`); host-only unchanged. **MERGED** as PR #161 — tip advanced to `3a87f8bcd0e575a747909a6042ae881e8666098c` / published APK `lynx-v2-debug-3a87f8b` (see Next #121).

121. ~~**Docs tip APK/SHA honesty post-#120**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #120 **MERGED** as PR #161. Next #121 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-3a87f8b-c158`: tip `work/lynx-native` @ `3a87f8bcd0e575a747909a6042ae881e8666098c` (PR #161 / Next #120 MERGED) + published prerelease APK `lynx-v2-debug-3a87f8b` (was stale `lynx-v2-debug-06d4ac4` / tip `06d4ac4138eba038259490d857be37ba59c8daad`; tip delta `06d4ac4`→`3a87f8b` is docs-only). Lynx Mobile CI SUCCESS on tip `3a87f8b` (run 35307346909). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3a87f8b`); host-only unchanged. **MERGED** as PR #162 — tip advanced to `5018ded1c818d06d07c6a60667e36c16004f3d44` / published APK `lynx-v2-debug-5018ded` (see Next #122).

122. ~~**Docs tip APK/SHA honesty post-#121**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #121 **MERGED** as PR #162. Next #122 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-5018ded-2787`: tip `work/lynx-native` @ `5018ded1c818d06d07c6a60667e36c16004f3d44` (PR #162 / Next #121 MERGED) + published prerelease APK `lynx-v2-debug-5018ded` (was stale `lynx-v2-debug-3a87f8b` / tip `3a87f8bcd0e575a747909a6042ae881e8666098c`; tip delta `3a87f8b`→`5018ded` is docs-only). Lynx Mobile CI SUCCESS on tip `5018ded` (run 35320424832). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-5018ded`); host-only unchanged. **MERGED** as PR #163 — tip advanced to `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af` / published APK `lynx-v2-debug-c1e2f5c` (see Next #123).

123. ~~**Docs tip APK/SHA honesty post-#122**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #122 **MERGED** as PR #163. Next #123 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-c1e2f5c-9ae1`: tip `work/lynx-native` @ `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af` (PR #163 / Next #122 MERGED) + published prerelease APK `lynx-v2-debug-c1e2f5c` (was stale `lynx-v2-debug-5018ded` / tip `5018ded1c818d06d07c6a60667e36c16004f3d44`; tip delta `5018ded`→`c1e2f5c` is docs-only). Lynx Mobile CI SUCCESS on tip `c1e2f5c` (run 35333814514). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c1e2f5c`); host-only unchanged. **MERGED** as PR #164 — tip advanced to `74d2d29ac5311a4b72f52474f73027f729ab7944` / published APK `lynx-v2-debug-74d2d29` (see Next #124).

124. ~~**Docs tip APK/SHA honesty post-#123**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #123 **MERGED** as PR #164. Next #124 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-74d2d29-d266`: tip `work/lynx-native` @ `74d2d29ac5311a4b72f52474f73027f729ab7944` (PR #164 / Next #123 MERGED) + published prerelease APK `lynx-v2-debug-74d2d29` (was stale `lynx-v2-debug-c1e2f5c` / tip `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af`; tip delta `c1e2f5c`→`74d2d29` is docs-only). Lynx Mobile CI SUCCESS on tip `74d2d29` (run 35335482752). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-74d2d29`); host-only unchanged. **MERGED** as PR #165 — tip advanced to `cc15c1aefdd41e909ed3f55aa1171e067be84adf` / published APK `lynx-v2-debug-cc15c1a` (see Next #125).

125. ~~**Docs tip APK/SHA honesty post-#124**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #124 **MERGED** as PR #165. Next #125 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-cc15c1a-8aec`: tip `work/lynx-native` @ `cc15c1aefdd41e909ed3f55aa1171e067be84adf` (PR #165 / Next #124 MERGED) + published prerelease APK `lynx-v2-debug-cc15c1a` (was stale `lynx-v2-debug-74d2d29` / tip `74d2d29ac5311a4b72f52474f73027f729ab7944`; tip delta `74d2d29`→`cc15c1a` is docs-only). Lynx Mobile CI SUCCESS on tip `cc15c1a` (run 35353222951). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-cc15c1a`); host-only unchanged. **MERGED** as PR #166 — tip advanced to `592192f993ec3b429f49782c07d3c9194eea5127` / published APK `lynx-v2-debug-592192f` (see Next #126).

126. ~~**Docs tip APK/SHA honesty post-#125**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #125 **MERGED** as PR #166. Next #126 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-592192f-0bda`: tip `work/lynx-native` @ `592192f993ec3b429f49782c07d3c9194eea5127` (PR #166 / Next #125 MERGED) + published prerelease APK `lynx-v2-debug-592192f` (was stale `lynx-v2-debug-cc15c1a` / tip `cc15c1aefdd41e909ed3f55aa1171e067be84adf`; tip delta `cc15c1a`→`592192f` is docs-only). Lynx Mobile CI SUCCESS on tip `592192f` (run 35371422190). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-592192f`); host-only unchanged. **MERGED** as PR #167 — tip advanced to `6d5ef04efabf29704c60e2392f10a94c226bcb5c` / published APK `lynx-v2-debug-6d5ef04` (see Next #127).

127. ~~**Docs tip APK/SHA honesty post-#126**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #126 **MERGED** as PR #167. Next #127 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-6d5ef04-ca08`: tip `work/lynx-native` @ `6d5ef04efabf29704c60e2392f10a94c226bcb5c` (PR #167 / Next #126 MERGED) + published prerelease APK `lynx-v2-debug-6d5ef04` (was stale `lynx-v2-debug-592192f` / tip `592192f993ec3b429f49782c07d3c9194eea5127`; tip delta `592192f`→`6d5ef04` is docs-only). Lynx Mobile CI SUCCESS on tip `6d5ef04` (run 35388844201). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6d5ef04`); host-only unchanged. **MERGED** as PR #168 — tip advanced to `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a` / published APK `lynx-v2-debug-6c1f6d4` (see Next #128).

128. ~~**Docs tip APK/SHA honesty post-#127**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #127 **MERGED** as PR #168. Next #128 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-6c1f6d4-323f`: tip `work/lynx-native` @ `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a` (PR #168 / Next #127 MERGED) + published prerelease APK `lynx-v2-debug-6c1f6d4` (was stale `lynx-v2-debug-6d5ef04` / tip `6d5ef04efabf29704c60e2392f10a94c226bcb5c`; tip delta `6d5ef04`→`6c1f6d4` is docs-only). Lynx Mobile CI SUCCESS on tip `6c1f6d4` (run 35402341644). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6c1f6d4`); host-only unchanged. **MERGED** as PR #169 — tip advanced to `3695bd3f12086184a9c778201d232e92bc281f12` / published APK `lynx-v2-debug-3695bd3` (see Next #129).

129. ~~**Docs tip APK/SHA honesty post-#128**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #128 **MERGED** as PR #169. Next #129 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-3695bd3-b570`: tip `work/lynx-native` @ `3695bd3f12086184a9c778201d232e92bc281f12` (PR #169 / Next #128 MERGED) + published prerelease APK `lynx-v2-debug-3695bd3` (was stale `lynx-v2-debug-6c1f6d4` / tip `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a`; tip delta `6c1f6d4`→`3695bd3` is docs-only). Lynx Mobile CI SUCCESS on tip `3695bd3` (run 35412917473). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3695bd3`); host-only unchanged. **MERGED** as PR #170 — tip advanced to `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0` / published APK `lynx-v2-debug-b23a0a4` (see Next #130).

130. ~~**Docs tip APK/SHA honesty post-#129**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #129 **MERGED** as PR #170. Next #130 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-b23a0a4-aa04`: tip `work/lynx-native` @ `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0` (PR #170 / Next #129 MERGED) + published prerelease APK `lynx-v2-debug-b23a0a4` (was stale `lynx-v2-debug-3695bd3` / tip `3695bd3f12086184a9c778201d232e92bc281f12`; tip delta `3695bd3`→`b23a0a4` is docs-only). Lynx Mobile CI SUCCESS on tip `b23a0a4` (run 35421462192). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b23a0a4`); host-only unchanged. **MERGED** as PR #171 — tip advanced to `a11eb83c3136388ca951194466d949ec3c554956` / published APK `lynx-v2-debug-a11eb83` (see Next #131).

131. ~~**Docs tip APK/SHA honesty post-#130**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #130 **MERGED** as PR #171. Next #131 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-a11eb83-abb3`: tip `work/lynx-native` @ `a11eb83c3136388ca951194466d949ec3c554956` (PR #171 / Next #130 MERGED) + published prerelease APK `lynx-v2-debug-a11eb83` (was stale `lynx-v2-debug-b23a0a4` / tip `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0`; tip delta `b23a0a4`→`a11eb83` is docs-only). Lynx Mobile CI SUCCESS on tip `a11eb83` (run 35429567861). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a11eb83`); host-only unchanged. **MERGED** as PR #172 — tip advanced to `d8c0ed2889958ea6a0882d6a887501c4d3ef7986` / published APK `lynx-v2-debug-d8c0ed2` (see Next #132).

132. ~~**Docs tip APK/SHA honesty post-#131**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #131 **MERGED** as PR #172. Next #132 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-d8c0ed2-898e`: tip `work/lynx-native` @ `d8c0ed2889958ea6a0882d6a887501c4d3ef7986` (PR #172 / Next #131 MERGED) + published prerelease APK `lynx-v2-debug-d8c0ed2` (was stale `lynx-v2-debug-a11eb83` / tip `a11eb83c3136388ca951194466d949ec3c554956`; tip delta `a11eb83`→`d8c0ed2` is docs-only). Lynx Mobile CI SUCCESS on tip `d8c0ed2` (run 35437528510). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d8c0ed2`); host-only unchanged. **MERGED** as PR #173 — tip advanced to `59b8a88edbb7d1cd15b46cb01830f0180e0b073e` / published APK `lynx-v2-debug-59b8a88` (see Next #133).

133. ~~**Docs tip APK/SHA honesty post-#132**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #132 **MERGED** as PR #173. Next #133 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-59b8a88-a009`: tip `work/lynx-native` @ `59b8a88edbb7d1cd15b46cb01830f0180e0b073e` (PR #173 / Next #132 MERGED) + published prerelease APK `lynx-v2-debug-59b8a88` (was stale `lynx-v2-debug-d8c0ed2` / tip `d8c0ed2889958ea6a0882d6a887501c4d3ef7986`; tip delta `d8c0ed2`→`59b8a88` is docs-only). Lynx Mobile CI SUCCESS on tip `59b8a88` (run 35445932721). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-59b8a88`); host-only unchanged. **MERGED** as PR #174 — tip advanced to `df879926fd2c8d4ad18d26bdee033f9345cc5c47` / published APK `lynx-v2-debug-df87992` (see Next #134).

134. ~~**Docs tip APK/SHA honesty post-#133**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #133 **MERGED** as PR #174. Next #134 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-df87992-fa5f`: tip `work/lynx-native` @ `df879926fd2c8d4ad18d26bdee033f9345cc5c47` (PR #174 / Next #133 MERGED) + published prerelease APK `lynx-v2-debug-df87992` (was stale `lynx-v2-debug-59b8a88` / tip `59b8a88edbb7d1cd15b46cb01830f0180e0b073e`; tip delta `59b8a88`→`df87992` is docs-only). Lynx Mobile CI SUCCESS on tip `df87992` (run 35455446737). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-df87992`); host-only unchanged. **MERGED** as PR #175 — tip advanced to `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f` / published APK `lynx-v2-debug-b7db06b` (see Next #135).

135. ~~**Docs tip APK/SHA honesty post-#134**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #134 **MERGED** as PR #175. Next #135 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-b7db06b-c05d`: tip `work/lynx-native` @ `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f` (PR #175 / Next #134 MERGED) + published prerelease APK `lynx-v2-debug-b7db06b` (was stale `lynx-v2-debug-df87992` / tip `df879926fd2c8d4ad18d26bdee033f9345cc5c47`; tip delta `df87992`→`b7db06b` is docs-only). Lynx Mobile CI SUCCESS on tip `b7db06b` (run 35464341914). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b7db06b`); host-only unchanged. **MERGED** as PR #176 — tip advanced to `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8` / published APK `lynx-v2-debug-3becdfa` (see Next #136).

136. ~~**Docs tip APK/SHA honesty post-#135**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #135 **MERGED** as PR #176. Next #136 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-3becdfa-7678`: tip `work/lynx-native` @ `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8` (PR #176 / Next #135 MERGED) + published prerelease APK `lynx-v2-debug-3becdfa` (was stale `lynx-v2-debug-b7db06b` / tip `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f`; tip delta `b7db06b`→`3becdfa` is docs-only). Lynx Mobile CI SUCCESS on tip `3becdfa` (run 35473297750). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3becdfa`); host-only unchanged. **MERGED** as PR #177 — tip advanced to `165f27c8a55edf929de37de42255c859eef2d34c` / published APK `lynx-v2-debug-165f27c` (see Next #137).

137. ~~**Docs tip APK/SHA honesty post-#136**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #136 **MERGED** as PR #177. Next #137 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-165f27c-ea47`: tip `work/lynx-native` @ `165f27c8a55edf929de37de42255c859eef2d34c` (PR #177 / Next #136 MERGED) + published prerelease APK `lynx-v2-debug-165f27c` (was stale `lynx-v2-debug-3becdfa` / tip `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8`; tip delta `3becdfa`→`165f27c` is docs-only). Lynx Mobile CI SUCCESS on tip `165f27c` (run 35481508022). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-165f27c`); host-only unchanged. **MERGED** as PR #178 — tip advanced to `ed506522f7bab857424af81a703520558f1fe359` / published APK `lynx-v2-debug-ed50652` (see Next #138).

138. ~~**Docs tip APK/SHA honesty post-#137**~~ — Cap phone skim (MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome vs `packages/lynx`) found **no** new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host). Incremental leftovers (not new product API wires): Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search filter is wired (`filterLynxSettingsPages`); search-field chrome vs `LynxInput` is leftover, not a missing route. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #137 **MERGED** as PR #178. Next #138 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-ed50652-743f`: tip `work/lynx-native` @ `ed506522f7bab857424af81a703520558f1fe359` (PR #178 / Next #137 MERGED) + published prerelease APK `lynx-v2-debug-ed50652` (was stale `lynx-v2-debug-165f27c` / tip `165f27c8a55edf929de37de42255c859eef2d34c`; tip delta `165f27c`→`ed50652` is docs-only). Lynx Mobile CI SUCCESS on tip `ed50652` (run 35489088418). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ed50652`); host-only unchanged. **MERGED** as PR #180 — tip advanced to `42bb986425ea447b164a779d8087f271630d75a4` / published APK still `lynx-v2-debug-ed50652` (see Next #139).

139. ~~**Cap Settings search LynxInput**~~ — 代码接上 in `cursor/lynx-settings-search-input-3e1b`. Cap phone Settings uses a real `<input>` (`settingsSearchQuery` + `onChange`). Lynx `SettingsSearchField` is now an editable `LynxInput` (`value` + `bindinput` → `onChange` / `setSearchQuery`, placeholder `lynx.settings.search.placeholder`) instead of display-only `LynxText` + height-0 stub. Filter path unchanged: `filterLynxSettingsPages(searchQuery)` over the 22-slug catalog. Host IME chrome polish out of scope. Docs honesty at write: base tip `work/lynx-native` @ `42bb986425ea447b164a779d8087f271630d75a4` (PR #180 / Next #138 MERGED) + published-at-merge APK `lynx-v2-debug-ed50652` (release existed for tip `ed506522f`; do NOT invent `lynx-v2-debug-42bb986` as that head's APK). Product **NOT DONE** / 三关未齐 / not EXHAUSTED; Settings search stays 代码接上, not 三关 landed / not 真机过; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee); host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/@dnd-kit/MobileWindowMotion/iPad/Cap toast/bulk). **MERGED** as PR #181 — tip advanced to `de4ff4ea23f3446df77f8c1764e51507aabd9ce7` / published APK `lynx-v2-debug-de4ff4e` (see Next #140).

140. ~~**Docs tip APK/SHA honesty post-#139**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #139 **MERGED** as PR #181. Next #140 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-de4ff4e-a175`: tip `work/lynx-native` @ `de4ff4ea23f3446df77f8c1764e51507aabd9ce7` (PR #181 / Next #139 MERGED) + published prerelease APK `lynx-v2-debug-de4ff4e` (was stale `lynx-v2-debug-ed50652` / tip bases `ed506522f7bab857424af81a703520558f1fe359` / Next #139 base `42bb986425ea447b164a779d8087f271630d75a4` / published-at-merge APK for #139 head). Tip delta `42bb986`→`de4ff4ea23` includes product: SettingsTab LynxInput + SettingsTab.test.ts + gap-board/acceptance updates for Next #139 — then tip advanced to `de4ff4ea23` and Mobile CI published `lynx-v2-debug-de4ff4e`. Lynx Mobile CI SUCCESS on tip `de4ff4e` (run 35497211638). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-de4ff4e`); host-only unchanged. **MERGED** as PR #182 — tip advanced to `fa087e5bccd27bad126a5aedfab4255ce4553f75` / published APK `lynx-v2-debug-fa087e5` (see Next #141).

141. ~~**Docs tip APK/SHA honesty post-#140**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #140 **MERGED** as PR #182. Next #141 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-fa087e5-977b`: tip `work/lynx-native` @ `fa087e5bccd27bad126a5aedfab4255ce4553f75` (PR #182 / Next #140 MERGED) + published prerelease APK `lynx-v2-debug-fa087e5` (was stale `lynx-v2-debug-de4ff4e` / tip `de4ff4ea23f3446df77f8c1764e51507aabd9ce7`; tip delta `de4ff4e`→`fa087e5` is docs-only). Lynx Mobile CI SUCCESS on tip `fa087e5` (run 35505471290). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-fa087e5`); host-only unchanged. **MERGED** as PR #183 — tip advanced to `5f98d1c5427a064f60cddf743e88ba6ad454f1da` / published APK `lynx-v2-debug-5f98d1c` (see Next #142).

142. ~~**Docs tip APK/SHA honesty post-#141**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #141 **MERGED** as PR #183. Next #142 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-5f98d1c-0e2e`: tip `work/lynx-native` @ `5f98d1c5427a064f60cddf743e88ba6ad454f1da` (PR #183 / Next #141 MERGED) + published prerelease APK `lynx-v2-debug-5f98d1c` (was stale `lynx-v2-debug-fa087e5` / tip `fa087e5bccd27bad126a5aedfab4255ce4553f75`; tip delta `fa087e5`→`5f98d1c` is docs-only). Lynx Mobile CI SUCCESS on tip `5f98d1c` (run 35513768378). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-5f98d1c`); host-only unchanged. **MERGED** as PR #184 — tip advanced to `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e` / published APK `lynx-v2-debug-9fc7723` (see Next #143).

143. ~~**Docs tip APK/SHA honesty post-#142**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #142 **MERGED** as PR #184. Next #143 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-9fc7723-757d`: tip `work/lynx-native` @ `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e` (PR #184 / Next #142 MERGED) + published prerelease APK `lynx-v2-debug-9fc7723` (was stale `lynx-v2-debug-5f98d1c` / tip `5f98d1c5427a064f60cddf743e88ba6ad454f1da`; tip delta `5f98d1c`→`9fc7723` is docs-only). Lynx Mobile CI SUCCESS on tip `9fc7723` (run 35523180671). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-9fc7723`); host-only unchanged. **MERGED** as PR #185 — tip advanced to `84107429dc9cd9e98d9de82807aa509d2bf8a496` / published APK `lynx-v2-debug-8410742` (see Next #144).

144. ~~**Docs tip APK/SHA honesty post-#143**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #143 **MERGED** as PR #185. Next #144 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-8410742-27a6`: tip `work/lynx-native` @ `84107429dc9cd9e98d9de82807aa509d2bf8a496` (PR #185 / Next #143 MERGED) + published prerelease APK `lynx-v2-debug-8410742` (was stale `lynx-v2-debug-9fc7723` / tip `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e`; tip delta `9fc7723`→`8410742` is docs-only). Lynx Mobile CI SUCCESS on tip `8410742` (run 35532791147). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-8410742`); host-only unchanged. **MERGED** as PR #186 — tip advanced to `c3cb78a79bf80d0a90d448bb7133adc5869084d6` / published APK `lynx-v2-debug-c3cb78a` (see Next #145).

145. ~~**Docs tip APK/SHA honesty post-#144**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #144 **MERGED** as PR #186. Next #145 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-c3cb78a-6321`: tip `work/lynx-native` @ `c3cb78a79bf80d0a90d448bb7133adc5869084d6` (PR #186 / Next #144 MERGED) + published prerelease APK `lynx-v2-debug-c3cb78a` (was stale `lynx-v2-debug-8410742` / tip `84107429dc9cd9e98d9de82807aa509d2bf8a496`; tip delta `8410742`→`c3cb78a` is docs-only). Lynx Mobile CI SUCCESS on tip `c3cb78a` (run 35542179230). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c3cb78a`); host-only unchanged. **MERGED** as PR #187 — tip advanced to `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5` / published APK `lynx-v2-debug-7227ddc` (see Next #146).

146. ~~**Docs tip APK/SHA honesty post-#145**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #145 **MERGED** as PR #187. Next #146 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-7227ddc-4eb3`: tip `work/lynx-native` @ `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5` (PR #187 / Next #145 MERGED) + published prerelease APK `lynx-v2-debug-7227ddc` (was stale `lynx-v2-debug-c3cb78a` / tip `c3cb78a79bf80d0a90d448bb7133adc5869084d6`; tip delta `c3cb78a`→`7227ddc` is docs-only). Lynx Mobile CI SUCCESS on tip `7227ddc` (run 35551435386). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-7227ddc`); host-only unchanged. **MERGED** as PR #188 — tip advanced to `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d` / published APK `lynx-v2-debug-6b78c3d` (see Next #147).

147. ~~**Docs tip APK/SHA honesty post-#146**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #146 **MERGED** as PR #188. Next #147 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-6b78c3d-7dfd`: tip `work/lynx-native` @ `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d` (PR #188 / Next #146 MERGED) + published prerelease APK `lynx-v2-debug-6b78c3d` (was stale `lynx-v2-debug-7227ddc` / tip `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5`; tip delta `7227ddc`→`6b78c3d` is docs-only). Lynx Mobile CI SUCCESS on tip `6b78c3d` (run 35561042584). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6b78c3d`); host-only unchanged. **MERGED** as PR #189 — tip advanced to `a46ac25c3fe489f5727362912740a4622976414c` / published APK `lynx-v2-debug-a46ac25` (see Next #148).

148. ~~**Docs tip APK/SHA honesty post-#147**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #147 **MERGED** as PR #189. Next #148 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-a46ac25-3b5b`: tip `work/lynx-native` @ `a46ac25c3fe489f5727362912740a4622976414c` (PR #189 / Next #147 MERGED) + published prerelease APK `lynx-v2-debug-a46ac25` (was stale `lynx-v2-debug-6b78c3d` / tip `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d`; tip delta `6b78c3d`→`a46ac25` is docs-only). Lynx Mobile CI SUCCESS on tip `a46ac25` (run 35574289217). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a46ac25`); host-only unchanged. **MERGED** as PR #190 — tip advanced to `38b31757b7460ab6781edb71acc9e933fab30e0c` / published APK `lynx-v2-debug-38b3175` (see Next #149).

149. ~~**Docs tip APK/SHA honesty post-#148**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #148 **MERGED** as PR #190. Next #149 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-38b3175-4ca9`: tip `work/lynx-native` @ `38b31757b7460ab6781edb71acc9e933fab30e0c` (PR #190 / Next #148 MERGED) + published prerelease APK `lynx-v2-debug-38b3175` (was stale `lynx-v2-debug-a46ac25` / tip `a46ac25c3fe489f5727362912740a4622976414c`; tip delta `a46ac25`→`38b3175` is docs-only). Lynx Mobile CI SUCCESS on tip `38b3175` (run 35588189358). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-38b3175`); host-only unchanged. **MERGED** as PR #191 — tip advanced to `daca88c832f3df0d1a59bf5ca3eaa828f1885656` / published APK `lynx-v2-debug-daca88c` (see Next #150).

150. ~~**Docs tip APK/SHA honesty post-#149**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #149 **MERGED** as PR #191. Next #150 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-daca88c-80a9`: tip `work/lynx-native` @ `daca88c832f3df0d1a59bf5ca3eaa828f1885656` (PR #191 / Next #149 MERGED) + published prerelease APK `lynx-v2-debug-daca88c` (was stale `lynx-v2-debug-38b3175` / tip `38b31757b7460ab6781edb71acc9e933fab30e0c`; tip delta `38b3175`→`daca88c` is docs-only). Lynx Mobile CI SUCCESS on tip `daca88c` (run 35589404227). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-daca88c`); host-only unchanged. **MERGED** as PR #192 — tip advanced to `dcb67d6075b670ce24fb6efcf19384ae8adbc07e` / published APK `lynx-v2-debug-dcb67d6` (see Next #151).

151. ~~**Docs tip APK/SHA honesty post-#150**~~ — Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #150 **MERGED** as PR #192. Next #151 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-dcb67d6-7620`: tip `work/lynx-native` @ `dcb67d6075b670ce24fb6efcf19384ae8adbc07e` (PR #192 / Next #150 MERGED) + published prerelease APK `lynx-v2-debug-dcb67d6` (was stale `lynx-v2-debug-daca88c` / tip `daca88c832f3df0d1a59bf5ca3eaa828f1885656`; tip delta `daca88c`→`dcb67d6` is docs-only). Lynx Mobile CI SUCCESS on tip `dcb67d6` (run 35610772314). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-dcb67d6`); host-only unchanged. **MERGED** as PR #193 — tip advanced to `309af29373d68d7d4ea3757af9adf4896946490f` / published APK `lynx-v2-debug-309af29` (see Next #152).

152. ~~**Docs tip APK/SHA honesty post-#151**~~ — Cap skim pending / **NO_NEW** expected from prior skim + docs-only tip delta. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #151 **MERGED** as PR #193. Next #152 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-309af29-35bd`: tip `work/lynx-native` @ `309af29373d68d7d4ea3757af9adf4896946490f` (PR #193 / Next #151 MERGED) + published prerelease APK `lynx-v2-debug-309af29` (was stale `lynx-v2-debug-dcb67d6` / tip `dcb67d6075b670ce24fb6efcf19384ae8adbc07e`; tip delta `dcb67d6`→`309af29` is docs-only). Lynx Mobile CI SUCCESS on tip `309af29` (run 35629501281). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-309af29`); host-only unchanged. **MERGED** as PR #194 — tip advanced to `c3a062a134a9796041ea605eb2e1acc66a2b289d` / published APK `lynx-v2-debug-c3a062a` (see Next #153).

153. **Docs tip APK/SHA honesty post-#152** — Cap skim pending / **NO_NEW** expected from prior skim + docs-only tip delta. Incremental leftovers unchanged: Scheduled history first-page list already 代码接上; Cap load-more/`nextCursor` + per-task filter stay thin polish. Settings search field is 代码接上 (`LynxInput` + `bindinput`, Next #139); not a missing route / not 三关 landed. Session-row question/permission badges and Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) and host-only (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) unchanged. Next #152 **MERGED** as PR #194. Next #153 = **docs honesty only** in `cursor/lynx-docs-tip-apk-honesty-c3a062a-0299`: tip `work/lynx-native` @ `c3a062a134a9796041ea605eb2e1acc66a2b289d` (PR #194 / Next #152 MERGED) + published prerelease APK `lynx-v2-debug-c3a062a` (was stale `lynx-v2-debug-309af29` / tip `309af29373d68d7d4ea3757af9adf4896946490f`; tip delta `309af29`→`c3a062a` is docs-only). Lynx Mobile CI SUCCESS on tip `c3a062a` (run 35646713115). Do NOT invent a newer APK than published. Product **NOT DONE** / 三关未齐 / not EXHAUSTED; 真机残差 unchanged (Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c3a062a`); host-only unchanged.

Do not invent ASR / Bonjour / Capgo / TanStack 1.18. Share / Live Activity / widgets stay later.

---

## 真机残差

**Open (2026-09-10):** Connect welcome **card chrome** on Android — cssVar→hex (PR #92) confirmed: cream page + primary orange work on device/emu. Residual: cream `#fffdf4` ≈ elevated `#fbfaf2`, so done-phase cards still look like a flat off-white stack (softemu `/workspace/lynx-emu-connect-welcome.png`). Mitigated in `cursor/lynx-connect-welcome-card-chrome` (1px `interactive.selection` border + solid + radius 12 + optional soft boxShadow; elevated fill kept) — **merged** tip `eca4a0305` / published APK `lynx-v2-debug-eca4a03` (PR #94); tip advanced through `489336a34` / `lynx-v2-debug-489336a` (PR #95 docs honesty), `4d871bad0` / `lynx-v2-debug-4d871ba` (PR #96 / Next #57), `53ea9cdee4` / published APK `lynx-v2-debug-53ea9cd` (PR #97 / Next #58 MERGED; tip APK honesty in Next #59), and `5ae491f6a3` / published APK `lynx-v2-debug-5ae491f` (PR #98 / Next #59 MERGED; tip APK honesty in Next #60 → tip `c8a72d6025` via PR #99), then Cap Changes stageAll/unstageAll/revertAll tip `773dd989b9` / published APK `lynx-v2-debug-773dd98` (PR #100 / Next #61 MERGED; tip APK honesty in Next #62 → tip `dda945b12` via PR #101), then tip APK honesty post-#62 tip `dda945b12` / published APK `lynx-v2-debug-dda945b` (Next #63 → tip `2384ea6c95` via PR #102), then tip APK honesty post-#63 tip `2384ea6c95` / published APK `lynx-v2-debug-2384ea6` (Next #64 → tip `3f15ea4196` via PR #103), then tip APK honesty post-#64 tip `3f15ea4196` / published APK `lynx-v2-debug-3f15ea4` (Next #65 → tip `8ba89898a31` via PR #104), then tip APK honesty post-#65 tip `8ba89898a31` / published APK `lynx-v2-debug-8ba8989` (Next #66 → tip `538c31239fa4` via PR #105), then tip APK honesty post-#66 tip `538c31239fa4` / published APK `lynx-v2-debug-538c312` (Next #67 → tip `cd074ee2b2b3` via PR #106), then tip APK honesty post-#67 tip `cd074ee2b2b3` / published APK `lynx-v2-debug-cd074ee` (Next #68 → tip `50fa2626e946` via PR #107), then tip APK honesty post-#68 tip `50fa2626e946` / published APK `lynx-v2-debug-50fa262` (Next #69 → tip `fb9a969c7c0d` via PR #108), then tip APK honesty post-#69 tip `fb9a969c7c0d` / published APK `lynx-v2-debug-fb9a969` (Next #70 → tip `68b1919a566c` via PR #109), then tip APK honesty post-#70 tip `68b1919a566c` / published APK `lynx-v2-debug-68b1919` (Next #71 → tip `3b2ae4c1283700794d3b5124605cee914c4dc545` via PR #110), then tip APK honesty post-#71 tip `3b2ae4c1283700794d3b5124605cee914c4dc545` / published APK `lynx-v2-debug-3b2ae4c` (Next #72 → tip `ea6349303` via PR #111), then Cap Scheduled Run now + Delete tip `0a56dbedd1635ebe3cc31ed40fa5f428bc677bfc` / published APK `lynx-v2-debug-0a56dbe` (PR #112 / Next #73 MERGED; tip APK honesty in Next #74 → tip `b7986841cc7341800880febf6958299e65d50592` via PR #113), then tip APK honesty post-#74 tip `b7986841cc7341800880febf6958299e65d50592` / published APK `lynx-v2-debug-b798684` (Next #75 → tip `51ab13b832164fac30956bb1297e2f1d93ed430b` via PR #114 + PR #115 auto-connect timeout), then tip APK honesty post-#75 tip `51ab13b832164fac30956bb1297e2f1d93ed430b` / published APK `lynx-v2-debug-51ab13b` (Next #76 → tip `896bd125704d86d11089ee138ccdd841a8a21a94` via PR #116), then tip APK honesty post-#76 tip `896bd125704d86d11089ee138ccdd841a8a21a94` / published APK `lynx-v2-debug-896bd12` (Next #77 → tip `51870e1d175114ce0d4ad820db41311bd702d083` via PR #117), then tip APK honesty post-#77 tip `51870e1d175114ce0d4ad820db41311bd702d083` / published APK `lynx-v2-debug-51870e1` (Next #78), then tip APK honesty post-#78 tip `46435859225186b343d439d0744385f651c1d9a2` / published APK `lynx-v2-debug-4643585` (Next #79 → tip `cf705f5d1458824ac1a8270d498cd83b3e44ff94` via PR #119), then tip APK honesty post-#79 tip `cf705f5d1458824ac1a8270d498cd83b3e44ff94` / published APK `lynx-v2-debug-cf705f5` (Next #80 → tip `267ba627842656370ae9b1295c4b59ff9c505c38` via PR #120), then tip APK honesty post-#80 tip `267ba627842656370ae9b1295c4b59ff9c505c38` / published APK `lynx-v2-debug-267ba62` (Next #81 → tip `0b47a71f14d8a1945dae14e333ba695451707950` via PR #121), then tip APK honesty post-#81 tip `0b47a71f14d8a1945dae14e333ba695451707950` / published APK `lynx-v2-debug-0b47a71` (Next #82 → tip `5287db7438ae51bad0033ee6614c83fee7ad6f2a` via PR #122), then tip APK honesty post-#82 tip `5287db7438ae51bad0033ee6614c83fee7ad6f2a` / published APK `lynx-v2-debug-5287db7` (Next #83 → tip `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f` via PR #123), then tip APK honesty post-#83 tip `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f` / published APK `lynx-v2-debug-3d0d768` (Next #84 → tip `607eaf6dfc726f7d18fd6eac2b44542e39365b09` via PR #124), then tip APK honesty post-#84 tip `607eaf6dfc726f7d18fd6eac2b44542e39365b09` / published APK `lynx-v2-debug-607eaf6` (Next #85 → tip `edaec0d9bbab4f132a51e4fde3259dd994b49369` via PR #125), then tip APK honesty post-#85 tip `edaec0d9bbab4f132a51e4fde3259dd994b49369` / published APK `lynx-v2-debug-edaec0d` (Next #86 → tip `dbad742588da5f7c575d23c5269b3317dd1cd1ce` via PR #126), then tip APK honesty post-#86 tip `dbad742588da5f7c575d23c5269b3317dd1cd1ce` / published APK `lynx-v2-debug-dbad742` (Next #87 → tip `4d16bf8070865883c79b0e13a3c47e63d573311c` via PR #127), then tip APK honesty post-#87 tip `4d16bf8070865883c79b0e13a3c47e63d573311c` / published APK `lynx-v2-debug-4d16bf8` (Next #88 → tip `8ec03842faed8caa6c255a237e617ef1ddf2e89f` via PR #128), then tip APK honesty post-#88 tip `8ec03842faed8caa6c255a237e617ef1ddf2e89f` / published APK `lynx-v2-debug-8ec0384` (Next #89 → tip `c451d696bded3b8793d5a444b9ba39f43cff8dac` via PR #129), then Cap Settings archived-sessions slug + page body (Next #90 MERGED as PR #130; tip `4cc762cd2`; published APK later `lynx-v2-debug-4cc762c`), then Cap Assistant unread + mark-all-read (Next #91 MERGED as PR #131; tip `3fda6eb3f2`; published APK stays `lynx-v2-debug-4cc762c` — no `lynx-v2-debug-3fda6eb` at write time; Mobile CI may later publish for newer tip), then tip APK honesty post-#91 tip `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65` / published APK `lynx-v2-debug-4cc762c` (Next #92 MERGED as PR #132), then tip APK honesty post-#92 tip `63da3d63aa354eb66677f6ff57d5dd546ddca24b` / published APK `lynx-v2-debug-63da3d6` (Next #93 MERGED as PR #133), then tip APK honesty post-#93 tip `50af885b81849d92bed38116930abc3ddbbf88ef` / published APK `lynx-v2-debug-50af885` (Next #94 MERGED as PR #134), then tip APK honesty post-#94 tip `4520eb00be8bd1cc8faea2cca151d3d55ae39b91` / published APK still `lynx-v2-debug-50af885` (Next #95 MERGED as PR #136), then PR #135 pinned setup-android packages (`tools` removed upstream → `packages:platform-tools`) and Mobile CI republished `lynx-v2-debug-ee82d53` on tip `ee82d53a736a321b6ad6fd4573b03575def90eeb` (Next #96 MERGED as PR #137), then tip APK honesty post-#96 tip `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464` / published APK `lynx-v2-debug-d10c92d` (Next #97 MERGED as PR #138), then tip APK honesty post-#97 tip `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8` / published APK `lynx-v2-debug-a92b319` (Next #98 MERGED as PR #139), then tip APK honesty post-#98 tip `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2` / published APK `lynx-v2-debug-f1cf09e` (Next #99 MERGED as PR #140), then tip APK honesty post-#99 tip `41d632abebf2c62088d46371bb5761c73925dc7b` / published APK `lynx-v2-debug-41d632a` (Next #100 MERGED as PR #141), then tip APK honesty post-#100 tip `32aed54c6f1585f6da2a35c82ac16f78db92933e` / published APK `lynx-v2-debug-32aed54` (Next #101 MERGED as PR #142), then tip APK honesty post-#101 tip `105884e5067dd5ba26bb49c2957bb255deea92cd` / published APK `lynx-v2-debug-105884e` (Next #102 MERGED as PR #143), then tip APK honesty post-#102 tip `ac8b4c99c5e821ac435bea83daec65eb8f7e7171` / published APK `lynx-v2-debug-ac8b4c9` (Next #103 MERGED as PR #144), then tip APK honesty post-#103 tip `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76` / published APK `lynx-v2-debug-bb7aad6` (Next #104 MERGED as PR #145), then tip APK honesty post-#104 tip `14211c02c298ebfcf4b0e2d1720ee5c9635358bf` / published APK `lynx-v2-debug-14211c0` (Next #105 MERGED as PR #146), then tip APK honesty post-#105 tip `a9f6605301df86cbe2b8b7ad124aaac02142a834` / published APK `lynx-v2-debug-a9f6605` (Next #106 MERGED as PR #147), then tip APK honesty post-#106 tip `7e4af21a4f651fdf051905572188456fef28e72c` / published APK `lynx-v2-debug-7e4af21` (Next #107 MERGED as PR #148), then tip APK honesty post-#107 tip `22a4bc746ef851bc05d0710662e65d298fee540f` / published APK `lynx-v2-debug-22a4bc7` (Next #108 MERGED as PR #149), then tip APK honesty post-#108 tip `d256387d51cd4f166f86320d71b31804fe104243` / published APK `lynx-v2-debug-d256387` (Next #109 MERGED as PR #150), then tip APK honesty post-#109 tip `50934d8cf44a463310284d55a815a1d7426fb78b` / published APK `lynx-v2-debug-50934d8` (Next #110 MERGED as PR #151), then tip APK honesty post-#110 tip `c510559b425dcd0a8e06e5961e849570e744f014` / published APK `lynx-v2-debug-c510559` (Next #111 MERGED as PR #152), then tip APK honesty post-#111 tip `b32e74c05ff9041d787442cc3ab4087d2f15e87e` / published APK `lynx-v2-debug-b32e74c` (Next #112 MERGED as PR #153), then tip APK honesty post-#112 tip `aa08bbf7ed68ce46503b21db444c4aafb1787448` / published APK `lynx-v2-debug-aa08bbf` (Next #113 MERGED as PR #154), then tip APK honesty post-#113 tip `1142c4e2b0863c45ee25bc2452ea03078220155e` / published APK `lynx-v2-debug-1142c4e` (Next #114 MERGED as PR #155), then tip APK honesty post-#114 tip `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18` / published APK `lynx-v2-debug-a0dd0a9` (Next #115 MERGED as PR #156), then tip APK honesty post-#115 tip `04325ba8dfcd8700d4790670a03e2570d4f4acc4` / published APK `lynx-v2-debug-04325ba` (Next #116 MERGED as PR #157), then tip APK honesty post-#116 tip `4b4fbcc0f0e05eae50c12177f8486682a544b381` / published APK `lynx-v2-debug-4b4fbcc` (Next #117 MERGED as PR #158), then tip APK honesty post-#117 tip `554e70e18dc18fe509ae02fdcdd43faead587ed1` / published APK `lynx-v2-debug-554e70e` (Next #118 MERGED as PR #159), then tip APK honesty post-#118 tip `6215ead5164c086d720629165ab31aac3c3b15a1` / published APK `lynx-v2-debug-6215ead` (Next #119 MERGED as PR #160), then tip APK honesty post-#119 tip `06d4ac4138eba038259490d857be37ba59c8daad` / published APK `lynx-v2-debug-06d4ac4` (Next #120 MERGED as PR #161), then tip APK honesty post-#120 tip `3a87f8bcd0e575a747909a6042ae881e8666098c` / published APK `lynx-v2-debug-3a87f8b` (Next #121 MERGED as PR #162), then tip APK honesty post-#121 tip `5018ded1c818d06d07c6a60667e36c16004f3d44` / published APK `lynx-v2-debug-5018ded` (Next #122 MERGED as PR #163), then tip APK honesty post-#122 tip `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af` / published APK `lynx-v2-debug-c1e2f5c` (Next #123 MERGED as PR #164), then tip APK honesty post-#123 tip `74d2d29ac5311a4b72f52474f73027f729ab7944` / published APK `lynx-v2-debug-74d2d29` (Next #124 MERGED as PR #165), then tip APK honesty post-#124 tip `cc15c1aefdd41e909ed3f55aa1171e067be84adf` / published APK `lynx-v2-debug-cc15c1a` (Next #125 MERGED as PR #166), then tip APK honesty post-#125 tip `592192f993ec3b429f49782c07d3c9194eea5127` / published APK `lynx-v2-debug-592192f` (Next #126 MERGED as PR #167), then tip APK honesty post-#126 tip `6d5ef04efabf29704c60e2392f10a94c226bcb5c` / published APK `lynx-v2-debug-6d5ef04` (Next #127 MERGED as PR #168), then tip APK honesty post-#127 tip `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a` / published APK `lynx-v2-debug-6c1f6d4` (Next #128 MERGED as PR #169), then tip APK honesty post-#128 tip `3695bd3f12086184a9c778201d232e92bc281f12` / published APK `lynx-v2-debug-3695bd3` (Next #129 MERGED as PR #170), then tip APK honesty post-#129 tip `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0` / published APK `lynx-v2-debug-b23a0a4` (Next #130 MERGED as PR #171), then tip APK honesty post-#130 tip `a11eb83c3136388ca951194466d949ec3c554956` / published APK `lynx-v2-debug-a11eb83` (Next #131 MERGED as PR #172), then tip APK honesty post-#131 tip `d8c0ed2889958ea6a0882d6a887501c4d3ef7986` / published APK `lynx-v2-debug-d8c0ed2` (Next #132 MERGED as PR #173), then tip APK honesty post-#132 tip `59b8a88edbb7d1cd15b46cb01830f0180e0b073e` / published APK `lynx-v2-debug-59b8a88` (Next #133 MERGED as PR #174), then tip APK honesty post-#133 tip `df879926fd2c8d4ad18d26bdee033f9345cc5c47` / published APK `lynx-v2-debug-df87992` (Next #134 MERGED as PR #175), then tip APK honesty post-#134 tip `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f` / published APK `lynx-v2-debug-b7db06b` (Next #135 MERGED as PR #176), then tip APK honesty post-#135 tip `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8` / published APK `lynx-v2-debug-3becdfa` (Next #136 MERGED as PR #177), then tip APK honesty post-#136 tip `165f27c8a55edf929de37de42255c859eef2d34c` / published APK `lynx-v2-debug-165f27c` (Next #137 MERGED as PR #178), then tip APK honesty post-#137 tip `ed506522f7bab857424af81a703520558f1fe359` / published APK `lynx-v2-debug-ed50652` (Next #138 MERGED as PR #180; tip `42bb986425ea447b164a779d8087f271630d75a4`), then Cap Settings search LynxInput (Next #139 MERGED as PR #181; tip `de4ff4ea23f3446df77f8c1764e51507aabd9ce7`; published APK later `lynx-v2-debug-de4ff4e`), then tip APK honesty post-#139 tip `de4ff4ea23f3446df77f8c1764e51507aabd9ce7` / published APK `lynx-v2-debug-de4ff4e` (Next #140 MERGED as PR #182), then tip APK honesty post-#140 tip `fa087e5bccd27bad126a5aedfab4255ce4553f75` / published APK `lynx-v2-debug-fa087e5` (Next #141 MERGED as PR #183), then tip APK honesty post-#141 tip `5f98d1c5427a064f60cddf743e88ba6ad454f1da` / published APK `lynx-v2-debug-5f98d1c` (Next #142 MERGED as PR #184), then tip APK honesty post-#142 tip `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e` / published APK `lynx-v2-debug-9fc7723` (Next #143 MERGED as PR #185), then tip APK honesty post-#143 tip `84107429dc9cd9e98d9de82807aa509d2bf8a496` / published APK `lynx-v2-debug-8410742` (Next #144 MERGED as PR #186), then tip APK honesty post-#144 tip `c3cb78a79bf80d0a90d448bb7133adc5869084d6` / published APK `lynx-v2-debug-c3cb78a` (Next #145 MERGED as PR #187), then tip APK honesty post-#145 tip `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5` / published APK `lynx-v2-debug-7227ddc` (Next #146 MERGED as PR #188), then tip APK honesty post-#146 tip `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d` / published APK `lynx-v2-debug-6b78c3d` (Next #147 MERGED as PR #189), then tip APK honesty post-#147 tip `a46ac25c3fe489f5727362912740a4622976414c` / published APK `lynx-v2-debug-a46ac25` (Next #148 MERGED as PR #190), then tip APK honesty post-#148 tip `38b31757b7460ab6781edb71acc9e933fab30e0c` / published APK `lynx-v2-debug-38b3175` (Next #149 MERGED as PR #191), then tip APK honesty post-#149 tip `daca88c832f3df0d1a59bf5ca3eaa828f1885656` / published APK `lynx-v2-debug-daca88c` (Next #150 MERGED as PR #192), then tip APK honesty post-#150 tip `dcb67d6075b670ce24fb6efcf19384ae8adbc07e` / published APK `lynx-v2-debug-dcb67d6` (Next #151 MERGED as PR #193), then tip APK honesty post-#151 tip `309af29373d68d7d4ea3757af9adf4896946490f` / published APK `lynx-v2-debug-309af29` (Next #152 MERGED as PR #194), then tip APK honesty post-#152 tip `c3a062a134a9796041ea605eb2e1acc66a2b289d` / published APK `lynx-v2-debug-c3a062a` (Next #153). **真机过 still open** until Yee confirms visible card shells on published APK (`lynx-v2-debug-c3a062a`). Prior 有字无样式 residual from tip `a16c3f7` / `lynx-v2-debug-cfed156` is closed by hex paint.

Empty for other landed claims. Seed the **kinds** of residual this track must expect (from Cap + Flutter history):

| Residual class | Why it will show up |
|---|---|
| IME / Chinese composition | WebView and Flutter both burned here |
| Keyboard occupancy vs queue / Changes | Collapsed-height contract |
| Session open pin / prepend jump | Why LegendList exists |
| LAN vs relay race | 1.5s LAN headstart; relay-only must skip it |
| FCM on debug package | `.debug` suffix vs `google-services.json` |
| iOS glass vs Android blur | Reviewers will call Android “broken glass” if you clone |
| Share generic vs Direct Share | Silent default Assistant is a bug |
| Predictive Back vs Lynx gesture arena | Two owners of the back edge |

A row moves here only after 代码接上 + CI绿 and a **written** device log (device model, OS, build SHA, what failed). “Looks fine in simulator” is not a residual; it is still **missing** 真机过.

---

## 故意不移植

| Item | Why |
|---|---|
| Expo / Flutter stack | This track is Lynx |
| 1.18 TanStack Virtual chat list | Hard forbid — use 1.19 LegendList semantics |
| Bonjour / Nearby browse | pitfalls §1; pairing v2 excluded LAN discovery |
| Invented native ASR / Siri voice | pitfalls §2 |
| WebView keyboard FLIP / ImeSyncBridge | pitfalls §3 |
| Settings slugs `shortcuts`, `remote-instances`, `global-config`, `skills.catalog` | Not in `MOBILE_SETTINGS_PAGE_SLUGS` |
| Desktop Finder reveal | Not a mobile feature |
| Electron / VS Code runtimes | Out of scope |
| Project notes / Todo / 加入笔记 | Removed in Cap 1.19.2 |
| Plan as a root tab | Plan is a session tool only |
| Hosted-H5 `mobileKeyboardMode=resize-content` | Browser viewport quirk; Lynx uses native IME |
| `openchamber.iosNativeUi` as a user toggle | Cap WebView-era; Lynx **is** native. Do not ship a “open the old WebView” switch in v1 |
| Capgo web-bundle OTA as the Lynx updater | Capgo updates `mobile.html`. A Lynx IPA/APK needs a real binary/OTA story later — do not upload Lynx assets to the Capacitor channel |
| PWA install-to-homescreen | Not the native app |
| Desktop SSH remote-instances UI | Hidden on mobile Settings |
| Fake Flexoki/Finder product chrome | pitfalls §8 |
| iPad layout in slice 1 | Phone first; iPad is a different IA (sidebar + sheets) |
| Merging `work/flutter-native` | Sibling track; different runtime |

---

## Settings slug board (22 mobile pages — Cap main)

| Slug | Status | First honest body |
|---|---|---|
| `instances` | 代码接上 (wired body) | List + add/delete + paste pairing + password unlock; QR camera stub |
| `appearance` | 代码接上 (wired GET/PUT) | Theme mode + `flexoki-*` ids. No `iosNativeUi` toggle |
| `chat` | 代码接上 (wired GET/PUT) | Reasoning / queue / follow-up via `/api/config/settings` |
| `notifications` | 代码接上 (wired hooks + push register module) | Toggles via settings blob; host injects tokens; Lynx registers `/api/push/apns-token` |
| `sessions` | 代码接上 (wired GET/PUT) | Auto-delete + retention from settings blob |
| `archived-sessions` | 代码接上 (wired list/unarchive page) | Cap `ArchivedSessionsPage` / manager `mode="page"`: reuse `listLynxArchivedSessions` + `unarchiveLynxSession`; failure ≠ empty; honest no-runtime; no Cap toast |
| `summary-ai` | 代码接上 (wired GET/PUT + small-model) | Settings blob + `/api/small-model`; failure ≠ empty |
| `projects` | 代码接上 (list + editor) | settings blob list; detail save/delete via settings PUT |
| `git` | 代码接上 (wired gitmoji + GitIdentity* editor) | gitmoji toggle; identities list/create/update/delete via `/api/git/identities` (no fake-success) |
| `providers` | 代码接上 (list + editor) | catalog list; detail + **auth DELETE**; generic save unsupported |
| `agents` | 代码接上 (list + editor) | list + `/api/config/agents/:name` PATCH/DELETE |
| `assistants` | 代码接上 (list + editor) | snapshot list + PATCH/DELETE `/api/openchamber/assistants/:id` |
| `behavior` | 代码接上 (wired agents.md + response style) | `/api/behavior/agents-md` + settings blob response style |
| `commands` | 代码接上 (list + editor) | catalog + PATCH/DELETE `/api/config/commands/:name` |
| `mcp` | 代码接上 (list + editor) | list + PATCH/DELETE `/api/config/mcp/:name` |
| `plugins` | 代码接上 (list + editor) | list + PATCH/DELETE `/api/config/plugins/entry/:id` |
| `magic-prompts` | 代码接上 (list + editor) | list + PUT/DELETE `/api/magic-prompts/:id` |
| `snippets` | 代码接上 (list + editor) | list + PATCH/DELETE `/api/config/snippets/:name` |
| `skills.installed` | 代码接上 (list + editor) | list + PATCH/DELETE `/api/config/skills/:name` |
| `usage` | 代码接上 (list) | per-provider `/api/quota/:id`; one failure stays on that row |
| `voice` | 代码接上 (status + model download/delete UI via `/api/dictation/*`) | Cap STT model rows + optional Kokoro `ttsModels`; mic/WS ASR host-bound — no invented ASR |
| `about` | 代码接上 (wired + diagnostics export) | Lynx client version **separate** from `/api/system/info`; `openchamber.client-diagnostics.v1` export |

---



## 代码接上 (host bridge deepen + git identities + HTML/Pierre stubs + draft `/` `@` — not landed under 三关)

Slice on `cursor/lynx-linux-gaps-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Host HTTP client | Cap runtimeFetch / URLSession·OkHttp | `src/host/httpClient.ts` + iOS URLSession / Android OkHttp stubs |
| Secure store Keychain/Keystore | `@aparajita/capacitor-secure-storage` | `secureStore.ts` + SecItem / EncryptedSharedPreferences API shapes; bounded timeout; never log tokens |
| Camera QR → pairing callback | `mobileQrScan.ts` | Bridge `scanPairingQr` → `qrScanResult` → Lynx parse; adapter unavailable without host |
| IME inset publisher | native composer keyboard | `imeInset.ts` + host keyboard observers → `keyboardInset` / `imeInset` |
| OAuth browser | ASWebAuthenticationSession / Custom Tabs | `oauthBrowser.ts` + iOS/Android launcher stubs with clear inject points |
| Host message channel | Cap plugin call/listen | `hostChannel.ts` Cap-plugin method list; wires adapters |
| Virtual asset scheme handlers | `openchamber-asset://` | Scheme registry + iOS/Android handler stubs (`registerSchemeHandler`) |
| Autocomplete ABOVE glass | Cap `OpenChamberComposerAutocomplete` | `composerAutocompleteLayout` + `ComposerAutocompleteList`; ChatScreen + DraftComposer |
| DraftComposer `/` `@` catalogs | ChatScreen composerCatalog | Same load/detect/suggest/apply path as chat |
| Settings git identities | Cap GitIdentity* `/api/git/identities` | List/create/update/delete + global read; never fake-success |
| Files HTML preview | `MobileFilesSurface` iframe | Cap-like source/preview toggle; preview = honest host WebView stub + text source |
| PierreDiff polish | `PierreDiffViewer` | Portable line-kind/stats + Cap ChangeRow spacing 代码接上; Cap `@pierre/diffs` **unavailable** (honest blocker, not stub-faked) |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed (environment: Linux cloud VM).

---

## 代码接上 (composer actions in glass — not landed under 三关)

Slice on `cursor/lynx-composer-actions-in-glass-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Attach / Send / Stop / Queue inside glass | Cap `OpenChamberComposer` contentView chrome | Controls moved **into** `LynxComposerGlassCard` (Chat + Draft). No action row sibling below glass. |
| Cap pill vs card order | collapsed `+`·input·Send/Stop; expanded `+`·spacer·Agent·model·Send/Stop | `resolveLynxComposerInGlassActionOrder` + `LynxComposerActionsInGlass`; Chat collapses when draft empty |
| Autocomplete ABOVE glass preserved | Cap autocomplete sibling | Still `forbidInsideGlassContentView`; list never nested under composer GlassChrome |

**CI绿:** lynx-ci live on tip + local Vitest/`tsc`/rspeedy. Not 真机过.
**真机过:** not executed (Linux cloud VM). Host Mode B composer overlay / live UIGlassEffect still residual. Agent·model picker sheets → next slice.

---

## 代码接上 (composer Agent·model picker sheets — not landed under 三关)

Slice on `cursor/lynx-agent-model-picker-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Agent picker sheet | Cap `AgentSelector` + `MobileResizableSheet` → `GET /api/agent` | `LynxComposerPickerSheets` kind=agent; Chat expanded footer + Draft expanded card open overlay; clear = not-selected |
| Model picker sheet | Cap `MobileModelPickerPanel` → `/api/config/providers` | Same overlay kind=model; id `providerID/modelID` |
| Selection → session send | Cap selection store + `prompt_async` body | Updates composer `LynxComposerModel` used by send/queue (no separate session PATCH; Cap same) |
| Autocomplete ABOVE glass preserved | Cap autocomplete sibling | Sheets outside GlassChrome; list never nested under composer contentView |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed (Linux cloud VM). Host Mode B composer overlay / live UIGlassEffect still residual.

---

## 代码接上 (resizable picker sheets + Draft Stop abort — not landed under 三关)

Slice on `cursor/lynx-resizable-picker-sheets-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Half-height MobileResizableSheet | Cap `MobileResizableSheet` + snap grabber | `LynxMobileResizableSheet`: grabber, Cap-aligned **0.72 / 0.98** (~72%/98% / 72dvh/98dvh) bottom sheet, scrim + vertical-drag dismiss — **replaces** full-screen `surface.background` picker overlay |
| Picker + explorer reuse | Agent/model pickers; DirectoryExplorer | Composer pickers mount on shared sheet; DirectoryExplorer reused cheaply |
| Draft busy Stop = abort | Cap Chat composerActions.stop / `POST …/abort` | When Draft `busy`, Stop aborts materialize + `abortSession` — never re-send |
| Glass / autocomplete | Cap autocomplete sibling | Triggers stay in-glass; sheets outside; autocomplete ABOVE glass |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed (Linux cloud VM). Live drag feel / Mode B overlay / UIGlassEffect still residual.

## 代码接上 (composer GlassChrome — not landed under 三关)

Slice on `cursor/lynx-composer-glass-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Composer GlassChrome / blur-view | Cap `OpenChamberComposer` UIGlassEffect | `LynxComposerGlassCard` on Chat + Draft; iOS glass / glass-interactive; Android blur-radius; no elevated solid fill |
| Autocomplete sibling ABOVE glass | Cap `OpenChamberComposerAutocomplete` | `forbidInsideGlassContentView` remains true; list never nested under composer GlassChrome |
| Optional autocomplete glass chips | Cap search-chip spirit | Per-row `searchChip` GlassChrome **in sibling list tree only** (not composer contentView) |

**CI绿:** lynx-ci live on tip + local Vitest/`tsc`/rspeedy. Not 真机过.
**真机过:** not executed (Linux cloud VM). Host Mode B composer overlay / live UIGlassEffect paint still residual.

---

## 代码接上 (linux closable after #55: portable diff + stage/unstage + rename — not landed under 三关)

Slice on `cursor/lynx-linux-closable-after-55-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Cap sheet height docs hygiene | `MobileResizableSheet` 72/98dvh | Next #23 for PR#55; replace stale ~50%/92% with **0.72 / 0.98** |
| Portable text diff | Cap Changes + PierreDiffViewer data | `parseLynxUnifiedDiffLines` / stats / line tokens in Changes detail — **not** `@pierre/diffs` runtime |
| Stage / unstage | `stageGitFiles` / `unstageGitFiles` → `POST /api/git/stage\|unstage` | Changes row chips; failure ≠ fake-success |
| Projects rename | Cap session menu rename → `PATCH /session/:id` | Long-press → draft input → `renameLynxSession` |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed (Linux cloud VM). Host Mode B / Pierre runtime / WKWebView / Keychain / camera / IME still residual.

---

## 代码接上 (diff/chip polish on #56 — not landed under 三关)

Slice on `cursor/lynx-diff-chip-polish-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Diff add/del coloring | Cap ChangeRow `--status-success` / `--status-error` | Portable lines: **add→status.success**, **del→status.error**; hunk/meta stay `surface.mutedForeground` (del ≠ muted). Still **not** `@pierre/diffs`. |
| Stage/unstage glass chips | Cap ChangeRow +/- + searchChip spirit | `GlassChrome` `searchChip`-sized +/- chips on Changes rows (outside transcript glass rules). Host absent → elevated fallback. |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed (Linux cloud VM). Pierre runtime / Mode B / live UIGlassEffect still residual.

---

## 代码接上 (Pierre investigation + ChangeRow spacing on #57 tip — not landed under 三关)

Slice on `cursor/lynx-pierre-diffs-local` (base PR#57 tip `858e6d0bb`; PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Cap `@pierre/diffs` | `PierreDiffViewer` → `FileDiff` / `diffs-container` Shadow DOM + `PIERRE_RUNTIME_BASE_CSS` + worker | **Unavailable** in Lynx. `resolveLynxPierreDiffFeature({ preferPierre: true })` still activates **portable-text** and records `LYNX_PIERRE_DIFF_BLOCKERS`. No fake CSS/iframe. |
| Portable feature path | Cap git `file-diff` / `diff` text | Unchanged real APIs; monospace line colors + Cap `+n / -m` slash spacing. |
| ChangeRow chip spacing | Cap `ChangeRow`: `size-6` / `gap-1.5` / `mx-0.5` / `h-8` / `w-3.5` | `LYNX_CHANGE_ROW_SPACING` + stage chip **24px** (was ~32), status letter, Cap slash stats; `diffStats` from `GET /api/git/status`. |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed. Host WebView sheet would be required before any Pierre HTML mount — out of scope / not claimed.

---


## 代码接上 (Changes revert + generateCommitMessage + commitAndPush — not landed under 三关)

Slice on `cursor/lynx-changes-revert-commitmsg-local` (PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. Track CI: `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` are **live** on tip (historical slices predated install). 真机过: not executed.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Revert file / bulk | `MobileChangesSurface` → `POST /api/git/revert` | `revertLynxGitFile` / `revertLynxGitFiles`; row Revert chip. Failure ≠ fake-success. |
| generateCommitMessage | Cap `gitApi.generateCommitMessage` → `POST /api/small-model/generate` | Cap-default commit magic prompt text + diff collect via `/api/git/diff`. No Cap session-fallback; dead `/api/git/commit-message` unused. |
| commitAndPush | Cap `handleCommit({ pushAfter: true })` | `commitAndPushLynxGitChanges` commit→push. Cap fetch/pull-if-behind completed in Next #28. |

**CI绿:** lynx-ci live on tip. Local Vitest `@openchamber/lynx` also. Not 真机过.
**真机过:** not executed.

---

## 代码接上 (Changes revert glass + Cap confirm + pull-if-behind on #59 tip — not landed under 三关)

Slice on `cursor/lynx-revert-confirm-pull-local` (base PR#59 tip `778c844ae`; PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Revert GlassChrome chip | Cap ChangeRow `arrow-go-back` size-6 | `RevertGlassChip` = GlassChrome `searchChip` + Cap `arrow-go-back` unicode ↩ (same size as stage +/−); not plain ActionChip text. |
| Cap confirm before revert | Cap ChangesPanel Dialog (revert-all / directory) | Was elevated-in-sheet card in #28; **centered Dialog** (scrim+panel) in Next #29. Cap does **not** confirm commit&push — not added. |
| pull-if-behind on commit&push | Cap `handleCommit({ pushAfter: true })` | Parse status `ahead`/`behind`/`tracking`; commit→fetch→pull(rebase) if behind→push if ahead. Failure ≠ silent skip. |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed.

---

## 代码接上 (Centered Cap Dialog revert confirm on #60 tip — not landed under 三关)

Slice on `cursor/lynx-centered-confirm-dialog-local` (base PR#60 tip `ac8cfd793`; PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Cap centered Dialog | Cap `Dialog` / `DialogContent` (scrim + centered `max-w-md`) | `LynxCenteredDialog` replaces elevated-in-sheet revert confirm card. Scrim dismiss when not busy; Cancel + destructive Revert footer. Follow-on #30 moves mount to shell-root portal. |
| RevertGlassChip + pull-if-behind | unchanged from #28 | Keep glass Cap `arrow-go-back` ↩ chip + commit→fetch→pull-if-behind→push. |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed.

---

## 代码接上 (Dialog shell portal + theme onError + Cap arrow-go-back on #61 tip — not landed under 三关)

Slice on `cursor/lynx-dialog-portal-theme-local` (base PR#61 tip `7a4c7b6a8`; PR into `work/lynx-native`). Package Vitest + `tsc` + rspeedy. 真机过: not executed. Product **NOT DONE**.

| Item | Cap/web source | Lynx note |
|---|---|---|
| Shell-root DialogPortal | Cap `DialogPortal` → document body | `LynxShellDialogPortalProvider` + `LynxDialogPortal` host at `LynxShellApp` root — **full-screen overlay**; not nested absolute inside Changes `position:relative`. |
| Destructive tokens | Cap Button destructive / `--destructive-foreground` | Fill `status.error`; text `status.onError` → `--destructive-foreground` (never hardcoded `#fff`). |
| Revert chip glyph | Cap Icon `arrow-go-back` sprite | `LYNX_CAP_ARROW_GO_BACK_GLYPH` = ↩ (U+21A9). Cap SVG sprite needs DOM — unavailable on Lynx; no invented brand. |

**CI绿:** lynx-ci live; Android sideload APK live on tip. Not 真机过 / no iOS IPA.
**真机过:** not executed.


## Remaining (honest — not EXHAUSTED)

**Product NOT DONE / 三关未齐 / not EXHAUSTED.** Linux Cap product rows continue to close on tip `c3a062a134a9796041ea605eb2e1acc66a2b289d` + follow-ons (connect/settings/chat/projects/changes + Android first-paint #65–#69 + Cap phone overflow **new-session** in Next #32 + Voice model download/delete UI in Next #33 + Voice STT select + Changes dirty badge in Next #34 + PermissionCard metadata + permission auto-accept in Next #35 + Cap share-recipient picker in Next #36 + Projects menus/share in Next #37 + QueuedMessageChips + SessionGoalRow in Next #38 + MobileSessionStatusBar slim strip in Next #39 + SessionGoal create/manage dialog in Next #40 + Cap server message-queue client in Next #41 + Cap full sessions sheet in Next #42 + worktree create/delete dialogs in Next #43 + Cap deleteRemoteBranch on worktree delete in Next #44 + Cap SessionsSheet two-step archive + unarchive undo in Next #45 + Cap ArchivedSessionsDialog in Next #46 + Cap session rename smart-title in Next #47 + Cap session tree archive/delete + hard-delete undo in Next #48 + Cap MobileProjectEditSurface in Next #49 + Cap SessionsSheet header trailing actions in Next #50 + Cap Assistant catalog menus + create/enable in Next #51 + Cap ProjectsHome per-bucket Show more in Next #52 + Android cssVar hex + strict smoke in Next #53 + Cap MCP sheet status/connect in Next #54 + Connect welcome elevated card chrome in Next #55 + tip APK/SHA docs honesty in Next #56 + Cap header swipe → sessions + transcript sync hint in Next #57 + Cap Files sheet search in Next #58 + tip APK/SHA docs honesty post-#58 in Next #59 + tip APK/SHA docs honesty post-#59 in Next #60 + Cap Changes stageAll/unstageAll/revertAll in Next #61 + tip APK/SHA docs honesty post-#61 in Next #62 + tip APK/SHA docs honesty post-#62 in Next #63 + tip APK/SHA docs honesty post-#63 in Next #64 + tip APK/SHA docs honesty post-#64 in Next #65 + tip APK/SHA docs honesty post-#65 in Next #66 + tip APK/SHA docs honesty post-#66 in Next #67 + tip APK/SHA docs honesty post-#67 in Next #68 + tip APK/SHA docs honesty post-#68 in Next #69 + tip APK/SHA docs honesty post-#69 in Next #70 + tip APK/SHA docs honesty post-#70 in Next #71 + tip APK/SHA docs honesty post-#71 in Next #72 + Cap Scheduled Run now + Delete in Next #73 + tip APK/SHA docs honesty post-#73 in Next #74 + tip APK/SHA docs honesty post-#74 in Next #75 + tip APK/SHA docs honesty post-#75 in Next #76 + tip APK/SHA docs honesty post-#76 in Next #77 + tip APK/SHA docs honesty post-#77 in Next #78 + tip APK/SHA docs honesty post-#78 in Next #79 + tip APK/SHA docs honesty post-#79 in Next #80 + tip APK/SHA docs honesty post-#80 in Next #81 + tip APK/SHA docs honesty post-#81 in Next #82 + tip APK/SHA docs honesty post-#82 in Next #83 + tip APK/SHA docs honesty post-#83 in Next #84 + tip APK/SHA docs honesty post-#84 in Next #85 + tip APK/SHA docs honesty post-#85 in Next #86 + tip APK/SHA docs honesty post-#86 in Next #87 + tip APK/SHA docs honesty post-#87 in Next #88 + tip APK/SHA docs honesty post-#88 in Next #89 + Cap Settings archived-sessions slug + page body in Next #90 + Cap Assistant unread + mark-all-read in Next #91 + tip APK/SHA docs honesty post-#91 in Next #92 + tip APK/SHA docs honesty post-#92 in Next #93 + tip APK/SHA docs honesty post-#93 in Next #94 + tip APK/SHA docs honesty post-#94 in Next #95 + tip APK/SHA docs honesty post-#95 in Next #96 + tip APK/SHA docs honesty post-#96 in Next #97 + tip APK/SHA docs honesty post-#97 in Next #98 + tip APK/SHA docs honesty post-#98 in Next #99 + tip APK/SHA docs honesty post-#99 in Next #100 + tip APK/SHA docs honesty post-#100 in Next #101 + tip APK/SHA docs honesty post-#101 in Next #102 + tip APK/SHA docs honesty post-#102 in Next #103 + tip APK/SHA docs honesty post-#103 in Next #104 + tip APK/SHA docs honesty post-#104 in Next #105 + tip APK/SHA docs honesty post-#105 in Next #106 + tip APK/SHA docs honesty post-#106 in Next #107 + tip APK/SHA docs honesty post-#107 in Next #108 + tip APK/SHA docs honesty post-#108 in Next #109 + tip APK/SHA docs honesty post-#109 in Next #110 + tip APK/SHA docs honesty post-#110 in Next #111 + tip APK/SHA docs honesty post-#111 in Next #112 + tip APK/SHA docs honesty post-#112 in Next #113 + tip APK/SHA docs honesty post-#113 in Next #114 + tip APK/SHA docs honesty post-#114 in Next #115 + tip APK/SHA docs honesty post-#115 in Next #116 + tip APK/SHA docs honesty post-#116 in Next #117 + tip APK/SHA docs honesty post-#117 in Next #118 + tip APK/SHA docs honesty post-#118 in Next #119 + tip APK/SHA docs honesty post-#119 in Next #120 + tip APK/SHA docs honesty post-#120 in Next #121 + tip APK/SHA docs honesty post-#121 in Next #122 + tip APK/SHA docs honesty post-#122 in Next #123 + tip APK/SHA docs honesty post-#123 in Next #124 + tip APK/SHA docs honesty post-#124 in Next #125 + tip APK/SHA docs honesty post-#125 in Next #126 + tip APK/SHA docs honesty post-#126 in Next #127 + tip APK/SHA docs honesty post-#127 in Next #128 + tip APK/SHA docs honesty post-#128 in Next #129 + tip APK/SHA docs honesty post-#129 in Next #130 + tip APK/SHA docs honesty post-#130 in Next #131 + tip APK/SHA docs honesty post-#131 in Next #132 + tip APK/SHA docs honesty post-#132 in Next #133 + tip APK/SHA docs honesty post-#133 in Next #134 + tip APK/SHA docs honesty post-#134 in Next #135 + tip APK/SHA docs honesty post-#135 in Next #136 + tip APK/SHA docs honesty post-#136 in Next #137 + tip APK/SHA docs honesty post-#137 in Next #138 + Cap Settings search LynxInput in Next #139 + tip APK/SHA docs honesty post-#139 in Next #140 + tip APK/SHA docs honesty post-#140 in Next #141 + tip APK/SHA docs honesty post-#141 in Next #142 + tip APK/SHA docs honesty post-#142 in Next #143 + tip APK/SHA docs honesty post-#143 in Next #144 + tip APK/SHA docs honesty post-#144 in Next #145 + tip APK/SHA docs honesty post-#145 in Next #146 + tip APK/SHA docs honesty post-#146 in Next #147 + tip APK/SHA docs honesty post-#147 in Next #148 + tip APK/SHA docs honesty post-#148 in Next #149 + tip APK/SHA docs honesty post-#149 in Next #150 + tip APK/SHA docs honesty post-#150 in Next #151 + tip APK/SHA docs honesty post-#151 in Next #152 + tip APK/SHA docs honesty post-#152 in Next #153). Queue chips + SessionGoal row/dialog + slim session status bar + server MQ client + full sessions sheet + worktree dialogs + remote-branch delete + archive/undo + ArchivedSessionsDialog + smart-title + tree delete/undo + MobileProjectEditSurface + SessionsSheet trailing + Assistant catalog menus + ProjectsHome per-bucket Show more + MCP sheet connect + Connect welcome card chrome JS 代码接上 in #38–#55; tip APK/SHA docs honesty in #56; header swipe + sync hint 代码接上 in #57; Files sheet `/api/find/file` search 代码接上 in #58; tip APK/SHA docs honesty post-#58 in #59; tip APK/SHA docs honesty post-#59 in #60; Cap Changes stageAll/unstageAll/revertAll 代码接上 in #61; tip APK/SHA docs honesty post-#61 in #62; tip APK/SHA docs honesty post-#62 in #63; tip APK/SHA docs honesty post-#63 in #64; tip APK/SHA docs honesty post-#64 in #65; tip APK/SHA docs honesty post-#65 in #66; tip APK/SHA docs honesty post-#66 in #67; tip APK/SHA docs honesty post-#67 in #68; tip APK/SHA docs honesty post-#68 in #69; tip APK/SHA docs honesty post-#69 in #70; tip APK/SHA docs honesty post-#70 in #71; tip APK/SHA docs honesty post-#71 in #72; Cap Scheduled Run now + Delete 代码接上 in #73; tip APK/SHA docs honesty post-#73 in #74; tip APK/SHA docs honesty post-#74 in #75; tip APK/SHA docs honesty post-#75 in #76 (PR #115 bound auto-connect splash already on tip); tip APK/SHA docs honesty post-#76 in #77; tip APK/SHA docs honesty post-#77 in #78; tip APK/SHA docs honesty post-#78 in #79; tip APK/SHA docs honesty post-#79 in #80; tip APK/SHA docs honesty post-#80 in #81; tip APK/SHA docs honesty post-#81 in #82; tip APK/SHA docs honesty post-#82 in #83; tip APK/SHA docs honesty post-#83 in #84; tip APK/SHA docs honesty post-#84 in #85; tip APK/SHA docs honesty post-#85 in #86; tip APK/SHA docs honesty post-#86 in #87; tip APK/SHA docs honesty post-#87 in #88; tip APK/SHA docs honesty post-#88 in #89; Cap Settings archived-sessions slug + page body 代码接上 in #90; Cap Assistant unread + mark-all-read 代码接上 in #91; tip APK/SHA docs honesty post-#91 in #92; tip APK/SHA docs honesty post-#92 in #93; tip APK/SHA docs honesty post-#93 in #94; tip APK/SHA docs honesty post-#94 in #95; tip APK/SHA docs honesty post-#95 in #96; tip APK/SHA docs honesty post-#96 in #97; tip APK/SHA docs honesty post-#97 in #98; tip APK/SHA docs honesty post-#98 in #99; tip APK/SHA docs honesty post-#99 in #100; tip APK/SHA docs honesty post-#100 in #101; tip APK/SHA docs honesty post-#101 in #102; tip APK/SHA docs honesty post-#102 in #103; tip APK/SHA docs honesty post-#103 in #104; tip APK/SHA docs honesty post-#104 in #105; tip APK/SHA docs honesty post-#105 in #106; tip APK/SHA docs honesty post-#106 in #107; tip APK/SHA docs honesty post-#107 in #108; tip APK/SHA docs honesty post-#108 in #109; tip APK/SHA docs honesty post-#109 in #110; tip APK/SHA docs honesty post-#110 in #111; tip APK/SHA docs honesty post-#111 in #112; tip APK/SHA docs honesty post-#112 in #113; tip APK/SHA docs honesty post-#113 in #114; tip APK/SHA docs honesty post-#114 in #115; tip APK/SHA docs honesty post-#115 in #116; tip APK/SHA docs honesty post-#116 in #117; tip APK/SHA docs honesty post-#117 in #118; tip APK/SHA docs honesty post-#118 in #119; tip APK/SHA docs honesty post-#119 in #120; tip APK/SHA docs honesty post-#120 in #121; tip APK/SHA docs honesty post-#121 in #122; tip APK/SHA docs honesty post-#122 in #123; tip APK/SHA docs honesty post-#123 in #124; tip APK/SHA docs honesty post-#124 in #125; tip APK/SHA docs honesty post-#125 in #126; tip APK/SHA docs honesty post-#126 in #127; tip APK/SHA docs honesty post-#127 in #128; tip APK/SHA docs honesty post-#128 in #129; tip APK/SHA docs honesty post-#129 in #130; tip APK/SHA docs honesty post-#130 in #131; tip APK/SHA docs honesty post-#131 in #132; tip APK/SHA docs honesty post-#132 in #133; tip APK/SHA docs honesty post-#133 in #134; tip APK/SHA docs honesty post-#134 in #135; tip APK/SHA docs honesty post-#135 in #136; tip APK/SHA docs honesty post-#136 in #137; tip APK/SHA docs honesty post-#137 in #138; Cap Settings search LynxInput 代码接上 in #139; tip APK/SHA docs honesty post-#139 in #140; tip APK/SHA docs honesty post-#140 in #141; tip APK/SHA docs honesty post-#141 in #142; tip APK/SHA docs honesty post-#142 in #143; tip APK/SHA docs honesty post-#143 in #144; tip APK/SHA docs honesty post-#144 in #145; tip APK/SHA docs honesty post-#145 in #146; tip APK/SHA docs honesty post-#146 in #147; tip APK/SHA docs honesty post-#147 in #148; tip APK/SHA docs honesty post-#148 in #149; tip APK/SHA docs honesty post-#149 in #150; tip APK/SHA docs honesty post-#150 in #151 + tip APK/SHA docs honesty post-#151 in #152 + tip APK/SHA docs honesty post-#152 in #153. Remaining Cap-parity polish is thin (Cap `@dnd-kit` / MobileWindowMotion / iPad sidebar; Cap toast lib / bulk multi-select deferred); **host binders / HTML WKWebView / Pierre `@pierre/diffs` (honest DOM blocker — not ported) / Live Activity / iOS IPA / Share extension + ShareReceiverActivity / clipboard host / 真机** still block EXHAUSTED. **Do not** mark landed under 三关. 真机残差: Connect welcome **card chrome** confirmation still open for Yee (cream≈elevated flat stack mitigated by border chrome — host/device confirm; not Linux code alone); other claims empty. Tip APK honesty relative to tip: tip `work/lynx-native` @ `c3a062a134a9796041ea605eb2e1acc66a2b289d` (PR #194 / Next #152 MERGED). Published prerelease `lynx-v2-debug-c3a062a` (release exists; asset `openchamber-lynx-debug-c3a062a.apk`; was stale `lynx-v2-debug-309af29` / tip `309af29373d68d7d4ea3757af9adf4896946490f`). Tip delta `309af29`→`c3a062a` is docs-only. Do NOT invent a newer APK than published. Lynx Mobile CI SUCCESS on tip `c3a062a` (run 35646713115); host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host/@dnd-kit/MobileWindowMotion/iPad/Cap toast).

### 代码接上 prior slice (`cursor/lynx-closable-missing-local` / PR#48)

| Item | Notes |
|---|---|
| Host Xcode/Gradle scaffold + bridge stubs | `packages/lynx/host/**` + README run steps; SDK pods/AAR still unresolved on Linux |
| `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` | **Live** on tip (lynx-ci green; Android sideload APK green; prerelease `lynx-v2-debug-b170e47`). Template copy under `packages/lynx/ci/` remains for reference. |
| Projects DirectoryExplorer | `/api/fs/home` + `/api/fs/list` + settings `projects[]` add |
| 扫一扫 / 切换实例 chrome | Camera adapter honest `unavailable`; instances → secondary nav |
| Composer `/` `@` agent/model | Cap commands/agents/magic-prompts + `/api/config/providers` models |
| Assistant continuous/stateless admission | `POST …/assistants/:id/messages` — no invented ASR |
| Settings Voice | `/api/dictation/status` + STT select PUT `sttLocalModel` (Next #34) + model download/delete **UI** (Next #33); mic/WS host-bound |
| About diagnostics export | `openchamber.client-diagnostics.v1` local ring buffer |
| `openchamber-asset://` hooks | TS + iOS/Android stub resolvers |
| Predictive / edge-back contract | JS policy + host stubs + wiring notes |

### 代码接上 prior slice (`cursor/lynx-linux-gaps-local`)

| Item | Notes |
|---|---|
| Settings GitIdentity* editor | Real `/api/git/identities` list/create/update/delete (+ global read); no fake-success |
| Files HTML preview | Cap iframe → Lynx labeled text stub + `planLynxHtmlPreview` until host WKWebView sheet |
| DraftComposer `/` `@` catalogs | Same `loadLynxComposerCatalogs` path as ChatScreen |
| Autocomplete above glass | `LynxComposerAutocompleteList` sibling overlay; `forbidInsideGlassContentView`; documented in imeOccupancy + ia-ui |
| Host-bridge deepen | secureStore / httpClient / oauthBrowser / imeInset / hostChannel + iOS/Android stubs |
| Gap-board Missing strikethrough | PR#48 DirectoryExplorer / 扫一扫 / composer `/` `@` / admission / diagnostics / assets / predictive-back |

### 代码接上 prior slice (`cursor/lynx-composer-glass-local`)

| Item | Notes |
|---|---|
| Composer GlassChrome | `LynxComposerGlassCard` replaces elevated solid fill on Chat + Draft; iOS glass/interactive; Android blur-radius |
| Autocomplete ABOVE glass preserved | List stays sibling; `forbidInsideGlassContentView=true`; optional row `searchChip` only in sibling tree |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-composer-actions-in-glass-local`)

| Item | Notes |
|---|---|
| Actions inside glass | Chat Attach/Send/Stop/Queue moved into `LynxComposerGlassCard`; Draft keeps Send (+ Attach stub) inside pill |
| Cap order | Collapsed pill `+`·input·Send/Stop; expanded card footer `+`·spacer·Agent·model·Send/Stop (± Queue while working) |
| Autocomplete ABOVE glass | Unchanged sibling; never GlassChrome contentView child |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-agent-model-picker-local`)

| Item | Notes |
|---|---|
| Agent picker sheet | Cap MobileResizableSheet spirit; lists `GET /api/agent`; Chat + Draft expanded Agent button |
| Model picker sheet | Lists `/api/config/providers` models; Chat + Draft expanded model button |
| Selection updates composer | `LynxComposerModel` → `prompt_async` agent/provider/model (Cap selection-store spirit; no invent PATCH) |
| Glass / autocomplete | Triggers stay in-glass; sheets + autocomplete stay outside / ABOVE glass |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-resizable-picker-sheets-local`)

| Item | Notes |
|---|---|
| Half-height resizable sheet | `LynxMobileResizableSheet`: grabber, Cap **0.72 / 0.98** (~72dvh / ~98dvh) bottom sheet, scrim + vertical-drag dismiss — not full-screen `surface.background` |
| Picker / explorer reuse | Agent·model pickers + DirectoryExplorer mount on shared sheet |
| Draft busy Stop = abort | Stop aborts materialize / `abortSession` (Chat composerActions alignment) — never re-send |
| Glass / autocomplete | Triggers in-glass; sheets outside; autocomplete ABOVE glass |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-sheet-height-cap-local` / PR#55)

| Item | Notes |
|---|---|
| Cap sheet height snaps | half **0.72** / expanded **0.98** (~72dvh / ~98dvh); grabber / dismiss / outside-glass unchanged |
| Docs honesty | Next #23; stale ~50%/92% notes replaced; NOT DONE / 三关未齐 |

### 代码接上 prior slice (`cursor/lynx-linux-closable-after-55-local` / PR#56)

| Item | Notes |
|---|---|
| Portable text diff | Unified line kinds + insertions/deletions stats + semantic line colors in Changes detail; Cap `@pierre/diffs` remains stub |
| Cap stage / unstage | `POST /api/git/stage` + `/api/git/unstage` on Changes rows; failure ≠ fake-success |
| Projects session rename | Long-press → rename draft → `PATCH /session/:id` (`renameLynxSession`) |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-diff-chip-polish-local` / PR#57)

| Item | Notes |
|---|---|
| Diff add/del tokens | Cap `--status-success` / `--status-error` on portable lines; del distinguished from hunk/muted |
| Stage/unstage glass chips | Cap +/- on `GlassChrome` `searchChip` (Changes sheet only; outside transcript) |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 this slice (`cursor/lynx-pierre-diffs-local`)

| Item | Notes |
|---|---|
| Pierre investigation | Cap `@pierre/diffs@1.3.0-beta.6` in monorepo (`packages/ui`); Lynx **cannot** host — Shadow DOM / react-dom / worker blockers documented |
| Feature path | `resolveLynxPierreDiffFeature` + `preferPierre` → always portable-text; `pierreViewer: 'unavailable'` |
| ChangeRow spacing | Cap-measurable `LYNX_CHANGE_ROW_SPACING`; stage chip 24px; `+n / -m` slash; status letter; status `diffStats` |
| Docs honesty | ia-ui + gap-board mark 代码接上 / not 真机过 / NOT DONE |

### 代码接上 prior slice (`cursor/lynx-changes-revert-commitmsg-local` / Next #27)

| Item | Notes |
|---|---|
| Cap revert file / bulk | `POST /api/git/revert` via `revertLynxGitFile` / `revertLynxGitFiles`; Changes row Revert chip; failure ≠ fake-success |
| Cap generateCommitMessage | Cap mobile path `POST /api/small-model/generate` purpose `commit` + Cap-default magic prompt text + `/api/git/diff` collect; parse subject/highlights. Session-fallback / dead `/api/git/commit-message` **not** ported |
| Cap commitAndPush | `commitAndPushLynxGitChanges` = commit → push (pull-if-behind completed in Next #28); Changes Commit & Push chip |
| Docs honesty | ia-ui unchanged; gap-board Next #27; NOT DONE / 三关未齐 |

### 代码接上 prior slice (`cursor/lynx-revert-confirm-pull-local` / Next #28)

| Item | Notes |
|---|---|
| Revert GlassChrome searchChip | Cap ChangeRow icon size-6 → Lynx `RevertGlassChip` ↩ on GlassChrome `searchChip` (same as stage +/−); not ActionChip text |
| Cap confirm dialog before revert | Cap ChangesPanel Dialog spirit (initially elevated-in-sheet card; replaced by centered modal in #29). Cap does **not** confirm commit&push — omitted honestly |
| Cap pull-if-behind | Parse `ahead`/`behind`/`tracking` from `GET /api/git/status`; commit→fetch→pull(rebase) if behind→push if ahead; failure ≠ silent skip |
| Docs honesty | gap-board Next #28; NOT DONE / 三关未齐 |

### 代码接上 prior slice (`cursor/lynx-centered-confirm-dialog-local` / Next #29)

| Item | Notes |
|---|---|
| Cap centered Dialog confirm | `LynxCenteredDialog`: scrim (`bg-black/50` spirit) + flex-centered `max-w-md` panel — **not** elevated-in-sheet card, **not** MobileResizableSheet half-card |
| RevertGlassChip + pull-if-behind preserved | Unchanged from #28; confirm opens centered modal before `POST /api/git/revert` |
| Cap does not confirm commit&push | Still omitted honestly |
| Docs honesty | gap-board Next #29; NOT DONE / 三关未齐 / no 真机过 |

### 代码接上 this slice (`cursor/lynx-dialog-portal-theme-local` / Next #30)

| Item | Notes |
|---|---|
| Shell-root portal mount | `LynxDialogPortal` → `LynxShellDialogPortalProvider` host on `LynxShellApp` — full-screen overlay (Cap DialogPortal); **not** nested in Changes relative |
| Destructive theme tokens | `status.error` fill + `status.onError` (`--destructive-foreground`) text — never `#fff` |
| Cap arrow-go-back glyph | `LYNX_CAP_ARROW_GO_BACK_GLYPH` ↩ from Cap Icon `arrow-go-back`; Cap SVG sprite DOM-blocked on Lynx |
| Docs honesty | gap-board Next #30; NOT DONE / 三关未齐 / no 真机过 |

### 代码接上 this slice (Android first-paint + live CI honesty / Next #31)

| Item | Notes |
|---|---|
| Flexoki-light token paint | `LYNX_LIGHT_FALLBACKS` + `cssVar(token)` / `tokenColor()` → **resolved `#hex`** (never `var(--name, #hex)` — Android Lynx drops those inline values → 有字无样式). Cap CSS var *names* remain in `LYNX_TOKEN_CSS_VARS` for docs. |
| Android cream windowBackground | `themes.xml` / `colors.xml` `lynx_window_background` `#fffdf4`; nav bar softened (not pure black void) |
| HostActivity + ViewFactory globalProps | MATCH_PARENT LynxView; `globalProps` → `updateGlobalProps` + `TemplateData`; App reads `lynx.__globalProps` |
| ConnectWelcome first paint | Splash + done-phase use plain hex cream/elevated/primary; brand `OpenChamber Lynx` text; host TextView overlay until first_screen+20s |
| Strict emulator smoke | `scripts/lynx-android-emulator-smoke.sh` requires literal `text="Connecting` / `text="OpenChamber Lynx` — **not** content-desc alone, **not** splash console.log (false-green on cfed156 fixed) |
| Live CI | `.github/workflows/lynx-ci.yml` + `lynx-mobile-ci.yml` live |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / **真机过 still open** until Yee confirms styled welcome |

### 代码接上 this slice (`cursor/lynx-overflow-new-session-local` / Next #32)

| Item | Notes |
|---|---|
| Cap phone overflow new-session | First overflow item → shell `openDraft` / `LynxDraftComposer` (Cap `mobile.menu.newSession`) |
| Overflow order | Cap phone: new-session · Files · Changes · MCP · refresh (no Capgo update; no iPad Settings) |
| Docs honesty | Tip SHA / APK `lynx-v2-debug-b170e47`; Next #31 notes #67 TextDecoder / #68 page+splash / #69 no-explicit-page |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-voice-models-download-local` / Next #33)

| Item | Notes |
|---|---|
| Voice model rows | Cap STT list from status `models` + Download/progress/Delete via existing helpers |
| Kokoro TTS row | When `ttsModels` includes `kokoro-en-v0_19`, show Local TTS row (same routes) |
| No invented ASR | Policy banner + `LYNX_DICTATION_VOICE_POLICY`; mic/WS host-bound |
| Docs honesty | Tip SHA / APK `lynx-v2-debug-b170e47` (was stale `30b1c12`); Voice missing struck; Next #33 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |


### 代码接上 this slice (`cursor/lynx-voice-select-dirty-badge-local` / Next #34)

| Item | Notes |
|---|---|
| Voice STT model select | Cap `LOCAL_STT_MODELS` radio → `PUT /api/config/settings` `{ sttLocalModel }`; status refresh with `localModel` |
| Optional dictationEnabled | Cap Voice enable toggle via same settings blob when types fit |
| Keep #71 download/delete | Progress/delete unchanged; no invented ASR/mic/WS |
| Preview / browser TTS / say | Labeled unavailable — no invented audio |
| Overflow Changes dirty badge | Cap `dirtyChangeCount` from git status `entries.length` when ok; no-runtime/failure → no fake badge |
| Docs honesty | Tip SHA / APK `lynx-v2-debug-b170e47` (was stale `1a5c899`); Next #34 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-permission-metadata-auto-accept-local` / Next #35)

| Item | Notes |
|---|---|
| PermissionCard metadata | Cap `PermissionCard` + `types/permission.ts` `metadata`; Lynx `formatLynxPermissionMetadataLines` → plain text bash/edit/write/webfetch/generic (no DOM/react-dom/WorkerHighlightedCode) |
| Permission auto-accept | Cap `GET/PUT /api/permission-auto-accept` + lineage `lynxAutoRespondsPermission` + composer/draft toggle + client auto-reply `once` when enabled; failure ≠ fake-success (server runtime remains authoritative) |
| Docs honesty | Tip SHA on this PR head; APK may still `lynx-v2-debug-b170e47` until mobile-ci — do not invent APK SHA; Next #35 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-share-recipient-picker-local` / Next #36)

| Item | Notes |
|---|---|
| Share draft Partial target | Cap `NativeShareDraft` + `isAssignedNativeShareDraft`; Lynx `shareDraft.ts` — never silent-default |
| Full-page recipient picker | Cap `MobileShareRecipientPicker` → `LynxShareRecipientPicker` via shell DialogPortal (**full-page overlay, never a sheet**); plain Lynx views (no react-dom Dialog) |
| Bridge wiring | Cap `MobileShareBridge` spirit: unassigned → picker; assigned → `dispatchAssignedDraft` → POST `…/assistants/:id/share`; cancel drops/acks without inventing success |
| Shell mount | `LynxShareBridge` inside `LynxShellDialogPortalProvider`; host injects drafts via `shareInbox.acceptDraft` |
| Host-only (separate) | iOS Share Extension / Android `ShareReceiverActivity` stay host-only — not claimed by this JS picker |
| Docs honesty | Tip APK `lynx-v2-debug-93b4355`; Next #36 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-projects-menus-share-local` / Next #37)

| Item | Notes |
|---|---|
| Project / worktree menus | Cap `MobileRowActionsSheet` spirit via `buildLynxProjectMenuItems` / `buildLynxWorktreeMenuItems` long-press on ProjectsHome cards |
| Session share / copy / unshare | Cap/OpenCode `POST|DELETE /session/:id/share`; never fake-success without `share.url`; copy uses navigator.clipboard when present else labeled unavailable |
| newSession | Project/worktree → `openDraft({ directory })` / DraftComposer materialize in that directory |
| syncSessions | Cap-style `POST /api/openchamber/session-index/sync` for project dirs; honest failure/unsupported |
| edit / closeProject | Settings `projects[]` meta via `/api/config/settings`; close confirm via `LynxCenteredDialog` portal; labeled unavailable when not in settings |
| newWorktree / deleteWorktree | Cap `/api/git/worktrees` create/delete; confirm delete via centered dialog; labeled unavailable on 404/501 |
| Docs honesty | Tip APK `lynx-v2-debug-0b470a2`; Next #37 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-queue-chips-goal-local` / Next #38)

| Item | Notes |
|---|---|
| QueuedMessageChips | Cap chips above composer; Lynx `LynxQueuedMessageChips` + `queuedMessageChips.ts` on local `composerActions` queue — remove / send-now / flush; portable ↑/↓ reorder (no `@dnd-kit`) |
| Server message-queue | Cap `/api/openchamber/message-queue` thin client 代码接上 in Next #41; TanStack/Zustand flights still deferred |
| SessionGoalRow | Cap compact strip; Lynx `sessionGoal.ts` + `LynxSessionGoalRow` via GET `/session/:id` metadata + PATCH status; file objective GET `/api/goals/objective/:id` when `objectiveFile`; never fake-success |
| MobileSessionStatusBar (full Cap sheet) | Slim strip 代码接上 in Next #39; **full** Cap sessions sheet 代码接上 in Next #42; worktree create/delete dialogs in Next #43; remote-branch delete in Next #44; two-step archive + unarchive undo in Next #45; ArchivedSessionsDialog in Next #46; smart-title in Next #47; Settings `archived-sessions` page landed in Next #90; Cap `@dnd-kit` / MobileWindowMotion / Cap toast lib / bulk multi-select still deferred |
| Docs honesty | Tip APK `lynx-v2-debug-905c42e`; Next #38 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-session-status-bar-local` / Next #39)

| Item | Notes |
|---|---|
| Slim MobileSessionStatusBar | Cap related-session chips + busy/working indicator above composer (with SessionGoal + queue); **not** full Cap sheet |
| Pure helpers | Cap `shouldPreserveActiveProjectOnSessionOpen` + `resolveMobileSessionSheetDefaultFilter`; status normalize; session-index related list |
| Shell wiring | session-index → relatedSessions / orderedSessionIds; chip tap → openChat; "All" → Projects home (honest Cap sheet analogue) |
| Deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad sidebar (full sessions sheet in #42; worktree dialogs in #43; server MQ thin client in #41) |
| Docs honesty | Tip APK `lynx-v2-debug-ca32a72` (release exists); Next #39 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-session-goal-dialog-local` / Next #40)

| Item | Notes |
|---|---|
| set/clear helpers | Cap `setSessionGoal` / `clearSessionGoal` → `setLynxSessionGoal` / `clearLynxSessionGoal`; GET+PATCH metadata + PUT/DELETE `/api/goals/objective/:id`; never fake-success |
| SessionGoalDialog | Cap create/manage overlay via `LynxCenteredDialog` + shell `LynxDialogPortal`; objective + optional token budget; clear/save/start |
| Row + create entry | Row tap → manage dialog; create entry when no goal opens create dialog; Cap `/goal` arm toggle (never auto-send; arm applies on user Send) |
| Complete | Cap complete via existing `setLynxSessionGoalStatus('complete')` |
| Docs honesty | Tip `8def01d0` / APK `lynx-v2-debug-8def01d` (release exists); Next #40 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |

### 代码接上 this slice (`cursor/lynx-message-queue-server-local` / Next #41)

| Item | Notes |
|---|---|
| messageQueueServer.ts | Cap-compatible HTTP helpers over `LynxRuntimeFetch`: list/get scope (directory+session), admit text, reorder by queueItemIDs, remove, send-now, flush→send-now first; honest parse; no-runtime/HTTP never fake-success |
| composer + chips wire | Prefer server queue when runtime reachable; unavailable (501/transport) → local queue; chips keep portable ↑/↓ (no `@dnd-kit`) |
| Vitest | Happy-path parse + failure paths (`messageQueueServer.test.ts`) + composer server/local coverage |
| Docs honesty | Tip `c02f917f` / APK `lynx-v2-debug-c02f917` (release exists); Next #41 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |
| Still deferred | Cap TanStack/Zustand MQ sync flights; `@dnd-kit`; host-only unchanged (full sessions sheet in #42; worktree dialogs in #43; remote-branch delete in #44) |

### 代码接上 this slice (`cursor/lynx-sessions-trailing-local` / Next #50)

| Item | Notes |
|---|---|
| Trailing helpers | `resolveLynxSessionsSheetTrailingActionIds` / `TrailingActiveProject` / `NewChatDirectory` — Cap gating (newChat if projects; newWorktree if active git; addProject always) |
| SessionsSheet header | `LynxMobileResizableSheet` `trailing` slot: newChat → `onOpenDraft?.(directory)`; newWorktree → existing `LynxCreateWorktreeDialog`; addProject → `DirectoryExplorerSheet` |
| i18n | `lynx.chat.sessionsSheet.newChat`; reuse `lynx.projects.chrome.addProject` + `lynx.projects.menu.newWorktree` |
| Vitest | Focused trailing appear / directory resolve coverage in `sessionsSheet.test.ts` |
| Docs honesty | Tip `8f2df6d8` / published APK `lynx-v2-debug-8f2df6d` (do NOT invent newer); Next #50 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### 代码接上 this slice (`cursor/lynx-assistant-catalog-menus-local` / Next #51)

| Item | Notes |
|---|---|
| createLynxAssistant / setLynxAssistantsEnabled / deleteLynxAssistant | Cap POST `/api/openchamber/assistants`, PUT `…/settings` `{enabled,expectedRevision}`, DELETE `…/:id` + expectedRevision — Vitest happy + failure/no-runtime |
| AssistantTab menus | Long-press/overflow Edit → Settings assistants EntityEditor (focus id); Delete → `LynxCenteredDialog` portal; empty Create CTA → Settings assistants; disabled Enable CTA → PUT then refresh |
| Settings AssistantsSettingsBody | Create button: resolve first provider/model → POST → open EntityEditor; Enable when disabled banner |
| Portable errors | Inline/banner only — no Cap toast / sonner |
| Docs honesty | Tip `c1bb93c4` / published APK `lynx-v2-debug-c1bb93c` (do NOT invent newer); Next #51 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### 代码接上 this slice (`cursor/lynx-projects-home-show-more-local` / Next #52)

| Item | Notes |
|---|---|
| Per-bucket Show more | Cap `visibleCountByBucket` on ProjectsHome worktree/main groups — default 3 / +7; Show more / Show fewer |
| Search bypass | Cap `catalogSessions` spirit — keyword search filters full bucket catalog and skips compact slice / Show more while `searching` |
| Reuse SessionsSheet helpers | `sliceLynxSessionsSheetVisible` / `nextLynxSessionsSheetVisibleCount` / `collapseLynxSessionsSheetVisibleCount` + `lynxProjectsHomeBucketKey` (`projectId::worktreeId`); no new Cap APIs |
| Vitest | Bucket-key + Cap 3/+7 coverage in `sessionsSheet.test.ts` |
| Docs honesty | Tip `cfed15657` / published APK `lynx-v2-debug-cfed156` (was stale `c1bb93c`; do NOT invent newer); Next #52 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED / 真机残差 empty / no 真机过 claim |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### 代码接上 this slice (`cursor/lynx-mcp-sheet-connect-local` / Next #54)

| Item | Notes |
|---|---|
| MCP sheet status | Cap `GET /mcp` + `/api/config/mcp` merged rows (status ∪ config names) |
| Connect / disconnect | Cap Switch spirit → `POST /mcp/{name}/connect` / `disconnect`; refresh; busy guard |
| Honesty | Failure / no-runtime never fake ON; `needs_auth` labeled (OAuth browser host-only) |
| Vitest | `mcpSheet.test.ts` — parse/tone/merge/load/connect/disconnect |
| Docs honesty | Tip `7c5a135a9` / published APK `lynx-v2-debug-7c5a135` (do NOT invent newer); Next #54 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome style confirmation |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### 代码接上 this slice (`cursor/lynx-connect-welcome-card-chrome` / Next #55)

| Item | Notes |
|---|---|
| Flat elevated residual | After cssVar→hex, cream `#fffdf4` ≈ elevated `#fbfaf2` — cards read as flat stack on Android softemu |
| Card chrome | pending-password / paste / add-instance / scan-QR: `borderWidth 1` + `borderColor` `interactive.selection` (`#76736f30`) + `borderStyle solid` + `borderRadius 12` + elevated fill; optional soft `boxShadow` (border is primary) |
| Page bg | Stays cream `surface.background` `#fffdf4` (hex-only, no `var()`) |
| Docs honesty | Tip base `5239dd7e6` / published APK still `lynx-v2-debug-7c5a135` (do NOT invent newer); Next #55 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 = Connect welcome **card chrome** confirm for Yee |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-eca4a03` / Next #56)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp / sessions / chat) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `eca4a0305` (PR #94 MERGED); published APK `lynx-v2-debug-eca4a03` (was stale `lynx-v2-debug-7c5a135` / `7c5a135a9`/`5239dd7e6`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-eca4a03` |

### 代码接上 this slice (`cursor/lynx-header-swipe-sync-hint-local` / Next #57)

| Item | Notes |
|---|---|
| Header swipe → sessions | Cap `useHeaderSwipeToSessions` portable TS + machine; ChatScreen/shell → `onOpenSessionsSheet` / `onBack`; `headerSwipeDispatchRef` host pan (honest — no fake native pan) |
| Transcript sync hint | Cap resolve + smoother; Lynx timeline hydrate / user refresh / live-tail phase only (no Cap Zustand flights); header subtitle |
| Docs honesty | Tip `489336a34` / published APK `lynx-v2-debug-489336a` (was stale `eca4a03`); Next #57 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome **card chrome** confirm for Yee |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged |

### 代码接上 this slice (`cursor/lynx-files-search-local` / Next #58)

| Item | Notes |
|---|---|
| Files search API | Cap `GET /api/find/file` (web FilesAPI.search / OpenCode find.files) — `searchLynxFiles` with `dirs=false` `type=file` limit 40 + `x-opencode-directory`; failure ≠ empty; no-query/no-directory/no-runtime honest |
| Files sheet UI | Cap MobileFilesSurface search input + results; Lynx `LynxInput` debounce ~250ms; hits show name + relativePath; tap → existing preview; clear query restores directory browse |
| Vitest | `filesSurface.test.ts` — no-runtime/no-dir/no-query, HTTP fail, Cap string[] parse, `{data:[]}` shape, empty ok |
| Docs honesty | Base tip `4d871bad0` / published APK `lynx-v2-debug-4d871ba` (was stale `489336a`); do NOT invent newer APK for PR head; Next #58 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome **card chrome** confirm for Yee on `lynx-v2-debug-4d871ba` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (WKWebView HTML preview / Pierre / Keychain / camera / IME / Mode B / Share ext / iOS IPA) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-53ea9cd` / Next #59)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp / Files / Changes / SessionsSheet / chat header-swipe+sync / overflow) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `53ea9cdee4` (PR #97 / Next #58 MERGED); published APK `lynx-v2-debug-53ea9cd` (was stale `lynx-v2-debug-4d871ba` / tip `4d871bad0`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-53ea9cd` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-5ae491f` / Next #60)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp / Files / Changes / SessionsSheet / chat header-swipe+sync / overflow / MCP) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `5ae491f6a3` (PR #98 / Next #59 MERGED); published APK `lynx-v2-debug-5ae491f` (was stale `lynx-v2-debug-53ea9cd` / tip `53ea9cdee4`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-5ae491f` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA) |


### 代码接上 this slice (`cursor/lynx-changes-stage-all-local` / Next #61)

| Item | Notes |
|---|---|
| Cap Changes stageAll / unstageAll | Cap `ChangesPanel` group `onActionAll` → `stageLynxGitFiles` / `unstageLynxGitFiles` multi-path; staged/unstaged group headers + GlassChrome +/− all |
| Cap Changes revertAll | Cap revert-all toolbar + centered Dialog confirm → `revertLynxGitFiles`; single-file confirm unchanged |
| Docs honesty | Base tip `c8a72d6025` (PR #99 / Next #60 MERGED); published APK `lynx-v2-debug-5ae491f` (do NOT invent newer); Next #61 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome **card chrome** confirm for Yee on `lynx-v2-debug-5ae491f` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (WKWebView HTML preview / Pierre / Keychain / camera / IME / Mode B / Share ext / iOS IPA) |


### Docs honesty this slice (`cursor/lynx-gap-board-apk-773dd98` / Next #62)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp / Files / Changes / SessionsSheet / chat header-overflow / MCP / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `773dd989b9` (PR #100 / Next #61 MERGED); published APK `lynx-v2-debug-773dd98` (was stale `lynx-v2-debug-5ae491f` / tip bases `5ae491f6a3` / `c8a72d6025`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-773dd98` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/iOS IPA/FCM for lynx.debug / Live Activity) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-dda945b` / Next #63)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `dda945b12` (PR #101 / Next #62 MERGED); published APK `lynx-v2-debug-dda945b` (was stale `lynx-v2-debug-773dd98` / tip `773dd989b9`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-dda945b` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-2384ea6` / Next #64)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `2384ea6c95` (PR #102 / Next #63 MERGED); published APK `lynx-v2-debug-2384ea6` (was stale `lynx-v2-debug-dda945b` / tip `dda945b12`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-2384ea6` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-3f15ea4` / Next #65)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `3f15ea4196` (PR #103 / Next #64 MERGED); published APK `lynx-v2-debug-3f15ea4` (was stale `lynx-v2-debug-2384ea6` / tip `2384ea6c95`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3f15ea4` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-8ba8989` / Next #66)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `8ba89898a31` (PR #104 / Next #65 MERGED); published APK `lynx-v2-debug-8ba8989` (was stale `lynx-v2-debug-3f15ea4` / tip `3f15ea4196`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ba8989` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-538c312` / Next #67)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `538c31239fa4` (PR #105 / Next #66 MERGED); published APK `lynx-v2-debug-538c312` (was stale `lynx-v2-debug-8ba8989` / tip `8ba89898a31`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-538c312` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-cd074ee` / Next #68)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `cd074ee2b2b3` (PR #106 / Next #67 MERGED); published APK `lynx-v2-debug-cd074ee` (was stale `lynx-v2-debug-538c312` / tip `538c31239fa4`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-cd074ee` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-50fa262` / Next #69)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `50fa2626e946` (PR #107 / Next #68 MERGED); published APK `lynx-v2-debug-50fa262` (was stale `lynx-v2-debug-cd074ee` / tip `cd074ee2b2b3`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-50fa262` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-fb9a969` / Next #70)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `fb9a969c7c0d` (PR #108 / Next #69 MERGED); published APK `lynx-v2-debug-fb9a969` (was stale `lynx-v2-debug-50fa262` / tip `50fa2626e946`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-fb9a969` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-68b1919` / Next #71)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `68b1919a566c` (PR #109 / Next #70 MERGED); published APK `lynx-v2-debug-68b1919` (was stale `lynx-v2-debug-fb9a969` / tip `fb9a969c7c0d`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-68b1919` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-3b2ae4c` / Next #72)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `3b2ae4c1283700794d3b5124605cee914c4dc545` (PR #110 / Next #71 MERGED); published APK `lynx-v2-debug-3b2ae4c` (was stale `lynx-v2-debug-68b1919` / tip `68b1919a566c`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3b2ae4c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### 代码接上 this slice (`cursor/lynx-scheduled-run-delete-local` / Next #73)

| Item | Notes |
|---|---|
| Cap Run now | `runScheduledTaskNow` → `POST /api/projects/:projectId/scheduled-tasks/:taskId/run`; optional `sessionId` opens chat via existing `onOpenRunSession` |
| Cap Delete | `deleteScheduledTask` → `DELETE …/scheduled-tasks/:taskId`; two-tap confirm (no Cap toast/sonner) |
| Editor chrome | `ScheduledEditor` edit mode: Run now + Delete under Save; list reload via `onDeleted` / `onRan` |
| Vitest | `api.test.ts` — DELETE route, POST /run + sessionId, id guards, HTTP failure ≠ fake-success |
| Docs honesty | Base tip `ea6349303` (PR #111 / Next #72 MERGED); published APK still `lynx-v2-debug-3b2ae4c` (do NOT invent newer); Next #73 |
| Docs honesty | NOT DONE / 三关未齐 / not EXHAUSTED; 真机残差 keeps Connect welcome **card chrome** confirm for Yee on `lynx-v2-debug-3b2ae4c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-0a56dbe` / Next #74)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only |
| Tip / APK honesty | Tip `work/lynx-native` @ `0a56dbedd1635ebe3cc31ed40fa5f428bc677bfc` (PR #112 / Next #73 MERGED); published APK `lynx-v2-debug-0a56dbe` (was stale `lynx-v2-debug-3b2ae4c` / tip bases `3b2ae4c1283700794d3b5124605cee914c4dc545` / `ea6349303`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-0a56dbe` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-b798684` / Next #75)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host |
| Tip / APK honesty | Tip `work/lynx-native` @ `b7986841cc7341800880febf6958299e65d50592` (PR #113 / Next #74 MERGED); published APK `lynx-v2-debug-b798684` (was stale `lynx-v2-debug-0a56dbe` / tip `0a56dbedd1635ebe3cc31ed40fa5f428bc677bfc`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-b798684` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-51ab13b` / Next #76)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. PR #115 bound auto-connect splash already on tip (Connect welcome after timeout — not a remaining gap) |
| Tip / APK honesty | Tip `work/lynx-native` @ `51ab13b832164fac30956bb1297e2f1d93ed430b` (PR #115 auto-connect timeout MERGED; PR #114 / Next #75 MERGED); published APK `lynx-v2-debug-51ab13b` (was stale `lynx-v2-debug-b798684` / tip `b7986841cc7341800880febf6958299e65d50592`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-51ab13b` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-896bd12` / Next #77)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `896bd125704d86d11089ee138ccdd841a8a21a94` (PR #116 / Next #76 MERGED); published APK `lynx-v2-debug-896bd12` (was stale `lynx-v2-debug-51ab13b` / tip `51ab13b832164fac30956bb1297e2f1d93ed430b`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-896bd12` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-51870e1` / Next #78)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `51870e1d175114ce0d4ad820db41311bd702d083` (PR #117 / Next #77 MERGED); published APK `lynx-v2-debug-51870e1` (was stale `lynx-v2-debug-896bd12` / tip `896bd125704d86d11089ee138ccdd841a8a21a94`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-51870e1` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-4643585` / Next #79)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `46435859225186b343d439d0744385f651c1d9a2` (PR #118 / Next #78 MERGED); published APK `lynx-v2-debug-4643585` (was stale `lynx-v2-debug-51870e1` / tip `51870e1d175114ce0d4ad820db41311bd702d083`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-4643585` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-cf705f5` / Next #80)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `cf705f5d1458824ac1a8270d498cd83b3e44ff94` (PR #119 / Next #79 MERGED); published APK `lynx-v2-debug-cf705f5` (was stale `lynx-v2-debug-4643585` / tip `46435859225186b343d439d0744385f651c1d9a2`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-cf705f5` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-267ba62` / Next #81)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `267ba627842656370ae9b1295c4b59ff9c505c38` (PR #120 / Next #80 MERGED); published APK `lynx-v2-debug-267ba62` (was stale `lynx-v2-debug-cf705f5` / tip `cf705f5d1458824ac1a8270d498cd83b3e44ff94`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-267ba62` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-0b47a71` / Next #82)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `0b47a71f14d8a1945dae14e333ba695451707950` (PR #121 / Next #81 MERGED); published APK `lynx-v2-debug-0b47a71` (was stale `lynx-v2-debug-267ba62` / tip `267ba627842656370ae9b1295c4b59ff9c505c38`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-0b47a71` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-5287db7` / Next #83)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `5287db7438ae51bad0033ee6614c83fee7ad6f2a` (PR #122 / Next #82 MERGED); published APK `lynx-v2-debug-5287db7` (was stale `lynx-v2-debug-0b47a71` / tip `0b47a71f14d8a1945dae14e333ba695451707950`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-5287db7` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-3d0d768` / Next #84)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f` (PR #123 / Next #83 MERGED); published APK `lynx-v2-debug-3d0d768` (was stale `lynx-v2-debug-5287db7` / tip `5287db7438ae51bad0033ee6614c83fee7ad6f2a`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-3d0d768` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-607eaf6` / Next #85)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `607eaf6dfc726f7d18fd6eac2b44542e39365b09` (PR #124 / Next #84 MERGED); published APK `lynx-v2-debug-607eaf6` (was stale `lynx-v2-debug-3d0d768` / tip `3d0d7683a14c5695f9d2ba1705b14ef5614dbc1f`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-607eaf6` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-edaec0d` / Next #86)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `edaec0d9bbab4f132a51e4fde3259dd994b49369` (PR #125 / Next #85 MERGED); published APK `lynx-v2-debug-edaec0d` (was stale `lynx-v2-debug-607eaf6` / tip `607eaf6dfc726f7d18fd6eac2b44542e39365b09`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-edaec0d` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-dbad742` / Next #87)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `dbad742588da5f7c575d23c5269b3317dd1cd1ce` (PR #126 / Next #86 MERGED); published APK `lynx-v2-debug-dbad742` (was stale `lynx-v2-debug-edaec0d` / tip `edaec0d9bbab4f132a51e4fde3259dd994b49369`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-dbad742` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-4d16bf8` / Next #88)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `4d16bf8070865883c79b0e13a3c47e63d573311c` (PR #127 / Next #87 MERGED); published APK `lynx-v2-debug-4d16bf8` (was stale `lynx-v2-debug-dbad742` / tip `dbad742588da5f7c575d23c5269b3317dd1cd1ce`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-4d16bf8` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-gap-board-apk-8ec0384` / Next #89)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking |
| Tip / APK honesty | Tip `work/lynx-native` @ `8ec03842faed8caa6c255a237e617ef1ddf2e89f` (PR #128 / Next #88 MERGED); published APK `lynx-v2-debug-8ec0384` (was stale `lynx-v2-debug-4d16bf8` / tip `4d16bf8070865883c79b0e13a3c47e63d573311c`). Do NOT invent newer APK |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ec0384` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### This slice (`cursor/lynx-settings-archived-sessions-b1f2` / Next #90)

| Item | Notes |
|---|---|
| Cap skim | Cap **main** Settings home is 22 slugs including `archived-sessions` (`ArchivedSessionsPage` + `ArchivedSessionsManager mode="page"`). Tip Cap `packages/ui` still lists 21 — implement against Cap main. Lynx SessionsSheet already had ArchivedSessionsDialog + `listLynxArchivedSessions` / `unarchiveLynxSession`; this slice adds the Settings slug + in-tab page body (reuse, no duplicate API clients). Failure ≠ empty; honest no-runtime; no Cap toast / no invented ASR |
| Tip / APK honesty | Base tip `work/lynx-native` @ `c451d696bded3b8793d5a444b9ba39f43cff8dac` (PR #129 / Next #89 MERGED). Published APK `lynx-v2-debug-8ec0384` (release exists for tip `8ec03842f`). Do NOT invent a newer APK for this PR head |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ec0384` |
| Still deferred | Assistant unread + mark-all-read closed in Next #91. Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### This slice (`cursor/lynx-assistant-unread-4024` / Next #91)

| Item | Notes |
|---|---|
| Cap skim | Cap **main** Assistant unread + mark-all-read: `AssistantUnreadBadge` (99+, aria-label), `AssistantMarkAllReadButton` (ghost xs; hide when no unread+`readTip`), `AssistantReadMarker` (IntersectionObserver + bottom + focus), `POST /api/openchamber/assistants/:id/contact/read` `{ generation, ordinal, messageID }`, mark-all fanout batch 4 returning `{ failed }`. Lynx ports parse + real POST + catalog/dock badges + portable open/`readTip` mark. No Cap toast (inline banner). Host residual: Cap geometry observers |
| Tip / APK honesty | Base tip `work/lynx-native` @ `4cc762cd2a4a9be26e75cdef84e73b24b7d51b88` (PR #130 / Next #90 MERGED). Published APK `lynx-v2-debug-8ec0384` (release exists for tip `8ec03842f`). Do NOT invent a newer APK for this PR head |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on tip APK `lynx-v2-debug-8ec0384` |
| Still deferred | Cap IntersectionObserver read-marker geometry (host); Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-3a1d` / Next #92)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Next #90 archived-sessions **MERGED** (PR #130); Next #91 Assistant unread **MERGED** (PR #131) |
| Tip / APK honesty | Tip `work/lynx-native` @ `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65` (PR #131 / Next #91 MERGED). Published APK `lynx-v2-debug-4cc762c` (release exists for tip after PR #130 / `4cc762cd2`; was stale `lynx-v2-debug-8ec0384` / tip bases `8ec03842f` / `c451d696b` / `4cc762cd2` docs that predated this hop). No `lynx-v2-debug-3fda6eb` at write time — do NOT invent. Mobile CI may later publish for newer tip |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-4cc762c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-63da3d6-7704` / Next #93)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #92 **MERGED** (PR #132) |
| Tip / APK honesty | Tip `work/lynx-native` @ `63da3d63aa354eb66677f6ff57d5dd546ddca24b` (PR #132 / Next #92 MERGED). Published APK `lynx-v2-debug-63da3d6` (release exists; was stale `lynx-v2-debug-4cc762c` / tip `3fda6eb3f2f72580c7a25036c413fa8e5c17ab65`). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-63da3d6` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-50af885-1428` / Next #94)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #93 **MERGED** (PR #133) |
| Tip / APK honesty | Tip `work/lynx-native` @ `50af885b81849d92bed38116930abc3ddbbf88ef` (PR #133 / Next #93 MERGED). Published APK `lynx-v2-debug-50af885` (release exists; was stale `lynx-v2-debug-63da3d6` / tip `63da3d63aa354eb66677f6ff57d5dd546ddca24b`). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published tip APK `lynx-v2-debug-50af885` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-4520eb00-86f4` / Next #95)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #94 **MERGED** (PR #134). Same deferred list as #94 |
| Tip / APK honesty | Tip `work/lynx-native` @ `4520eb00be8bd1cc8faea2cca151d3d55ae39b91` (PR #134 / Next #94 MERGED). Published APK `lynx-v2-debug-50af885` (release exists; was the tip before this honesty merge / PR #133 / `50af885b81849d92bed38116930abc3ddbbf88ef`). Do NOT invent a newer APK than published (no `lynx-v2-debug-4520eb00`). Lynx Mobile CI on tip `4520eb00` FAILED at Setup Android SDK (`android-actions/setup-android@v3`: Failed to find package 'tools') — no newer published APK for `4520eb00` |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-50af885` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-ee82d53-99c4` / Next #96)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #95 **MERGED** (PR #136). Same deferred list as #95 |
| Tip / APK honesty | Tip `work/lynx-native` @ `ee82d53a736a321b6ad6fd4573b03575def90eeb` (PR #136 / Next #95 MERGED; includes PR #135 Mobile CI setup-android pin). Published APK `lynx-v2-debug-ee82d53` (release exists; was stale `lynx-v2-debug-50af885` / tip `4520eb00be8bd1cc8faea2cca151d3d55ae39b91`). PR #135 pinned setup-android packages (`tools` removed upstream → `packages:platform-tools`); Mobile CI SUCCESS on tip after #135/#136 republished `lynx-v2-debug-ee82d53`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ee82d53` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-d10c92d-eb2e` / Next #97)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #96 **MERGED** (PR #137). Same deferred list as #96 |
| Tip / APK honesty | Tip `work/lynx-native` @ `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464` (PR #137 / Next #96 MERGED). Published APK `lynx-v2-debug-d10c92d` (release exists; was stale `lynx-v2-debug-ee82d53` / tip `ee82d53a736a321b6ad6fd4573b03575def90eeb`). Lynx Mobile CI SUCCESS on tip `d10c92db` after #137 republished `lynx-v2-debug-d10c92d`. lynx-ci may not have re-fired on this docs-only tip push (OK; not claimed red). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d10c92d` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-a92b319-743c` / Next #98)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #97 **MERGED** (PR #138). Same deferred list as #97 |
| Tip / APK honesty | Tip `work/lynx-native` @ `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8` (PR #138 / Next #97 MERGED). Published APK `lynx-v2-debug-a92b319` (release exists; was stale `lynx-v2-debug-d10c92d` / tip `d10c92db2d5b6ad5c322a75bbfc8b9ca69c8d464`). Lynx Mobile CI SUCCESS on tip `a92b319` (run 34943000499) after #138 republished `lynx-v2-debug-a92b319`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a92b319` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-f1cf09e-2a7e` / Next #99)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #98 **MERGED** (PR #139). Same deferred list as #98 |
| Tip / APK honesty | Tip `work/lynx-native` @ `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2` (PR #139 / Next #98 MERGED). Published APK `lynx-v2-debug-f1cf09e` (release exists; was stale `lynx-v2-debug-a92b319` / tip `a92b319a1ed1d79c326d662c3d8beca3e2ff05b8`). Lynx Mobile CI SUCCESS on tip `f1cf09ef` (run 34956940347) after #139 republished `lynx-v2-debug-f1cf09e`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-f1cf09e` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-41d632a-0ec0` / Next #100)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #99 **MERGED** (PR #140). Same deferred list as #99 |
| Tip / APK honesty | Tip `work/lynx-native` @ `41d632abebf2c62088d46371bb5761c73925dc7b` (PR #140 / Next #99 MERGED). Published APK `lynx-v2-debug-41d632a` (release exists; was stale `lynx-v2-debug-f1cf09e` / tip `f1cf09ef9a3a085e8e2f0b2a2272e819795766e2`). Lynx Mobile CI SUCCESS on tip `41d632ab` (run 34958905237) after #140 republished `lynx-v2-debug-41d632a`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-41d632a` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-32aed54-6fc4` / Next #101)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #100 **MERGED** (PR #141). Same deferred list as #100 |
| Tip / APK honesty | Tip `work/lynx-native` @ `32aed54c6f1585f6da2a35c82ac16f78db92933e` (PR #141 / Next #100 MERGED). Published APK `lynx-v2-debug-32aed54` (release exists; was stale `lynx-v2-debug-41d632a` / tip `41d632abebf2c62088d46371bb5761c73925dc7b`). Lynx Mobile CI SUCCESS on tip `32aed54c` (run 34976026554) after #141 republished `lynx-v2-debug-32aed54`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-32aed54` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-105884e-849e` / Next #102)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #101 **MERGED** (PR #142). Same deferred list as #101 |
| Tip / APK honesty | Tip `work/lynx-native` @ `105884e5067dd5ba26bb49c2957bb255deea92cd` (PR #142 / Next #101 MERGED). Published APK `lynx-v2-debug-105884e` (release exists; was stale `lynx-v2-debug-32aed54` / tip `32aed54c6f1585f6da2a35c82ac16f78db92933e`). Lynx Mobile CI SUCCESS on tip `105884e` (run 35015640496) after #142 republished `lynx-v2-debug-105884e`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-105884e` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-ac8b4c9-55c6` / Next #103)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #102 **MERGED** (PR #143). Same deferred list as #102 |
| Tip / APK honesty | Tip `work/lynx-native` @ `ac8b4c99c5e821ac435bea83daec65eb8f7e7171` (PR #143 / Next #102 MERGED). Published APK `lynx-v2-debug-ac8b4c9` (release exists; was stale `lynx-v2-debug-105884e` / tip `105884e5067dd5ba26bb49c2957bb255deea92cd`). Lynx Mobile CI SUCCESS on tip `ac8b4c99` (run 35031338604) after #143 republished `lynx-v2-debug-ac8b4c9`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ac8b4c9` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-bb7aad6-e044` / Next #104)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #103 **MERGED** (PR #144). Same deferred list as #103 |
| Tip / APK honesty | Tip `work/lynx-native` @ `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76` (PR #144 / Next #103 MERGED). Published APK `lynx-v2-debug-bb7aad6` (release exists; was stale `lynx-v2-debug-ac8b4c9` / tip `ac8b4c99c5e821ac435bea83daec65eb8f7e7171`). Lynx Mobile CI SUCCESS on tip `bb7aad63` (run 35044475784) after #144 republished `lynx-v2-debug-bb7aad6`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-bb7aad6` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-14211c0-0287` / Next #105)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #104 **MERGED** (PR #145). Same deferred list as #104 |
| Tip / APK honesty | Tip `work/lynx-native` @ `14211c02c298ebfcf4b0e2d1720ee5c9635358bf` (PR #145 / Next #104 MERGED). Published APK `lynx-v2-debug-14211c0` (release exists; was stale `lynx-v2-debug-bb7aad6` / tip `bb7aad63b0ba4d3bb7a9627bf1780de1bdffbb76`). Lynx Mobile CI SUCCESS on tip `14211c02` (run 35055816332) after #145 republished `lynx-v2-debug-14211c0`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-14211c0` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-a9f6605-7da5` / Next #106)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #105 **MERGED** (PR #146). Same deferred list as #105 |
| Tip / APK honesty | Tip `work/lynx-native` @ `a9f6605301df86cbe2b8b7ad124aaac02142a834` (PR #146 / Next #105 MERGED). Published APK `lynx-v2-debug-a9f6605` (release exists; was stale `lynx-v2-debug-14211c0` / tip `14211c02c298ebfcf4b0e2d1720ee5c9635358bf`). Lynx Mobile CI SUCCESS on tip `a9f660530` (run 35069830726) after #146 republished `lynx-v2-debug-a9f6605`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a9f6605` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-7e4af21-05d8` / Next #107)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #106 **MERGED** (PR #147). Same deferred list as #106 |
| Tip / APK honesty | Tip `work/lynx-native` @ `7e4af21a4f651fdf051905572188456fef28e72c` (PR #147 / Next #106 MERGED). Published APK `lynx-v2-debug-7e4af21` (release exists; was stale `lynx-v2-debug-a9f6605` / tip `a9f6605301df86cbe2b8b7ad124aaac02142a834`). Lynx Mobile CI SUCCESS on tip `7e4af21` (run 35083475005) after #147 republished `lynx-v2-debug-7e4af21`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-7e4af21` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-22a4bc7-6fb9` / Next #108)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #107 **MERGED** (PR #148). Same deferred list as #107 |
| Tip / APK honesty | Tip `work/lynx-native` @ `22a4bc746ef851bc05d0710662e65d298fee540f` (PR #148 / Next #107 MERGED). Published APK `lynx-v2-debug-22a4bc7` (release exists; was stale `lynx-v2-debug-7e4af21` / tip `7e4af21a4f651fdf051905572188456fef28e72c`). Lynx Mobile CI SUCCESS on tip `22a4bc7` (run 35090970058) after #148 republished `lynx-v2-debug-22a4bc7`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-22a4bc7` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-d256387-150c` / Next #109)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #108 **MERGED** (PR #149). Same deferred list as #108 |
| Tip / APK honesty | Tip `work/lynx-native` @ `d256387d51cd4f166f86320d71b31804fe104243` (PR #149 / Next #108 MERGED). Published APK `lynx-v2-debug-d256387` (release exists; was stale `lynx-v2-debug-22a4bc7` / tip `22a4bc746ef851bc05d0710662e65d298fee540f`). Lynx Mobile CI SUCCESS on tip `d256387` (run 35104578745) after #149 republished `lynx-v2-debug-d256387`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d256387` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-50934d8-1f4a` / Next #110)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #109 **MERGED** (PR #150). Same deferred list as #109 |
| Tip / APK honesty | Tip `work/lynx-native` @ `50934d8cf44a463310284d55a815a1d7426fb78b` (PR #150 / Next #109 MERGED). Published APK `lynx-v2-debug-50934d8` (release exists; was stale `lynx-v2-debug-d256387` / tip `d256387d51cd4f166f86320d71b31804fe104243`). Lynx Mobile CI SUCCESS on tip `50934d8` (run 35158475515) after #150 republished `lynx-v2-debug-50934d8`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-50934d8` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-c510559-f08d` / Next #111)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #110 **MERGED** (PR #151). Same deferred list as #110 |
| Tip / APK honesty | Tip `work/lynx-native` @ `c510559b425dcd0a8e06e5961e849570e744f014` (PR #151 / Next #110 MERGED). Published APK `lynx-v2-debug-c510559` (release exists; was stale `lynx-v2-debug-50934d8` / tip `50934d8cf44a463310284d55a815a1d7426fb78b`). Lynx Mobile CI SUCCESS on tip `c510559` (run 35171179409) after #151 republished `lynx-v2-debug-c510559`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c510559` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-b32e74c-176f` / Next #112)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #111 **MERGED** (PR #152). Same deferred list as #111 |
| Tip / APK honesty | Tip `work/lynx-native` @ `b32e74c05ff9041d787442cc3ab4087d2f15e87e` (PR #152 / Next #111 MERGED). Published APK `lynx-v2-debug-b32e74c` (release exists; was stale `lynx-v2-debug-c510559` / tip `c510559b425dcd0a8e06e5961e849570e744f014`). Lynx Mobile CI SUCCESS on tip `b32e74c` (run 35181873152) after #152 republished `lynx-v2-debug-b32e74c`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b32e74c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-aa08bbf-a7ad` / Next #113)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #112 **MERGED** (PR #153). Same deferred list as #112 |
| Tip / APK honesty | Tip `work/lynx-native` @ `aa08bbf7ed68ce46503b21db444c4aafb1787448` (PR #153 / Next #112 MERGED). Published APK `lynx-v2-debug-aa08bbf` (release exists; was stale `lynx-v2-debug-b32e74c` / tip `b32e74c05ff9041d787442cc3ab4087d2f15e87e`). Lynx Mobile CI SUCCESS on tip `aa08bbf` (run 35195640711) after #153 republished `lynx-v2-debug-aa08bbf`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-aa08bbf` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-1142c4e-522c` / Next #114)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #113 **MERGED** (PR #154). Same deferred list as #113 |
| Tip / APK honesty | Tip `work/lynx-native` @ `1142c4e2b0863c45ee25bc2452ea03078220155e` (PR #154 / Next #113 MERGED). Published APK `lynx-v2-debug-1142c4e` (release exists; was stale `lynx-v2-debug-aa08bbf` / tip `aa08bbf7ed68ce46503b21db444c4aafb1787448`). Lynx Mobile CI SUCCESS on tip `1142c4e` (run 35209662353) after #154 republished `lynx-v2-debug-1142c4e`. Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-1142c4e` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-a0dd0a9-07b2` / Next #115)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #114 **MERGED** (PR #155). Same deferred list as #114 |
| Tip / APK honesty | Tip `work/lynx-native` @ `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18` (PR #155 / Next #114 MERGED). Published APK `lynx-v2-debug-a0dd0a9` (release exists; was stale `lynx-v2-debug-1142c4e` / tip `1142c4e2b0863c45ee25bc2452ea03078220155e`). Lynx Mobile CI SUCCESS on tip `a0dd0a9` (run 35211291083). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a0dd0a9` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-04325ba-04ae` / Next #116)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #115 **MERGED** (PR #156). Same deferred list as #115 |
| Tip / APK honesty | Tip `work/lynx-native` @ `04325ba8dfcd8700d4790670a03e2570d4f4acc4` (PR #156 / Next #115 MERGED). Published APK `lynx-v2-debug-04325ba` (release exists; was stale `lynx-v2-debug-a0dd0a9` / tip `a0dd0a93e016adca96ceca2fe3b9d0c710e7cd18`). Lynx Mobile CI SUCCESS on tip `04325ba` (run 35229914916). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-04325ba` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-4b4fbcc-2e79` / Next #117)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #116 **MERGED** (PR #157). Same deferred list as #116 |
| Tip / APK honesty | Tip `work/lynx-native` @ `4b4fbcc0f0e05eae50c12177f8486682a544b381` (PR #157 / Next #116 MERGED). Published APK `lynx-v2-debug-4b4fbcc` (release exists; was stale `lynx-v2-debug-04325ba` / tip `04325ba8dfcd8700d4790670a03e2570d4f4acc4`). Lynx Mobile CI SUCCESS on tip `4b4fbcc` (run 35249088590). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-4b4fbcc` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-554e70e-17cf` / Next #118)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #117 **MERGED** (PR #158). Same deferred list as #117 |
| Tip / APK honesty | Tip `work/lynx-native` @ `554e70e18dc18fe509ae02fdcdd43faead587ed1` (PR #158 / Next #117 MERGED). Published APK `lynx-v2-debug-554e70e` (release exists; was stale `lynx-v2-debug-4b4fbcc` / tip `4b4fbcc0f0e05eae50c12177f8486682a544b381`). Lynx Mobile CI SUCCESS on tip `554e70e` (run 35266255595). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-554e70e` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-6215ead-a6ae` / Next #119)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #118 **MERGED** (PR #159). Same deferred list as #118 |
| Tip / APK honesty | Tip `work/lynx-native` @ `6215ead5164c086d720629165ab31aac3c3b15a1` (PR #159 / Next #118 MERGED). Published APK `lynx-v2-debug-6215ead` (release exists; was stale `lynx-v2-debug-554e70e` / tip `554e70e18dc18fe509ae02fdcdd43faead587ed1`). Lynx Mobile CI SUCCESS on tip `6215ead` (run 35283067717). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6215ead` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-06d4ac4-58e5` / Next #120)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #119 **MERGED** (PR #160). Same deferred list as #119 |
| Tip / APK honesty | Tip `work/lynx-native` @ `06d4ac4138eba038259490d857be37ba59c8daad` (PR #160 / Next #119 MERGED). Published APK `lynx-v2-debug-06d4ac4` (release exists; was stale `lynx-v2-debug-6215ead` / tip `6215ead5164c086d720629165ab31aac3c3b15a1`). Lynx Mobile CI SUCCESS on tip `06d4ac4` (run 35295752653). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-06d4ac4` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-3a87f8b-c158` / Next #121)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #120 **MERGED** (PR #161). Same deferred list as #120 |
| Tip / APK honesty | Tip `work/lynx-native` @ `3a87f8bcd0e575a747909a6042ae881e8666098c` (PR #161 / Next #120 MERGED). Published APK `lynx-v2-debug-3a87f8b` (release exists; was stale `lynx-v2-debug-06d4ac4` / tip `06d4ac4138eba038259490d857be37ba59c8daad`; tip delta `06d4ac4`→`3a87f8b` is docs-only). Lynx Mobile CI SUCCESS on tip `3a87f8b` (run 35307346909). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3a87f8b` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-5018ded-2787` / Next #122)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #121 **MERGED** (PR #162). Same deferred list as #121 |
| Tip / APK honesty | Tip `work/lynx-native` @ `5018ded1c818d06d07c6a60667e36c16004f3d44` (PR #162 / Next #121 MERGED). Published APK `lynx-v2-debug-5018ded` (release exists; was stale `lynx-v2-debug-3a87f8b` / tip `3a87f8bcd0e575a747909a6042ae881e8666098c`; tip delta `3a87f8b`→`5018ded` is docs-only). Lynx Mobile CI SUCCESS on tip `5018ded` (run 35320424832). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-5018ded` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-c1e2f5c-9ae1` / Next #123)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #122 **MERGED** (PR #163). Same deferred list as #122 |
| Tip / APK honesty | Tip `work/lynx-native` @ `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af` (PR #163 / Next #122 MERGED). Published APK `lynx-v2-debug-c1e2f5c` (release exists; was stale `lynx-v2-debug-5018ded` / tip `5018ded1c818d06d07c6a60667e36c16004f3d44`; tip delta `5018ded`→`c1e2f5c` is docs-only). Lynx Mobile CI SUCCESS on tip `c1e2f5c` (run 35333814514). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c1e2f5c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-74d2d29-d266` / Next #124)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #123 **MERGED** (PR #164). Same deferred list as #123 |
| Tip / APK honesty | Tip `work/lynx-native` @ `74d2d29ac5311a4b72f52474f73027f729ab7944` (PR #164 / Next #123 MERGED). Published APK `lynx-v2-debug-74d2d29` (release exists; was stale `lynx-v2-debug-c1e2f5c` / tip `c1e2f5c67b5d8e0d75a4363dae1a61b8f95593af`; tip delta `c1e2f5c`→`74d2d29` is docs-only). Lynx Mobile CI SUCCESS on tip `74d2d29` (run 35335482752). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-74d2d29` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-cc15c1a-8aec` / Next #125)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #124 **MERGED** (PR #165). Same deferred list as #124 |
| Tip / APK honesty | Tip `work/lynx-native` @ `cc15c1aefdd41e909ed3f55aa1171e067be84adf` (PR #165 / Next #124 MERGED). Published APK `lynx-v2-debug-cc15c1a` (release exists; was stale `lynx-v2-debug-74d2d29` / tip `74d2d29ac5311a4b72f52474f73027f729ab7944`; tip delta `74d2d29`→`cc15c1a` is docs-only). Lynx Mobile CI SUCCESS on tip `cc15c1a` (run 35353222951). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-cc15c1a` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-592192f-0bda` / Next #126)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #125 **MERGED** (PR #166). Same deferred list as #125 |
| Tip / APK honesty | Tip `work/lynx-native` @ `592192f993ec3b429f49782c07d3c9194eea5127` (PR #166 / Next #125 MERGED). Published APK `lynx-v2-debug-592192f` (release exists; was stale `lynx-v2-debug-cc15c1a` / tip `cc15c1aefdd41e909ed3f55aa1171e067be84adf`; tip delta `cc15c1a`→`592192f` is docs-only). Lynx Mobile CI SUCCESS on tip `592192f` (run 35371422190). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-592192f` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-6d5ef04-ca08` / Next #127)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #126 **MERGED** (PR #167). Same deferred list as #126 |
| Tip / APK honesty | Tip `work/lynx-native` @ `6d5ef04efabf29704c60e2392f10a94c226bcb5c` (PR #167 / Next #126 MERGED). Published APK `lynx-v2-debug-6d5ef04` (release exists; was stale `lynx-v2-debug-592192f` / tip `592192f993ec3b429f49782c07d3c9194eea5127`; tip delta `592192f`→`6d5ef04` is docs-only). Lynx Mobile CI SUCCESS on tip `6d5ef04` (run 35388844201). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6d5ef04` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-6c1f6d4-323f` / Next #128)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #127 **MERGED** (PR #168). Same deferred list as #127 |
| Tip / APK honesty | Tip `work/lynx-native` @ `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a` (PR #168 / Next #127 MERGED). Published APK `lynx-v2-debug-6c1f6d4` (release exists; was stale `lynx-v2-debug-6d5ef04` / tip `6d5ef04efabf29704c60e2392f10a94c226bcb5c`; tip delta `6d5ef04`→`6c1f6d4` is docs-only). Lynx Mobile CI SUCCESS on tip `6c1f6d4` (run 35402341644). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6c1f6d4` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-3695bd3-b570` / Next #129)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #128 **MERGED** (PR #169). Same deferred list as #128 |
| Tip / APK honesty | Tip `work/lynx-native` @ `3695bd3f12086184a9c778201d232e92bc281f12` (PR #169 / Next #128 MERGED). Published APK `lynx-v2-debug-3695bd3` (release exists; was stale `lynx-v2-debug-6c1f6d4` / tip `6c1f6d4c61438e1678a8f1afafc283ed5d9fcc1a`; tip delta `6c1f6d4`→`3695bd3` is docs-only). Lynx Mobile CI SUCCESS on tip `3695bd3` (run 35412917473). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3695bd3` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-b23a0a4-aa04` / Next #130)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #129 **MERGED** (PR #170). Same deferred list as #129 |
| Tip / APK honesty | Tip `work/lynx-native` @ `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0` (PR #170 / Next #129 MERGED). Published APK `lynx-v2-debug-b23a0a4` (release exists; was stale `lynx-v2-debug-3695bd3` / tip `3695bd3f12086184a9c778201d232e92bc281f12`; tip delta `3695bd3`→`b23a0a4` is docs-only). Lynx Mobile CI SUCCESS on tip `b23a0a4` (run 35421462192). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b23a0a4` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-a11eb83-abb3` / Next #131)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #130 **MERGED** (PR #171). Same deferred list as #130 |
| Tip / APK honesty | Tip `work/lynx-native` @ `a11eb83c3136388ca951194466d949ec3c554956` (PR #171 / Next #130 MERGED). Published APK `lynx-v2-debug-a11eb83` (release exists; was stale `lynx-v2-debug-b23a0a4` / tip `b23a0a4b0e8c997d20ecbbeb85c45643fb3ac1d0`; tip delta `b23a0a4`→`a11eb83` is docs-only). Lynx Mobile CI SUCCESS on tip `a11eb83` (run 35429567861). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a11eb83` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-d8c0ed2-898e` / Next #132)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #131 **MERGED** (PR #172). Same deferred list as #131 |
| Tip / APK honesty | Tip `work/lynx-native` @ `d8c0ed2889958ea6a0882d6a887501c4d3ef7986` (PR #172 / Next #131 MERGED). Published APK `lynx-v2-debug-d8c0ed2` (release exists; was stale `lynx-v2-debug-a11eb83` / tip `a11eb83c3136388ca951194466d949ec3c554956`; tip delta `a11eb83`→`d8c0ed2` is docs-only). Lynx Mobile CI SUCCESS on tip `d8c0ed2` (run 35437528510). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-d8c0ed2` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-59b8a88-a009` / Next #133)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #132 **MERGED** (PR #173). Same deferred list as #132 |
| Tip / APK honesty | Tip `work/lynx-native` @ `59b8a88edbb7d1cd15b46cb01830f0180e0b073e` (PR #173 / Next #132 MERGED). Published APK `lynx-v2-debug-59b8a88` (release exists; was stale `lynx-v2-debug-d8c0ed2` / tip `d8c0ed2889958ea6a0882d6a887501c4d3ef7986`; tip delta `d8c0ed2`→`59b8a88` is docs-only). Lynx Mobile CI SUCCESS on tip `59b8a88` (run 35445932721). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-59b8a88` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-df87992-fa5f` / Next #134)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #133 **MERGED** (PR #174). Same deferred list as #133 |
| Tip / APK honesty | Tip `work/lynx-native` @ `df879926fd2c8d4ad18d26bdee033f9345cc5c47` (PR #174 / Next #133 MERGED). Published APK `lynx-v2-debug-df87992` (release exists; was stale `lynx-v2-debug-59b8a88` / tip `59b8a88edbb7d1cd15b46cb01830f0180e0b073e`; tip delta `59b8a88`→`df87992` is docs-only). Lynx Mobile CI SUCCESS on tip `df87992` (run 35455446737). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-df87992` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-b7db06b-c05d` / Next #135)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #134 **MERGED** (PR #175). Same deferred list as #134 |
| Tip / APK honesty | Tip `work/lynx-native` @ `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f` (PR #175 / Next #134 MERGED). Published APK `lynx-v2-debug-b7db06b` (release exists; was stale `lynx-v2-debug-df87992` / tip `df879926fd2c8d4ad18d26bdee033f9345cc5c47`; tip delta `df87992`→`b7db06b` is docs-only). Lynx Mobile CI SUCCESS on tip `b7db06b` (run 35464341914). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-b7db06b` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-3becdfa-7678` / Next #136)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #135 **MERGED** (PR #176). Same deferred list as #135 |
| Tip / APK honesty | Tip `work/lynx-native` @ `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8` (PR #176 / Next #135 MERGED). Published APK `lynx-v2-debug-3becdfa` (release exists; was stale `lynx-v2-debug-b7db06b` / tip `b7db06b75f9b9f37c7a598efaa03987c3a1bdd1f`; tip delta `b7db06b`→`3becdfa` is docs-only). Lynx Mobile CI SUCCESS on tip `3becdfa` (run 35473297750). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-3becdfa` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-165f27c-ea47` / Next #137)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #136 **MERGED** (PR #177). Same deferred list as #136 |
| Tip / APK honesty | Tip `work/lynx-native` @ `165f27c8a55edf929de37de42255c859eef2d34c` (PR #177 / Next #136 MERGED). Published APK `lynx-v2-debug-165f27c` (release exists; was stale `lynx-v2-debug-3becdfa` / tip `3becdfa5bd66cd9ae6006e9b4c91daa46ff933c8`; tip delta `3becdfa`→`165f27c` is docs-only). Lynx Mobile CI SUCCESS on tip `165f27c` (run 35481508022). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-165f27c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-ed50652-743f` / Next #138)

| Item | Notes |
|---|---|
| Cap skim | Cap phone (`packages/ui` MobileApp overflow / Files / Changes incl. stageAll/unstageAll/revertAll / SessionsSheet / chat header-swipe+sync hint / MCP sheet / ProjectsHome / Assistant / Scheduled list/editor/run/delete/history / Settings / Connect welcome) vs Lynx — no new Linux-closable Cap product gap outside deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only. Incremental leftovers (not product API wires): Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search filter wired (`filterLynxSettingsPages`); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. **NO_NEW_LINUX_CLOSABLE_PRODUCT_GAP**. Next #137 **MERGED** (PR #178). Same deferred list as #137 |
| Tip / APK honesty | Tip `work/lynx-native` @ `ed506522f7bab857424af81a703520558f1fe359` (PR #178 / Next #137 MERGED). Published APK `lynx-v2-debug-ed50652` (release exists; was stale `lynx-v2-debug-165f27c` / tip `165f27c8a55edf929de37de42255c859eef2d34c`; tip delta `165f27c`→`ed50652` is docs-only). Lynx Mobile CI SUCCESS on tip `ed50652` (run 35489088418). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-ed50652` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### This slice (`cursor/lynx-settings-search-input-3e1b` / Next #139) — **MERGED** as PR #181

| Item | Notes |
|---|---|
| Cap skim | Cap phone Settings search is a real `<input>` (`settingsSearchQuery` + `onChange`). Lynx filter (`filterLynxSettingsPages`) was already wired; the leftover was display-only search chrome (`LynxText` + height-0 stub). This slice replaces `SettingsSearchField` with editable `LynxInput` (`value` + `bindinput` → `onChange` / `setSearchQuery`, placeholder `lynx.settings.search.placeholder`). Host IME chrome polish / Cap clear-button pixel polish out of scope. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance. Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor`. `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover. Next #138 **MERGED** (PR #180) |
| Tip / APK honesty | Write-time base tip `work/lynx-native` @ `42bb986425ea447b164a779d8087f271630d75a4` (PR #180 / Next #138 MERGED). Published-at-merge APK `lynx-v2-debug-ed50652` (release existed for tip `ed506522f`). Do NOT invent `lynx-v2-debug-42bb986` as that head's APK. **MERGED** as PR #181 — current tip/APK honesty is Next #153 (`c3a062a134` / `lynx-v2-debug-c3a062a`) |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search field is 代码接上 (editable LynxInput), not 三关 landed / not 真机过 |
| 真机残差 | Unchanged at write — Connect welcome **card chrome** confirm for Yee; current confirm APK is `lynx-v2-debug-c3a062a` (Next #153) |
| Still deferred | Host IME chrome polish; Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-de4ff4e-a175` / Next #140) — **MERGED** as PR #182

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #139 **MERGED** (PR #181) |
| Tip / APK honesty | Tip `work/lynx-native` @ `de4ff4ea23f3446df77f8c1764e51507aabd9ce7` (PR #181 / Next #139 MERGED). Published APK `lynx-v2-debug-de4ff4e` (release exists; was stale `lynx-v2-debug-ed50652` / tip bases `ed506522f7bab857424af81a703520558f1fe359` / Next #139 base `42bb986425ea447b164a779d8087f271630d75a4` / published-at-merge APK for #139 head). Tip delta `42bb986`→`de4ff4ea23` includes product Settings search LynxInput (Next #139 / PR #181) then Mobile CI published `lynx-v2-debug-de4ff4e`. Lynx Mobile CI SUCCESS on tip `de4ff4e` (run 35497211638). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-de4ff4e` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-fa087e5-977b` / Next #141) — **MERGED** as PR #183

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #140 **MERGED** (PR #182) |
| Tip / APK honesty | Tip `work/lynx-native` @ `fa087e5bccd27bad126a5aedfab4255ce4553f75` (PR #182 / Next #140 MERGED). Published APK `lynx-v2-debug-fa087e5` (release exists; was stale `lynx-v2-debug-de4ff4e` / tip `de4ff4ea23f3446df77f8c1764e51507aabd9ce7`; tip delta `de4ff4e`→`fa087e5` is docs-only). Lynx Mobile CI SUCCESS on tip `fa087e5` (run 35505471290). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-fa087e5` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-5f98d1c-0e2e` / Next #142) — **MERGED** as PR #184

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #141 **MERGED** (PR #183) |
| Tip / APK honesty | Tip `work/lynx-native` @ `5f98d1c5427a064f60cddf743e88ba6ad454f1da` (PR #183 / Next #141 MERGED). Published APK `lynx-v2-debug-5f98d1c` (release exists; was stale `lynx-v2-debug-fa087e5` / tip `fa087e5bccd27bad126a5aedfab4255ce4553f75`; tip delta `fa087e5`→`5f98d1c` is docs-only). Lynx Mobile CI SUCCESS on tip `5f98d1c` (run 35513768378). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-5f98d1c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-9fc7723-757d` / Next #143) — **MERGED** as PR #185

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #142 **MERGED** (PR #184) |
| Tip / APK honesty | Tip `work/lynx-native` @ `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e` (PR #184 / Next #142 MERGED). Published APK `lynx-v2-debug-9fc7723` (release exists; was stale `lynx-v2-debug-5f98d1c` / tip `5f98d1c5427a064f60cddf743e88ba6ad454f1da`; tip delta `5f98d1c`→`9fc7723` is docs-only). Lynx Mobile CI SUCCESS on tip `9fc7723` (run 35523180671). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-9fc7723` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-8410742-27a6` / Next #144) — **MERGED** as PR #186

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #143 **MERGED** (PR #185) |
| Tip / APK honesty | Tip `work/lynx-native` @ `84107429dc9cd9e98d9de82807aa509d2bf8a496` (PR #185 / Next #143 MERGED). Published APK `lynx-v2-debug-8410742` (release exists; was stale `lynx-v2-debug-9fc7723` / tip `9fc7723e6ea4deb1895d1f3736a5ff63528cbb9e`; tip delta `9fc7723`→`8410742` is docs-only). Lynx Mobile CI SUCCESS on tip `8410742` (run 35532791147). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-8410742` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-c3cb78a-6321` / Next #145) — **MERGED** as PR #187

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #144 **MERGED** (PR #186) |
| Tip / APK honesty | Tip `work/lynx-native` @ `c3cb78a79bf80d0a90d448bb7133adc5869084d6` (PR #186 / Next #144 MERGED). Published APK `lynx-v2-debug-c3cb78a` (release exists; was stale `lynx-v2-debug-8410742` / tip `84107429dc9cd9e98d9de82807aa509d2bf8a496`; tip delta `8410742`→`c3cb78a` is docs-only). Lynx Mobile CI SUCCESS on tip `c3cb78a` (run 35542179230). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c3cb78a` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |


### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-7227ddc-4eb3` / Next #146) — **MERGED** as PR #188

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #145 **MERGED** (PR #187) |
| Tip / APK honesty | Tip `work/lynx-native` @ `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5` (PR #187 / Next #145 MERGED). Published APK `lynx-v2-debug-7227ddc` (release exists; was stale `lynx-v2-debug-c3cb78a` / tip `c3cb78a79bf80d0a90d448bb7133adc5869084d6`; tip delta `c3cb78a`→`7227ddc` is docs-only). Lynx Mobile CI SUCCESS on tip `7227ddc` (run 35551435386). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-7227ddc` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |


### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-6b78c3d-7dfd` / Next #147) — **MERGED** as PR #189

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #146 **MERGED** (PR #188) |
| Tip / APK honesty | Tip `work/lynx-native` @ `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d` (PR #188 / Next #146 MERGED). Published APK `lynx-v2-debug-6b78c3d` (release exists; was stale `lynx-v2-debug-7227ddc` / tip `7227ddcafad8fa9b85c3d550b1bcd3eacf9806f5`; tip delta `7227ddc`→`6b78c3d` is docs-only). Lynx Mobile CI SUCCESS on tip `6b78c3d` (run 35561042584). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-6b78c3d` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |


### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-a46ac25-3b5b` / Next #148) — **MERGED** as PR #190

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #147 **MERGED** (PR #189) |
| Tip / APK honesty | Tip `work/lynx-native` @ `a46ac25c3fe489f5727362912740a4622976414c` (PR #189 / Next #147 MERGED). Published APK `lynx-v2-debug-a46ac25` (release exists; was stale `lynx-v2-debug-6b78c3d` / tip `6b78c3d41a54ad1e39c7b2ca503db8df7fd4e54d`; tip delta `6b78c3d`→`a46ac25` is docs-only). Lynx Mobile CI SUCCESS on tip `a46ac25` (run 35574289217). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-a46ac25` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-38b3175-4ca9` / Next #149) — **MERGED** as PR #191

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #148 **MERGED** (PR #190) |
| Tip / APK honesty | Tip `work/lynx-native` @ `38b31757b7460ab6781edb71acc9e933fab30e0c` (PR #190 / Next #148 MERGED). Published APK `lynx-v2-debug-38b3175` (release exists; was stale `lynx-v2-debug-a46ac25` / tip `a46ac25c3fe489f5727362912740a4622976414c`; tip delta `a46ac25`→`38b3175` is docs-only). Lynx Mobile CI SUCCESS on tip `38b3175` (run 35588189358). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-38b3175` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-daca88c-80a9` / Next #150) — **MERGED** as PR #192

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #149 **MERGED** (PR #191) |
| Tip / APK honesty | Tip `work/lynx-native` @ `daca88c832f3df0d1a59bf5ca3eaa828f1885656` (PR #191 / Next #149 MERGED). Published APK `lynx-v2-debug-daca88c` (release exists; was stale `lynx-v2-debug-38b3175` / tip `38b31757b7460ab6781edb71acc9e933fab30e0c`; tip delta `38b3175`→`daca88c` is docs-only). Lynx Mobile CI SUCCESS on tip `daca88c` (run 35589404227). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-daca88c` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-dcb67d6-7620` / Next #151) — **MERGED** as PR #193

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + Settings search gap closed in #139. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #150 **MERGED** (PR #192) |
| Tip / APK honesty | Tip `work/lynx-native` @ `dcb67d6075b670ce24fb6efcf19384ae8adbc07e` (PR #192 / Next #150 MERGED). Published APK `lynx-v2-debug-dcb67d6` (release exists; asset `openchamber-lynx-debug-dcb67d6.apk`; was stale `lynx-v2-debug-daca88c` / tip `daca88c832f3df0d1a59bf5ca3eaa828f1885656`; tip delta `daca88c`→`dcb67d6` is docs-only). Lynx Mobile CI SUCCESS on tip `dcb67d6` (run 35610772314). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-dcb67d6` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-309af29-35bd` / Next #152) — **MERGED** as PR #194

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + docs-only tip delta. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #151 **MERGED** (PR #193) |
| Tip / APK honesty | Tip `work/lynx-native` @ `309af29373d68d7d4ea3757af9adf4896946490f` (PR #193 / Next #151 MERGED). Published APK `lynx-v2-debug-309af29` (release exists; asset `openchamber-lynx-debug-309af29.apk`; was stale `lynx-v2-debug-dcb67d6` / tip `dcb67d6075b670ce24fb6efcf19384ae8adbc07e`; tip delta `dcb67d6`→`309af29` is docs-only). Lynx Mobile CI SUCCESS on tip `309af29` (run 35629501281). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-309af29` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Docs honesty this slice (`cursor/lynx-docs-tip-apk-honesty-c3a062a-0299` / Next #153)

| Item | Notes |
|---|---|
| Cap skim | Cap skim pending / **NO_NEW** expected from prior skim + docs-only tip delta. Incremental leftovers unchanged: Scheduled history first page already 代码接上 (Cap load-more/`nextCursor` + per-task filter stay polish); Settings search field 代码接上 (`LynxInput` + `bindinput`, Next #139), not 三关 landed; session-row question/permission badges + Files image/markdown preview stay polish / host. SessionsSheet/ProjectsHome row Stop is extra affordance (`abortSession` already wired from composer/goal). Active Show more beyond session-index snapshot is the same class as Scheduled history `nextCursor` (index-driven list). `GET /api/client-auth/connection/candidates` is post-connect lifecycle leftover, not welcome-blocking. Deferred thin leftovers (`@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast sonner / bulk multi-select) + host-only unchanged. Next #152 **MERGED** (PR #194) |
| Tip / APK honesty | Tip `work/lynx-native` @ `c3a062a134a9796041ea605eb2e1acc66a2b289d` (PR #194 / Next #152 MERGED). Published APK `lynx-v2-debug-c3a062a` (release exists; asset `openchamber-lynx-debug-c3a062a.apk`; was stale `lynx-v2-debug-309af29` / tip `309af29373d68d7d4ea3757af9adf4896946490f`; tip delta `309af29`→`c3a062a` is docs-only). Lynx Mobile CI SUCCESS on tip `c3a062a` (run 35646713115). Do NOT invent a newer APK than published |
| Product status | **NOT DONE** / 三关未齐 / not EXHAUSTED / no 真机过 claim. Settings search LynxInput stays 代码接上 (Next #139), not 三关 landed |
| 真机残差 | Unchanged — Connect welcome **card chrome** confirm for Yee on published APK `lynx-v2-debug-c3a062a` |
| Still deferred | Cap `@dnd-kit` / MobileWindowMotion / iPad / Cap toast / bulk; host-only unchanged (Keychain/camera/IME/Pierre/WKWebView/Mode B/Share ext/ShareReceiverActivity/iOS IPA/FCM for lynx.debug/Live Activity/clipboard host) |

### Still missing / host-only / 真机


| Remaining | Why |
|---|---|
| Native Keychain / Keystore live OS wiring | Stubs + JS adapter 代码接上; SecItem / EncryptedSharedPreferences still 真机 |
| Real QR camera / AVCapture / CameraX | Adapter + chrome 代码接上; binder returns unavailable until host |
| OAuth system browser open | Cap routes + host stubs 代码接上; ASWebAuthenticationSession / Custom Tabs 真机 |
| Host WKWebView / WebView HTML preview sheet | Text stub only — no invent Lynx DOM iframe |
| PierreDiff interactive viewer | **Blocked:** `@pierre/diffs` needs `diffs-container` Shadow DOM + `react-dom` + worker; Lynx/rspeedy has none. Feature path → portable-text only (`resolveLynxPierreDiffFeature`). Do not fake CSS/iframe. |
| Composer glass / actions / pickers 真机 / Mode B host overlay | GlassCard + in-glass actions + half-height resizable Agent·model sheets JS 代码接上; live UIGlassEffect + host Mode B overlay / drag feel still 真机 |
| IME keyboard binding + occupancy 真机 | Contract + inset publisher stubs 代码接上; LynxView IME must be host-bound |
| Edge-swipe / Predictive Back pan arena 真机 | Contract + stubs 代码接上; native gesture ownership still host |
| Haptics / HEIC / media pick / APNs·FCM token mint | Adapters 代码接上; native plugins still host |
| Share extension / Android `ShareReceiverActivity` | **Host-only** (separate from Next #36 JS full-page picker). Inbox + recipient picker 代码接上; native extension/receiver still host |
| Native clipboard binder | Next #37 copyLink uses `navigator.clipboard` when present; otherwise labeled unavailable — host clipboard plugin still host-only |
| MobileSessionStatusBar / sessions sheet | Slim strip 代码接上 in #39; full Cap sessions sheet 代码接上 in #42; worktree create/delete dialogs 代码接上 in #43; remote-branch delete 代码接上 in #44; two-step archive + unarchive undo 代码接上 in #45; ArchivedSessionsDialog 代码接上 in #46; smart-title 代码接上 in #47; tree archive/delete + delete undo 代码接上 in #48; MobileProjectEditSurface 代码接上 in #49; header trailing newChat/worktree/addProject 代码接上 in #50; ProjectsHome per-bucket Show more 代码接上 in #52; Settings `archived-sessions` slug + page body 代码接上 in #90; Assistant unread + mark-all-read 代码接上 in #91 (portable open/`readTip` mark; Cap IntersectionObserver geometry still host residual). Remaining: Cap `@dnd-kit` / MobileWindowMotion / iPad sidebar / Cap toast lib (sonner) / bulk multi-select (desktop sidebar folders) |
| Cap server message-queue | Thin Lynx client 代码接上 in #41 (`messageQueueServer` + composer wire); full Cap TanStack/Zustand sync + `@dnd-kit` still deferred |
| Live Activity / Widgets / Control Center | iOS-only host — later |
| Android sideload APK + lynx-ci | **Live** on tip (`lynx-mobile-ci` assembleRelease + prerelease `lynx-v2-debug-*`; lynx-ci green). First-paint cream/globalProps 代码接上 — **not** 真机过. |
| iOS IPA / CocoaPods Lynx resolve | Still missing on Linux; Mac/device required |
| FCM for `com.yee94.openchamber.lynx.debug` | Sideload id unique from Cap; Firebase Android app not registered yet (pitfalls §6) |
| Bonjour / Nearby, invented ASR, TanStack 1.18, Capgo OTA | 故意不移植 |
| 真机过 | Empty 真机残差 until a written device log |

Do **not** mark product rows **landed** under 三关 until CI绿 (track workflow running on GH) + 真机过.

## How to move a row

1. Implement against the inventory source, not a screenshot guess.
2. Add or update a focused test / harness (`docs/lynx-acceptance.md`).
3. Land on `work/lynx-native` only.
4. Flip **missing → landed** only after 代码接上 + CI绿.
5. Flip **landed →** keep a **真机残差** row until a physical device pass is written down.
6. Never silently recategorize a missing feature as 故意不移植 to make the board look green.